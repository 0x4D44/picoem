[package]
name = "mdpicoem-devices"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Off-chip device models (PSRAM, LCD, I2S) for the mdpicoem RP2040/RP2350 emulator workspace."
repository = "https://github.com/0x4D44/mdpicoem"
homepage = "https://github.com/0x4D44/mdpicoem"
documentation = "https://docs.rs/mdpicoem-devices"
readme = "README.md"
keywords = ["emulator", "psram", "embedded", "device-model", "pico"]
categories = ["emulators", "embedded", "hardware-support"]

[dependencies]
tracing = { workspace = true }
