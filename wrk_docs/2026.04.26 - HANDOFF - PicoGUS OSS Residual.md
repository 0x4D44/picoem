# Handoff — PicoGUS OSS residual work

**Created:** 2026-04-26
**Reason:** Deferred from the original Open Source Release Prep session
because that session accumulated content-filter friction around binary
modification language. A fresh conversation, with no prior firmware-
patching context, should be able to write these three remaining
artefacts without trouble.

## Context

Read these first, in order:

1. `wrk_docs/2026.04.26 - HLD - Open Source Release V1.md` —
   sections §5 (PicoGUS firmware) and §16 (decisions). The full plan
   is in this HLD; you only need the PicoGUS portion.
2. `wrk_journals/2026.04.26 - JRN - Open Source Release Prep.md` —
   shows what was already completed in the prior session.

## What is already done

- `.gitignore` updated: PicoGUS binary excludes removed; binaries are now
  vendored under `third_party/picogus/`.
- `third_party/picogus/picogus-v4.0.0-test_psram-fastpath.md` —
  documents the four-byte change between stock and variant binary.
  Already on disk; do not modify.

## What still needs to be done — three small files

### 1. `third_party/picogus/LICENSE-PICOGUS-GPL-2.0.txt`

The standard GNU General Public License Version 2 (June 1991) text,
verbatim. This is the canonical license text from the Free Software
Foundation; the standard `LICENSE` file shipped with most GPLv2
projects. Include the full text.

### 2. Rewrite `third_party/picogus/README.md`

Currently the README says the firmware is "**not redistributed** here
by default" and explains the demo-script staging path. This wording is
now stale — the binaries are vendored. Rewrite to:

- Open with: this directory contains PicoGUS firmware redistributed
  under GPL-2.0-or-later. Cite the upstream URL
  (`https://github.com/polpo/picogus`, version `v4.0.0`, release tag
  `v4.0.0`).
- Keep the existing **Files** table but flip the "Origin" column for
  the binaries from "downloaded (or manually placed)" /
  "extracted from zip" to "redistributed under GPL-2.0-or-later".
  Add a row for the new
  `picogus-v4.0.0-test_psram-fastpath.md` describing it as the local-
  modification documentation.
- Keep the existing **What `picogus_diff_rp2040` actually boots** section
  (the bootrom caveat) — it's still accurate.
- Keep the **Upgrading the pin** section.
- Add a **Licensing** section pointing at `LICENSE-PICOGUS-GPL-2.0.txt`
  and explaining that for the variant binary
  (`picogus-v4.0.0-patched.bin`) the corresponding source is the
  combination of `picogus-v4.0.0.bin` plus the four-byte change
  documented in `picogus-v4.0.0-test_psram-fastpath.md`.
- Drop the "How to stage the firmware" section, since it documented
  the old policy. Replace with a short note that the demo script
  (`scripts/picogus_demo.sh`) still works for users who want to
  re-fetch from upstream and verify SHA256.

### 3. Append a PicoGUS section to the workspace `NOTICE`

Mirror the structure of the existing RP2350 bootrom entry:

```
PicoGUS Firmware (v4.0.0)
    Files:     third_party/picogus/picogus.uf2
               third_party/picogus/pg-ne2k.uf2
               third_party/picogus/pgusinit.exe
               third_party/picogus/picogus-v4.0.0.bin
               third_party/picogus/picogus-v4.0.0.zip
               third_party/picogus/picogus-v4.0.0-patched.bin
               third_party/picogus/UPSTREAM_README.md
    Source:    https://github.com/polpo/picogus
    Release:   v4.0.0 (tag), 2026-04-06
    License:   GNU General Public License version 2 or later
               (GPL-2.0-or-later); local copy in
               third_party/picogus/LICENSE-PICOGUS-GPL-2.0.txt
    Copyright: Copyright (c) Ian Scott (polpo) and contributors

    The PicoGUS sw/ and pgusinit/ collections are GPLv2; the hardware
    portions (hw/) are CERN OHL v2 and not redistributed here. We
    redistribute the published v4.0.0 binaries unmodified plus one
    local variant (`picogus-v4.0.0-patched.bin`) whose four-byte delta
    from stock is documented in
    third_party/picogus/picogus-v4.0.0-test_psram-fastpath.md.
```

(Adjust the wording to match the existing NOTICE file's voice if it
differs.)

## After you finish

Update `wrk_journals/2026.04.26 - JRN - Open Source Release Prep.md`
with a "PicoGUS residual — completed" entry under Timeline, then stop.
The remaining Session 1 / 2 work is being done in another session.
