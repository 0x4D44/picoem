# 2026.04.23 — PicoGUS audio quality: I2S-framing hunt

## Context (continued from earlier 2026-04-23 journal)

Interpolator CLAMP16 fix (commit `b4c532c`) broke the silence:
4-second Monkey Island replay now produces peak 32767, ~11k non-zero
samples, with the non-zero window aligned to voice-1 activation at
trace-ns 3.49 s.

Remaining problem — audio is **bimodal**: every non-zero sample sits
either at the clamp rails (|x| > 30 000) or near zero (|x| < 1 000),
with essentially nothing in between. Only 27 sign transitions across
11 246 non-zero samples. This is not what a GUS rendering of the
Monkey Island theme should look like.

Strong prior evidence from yesterday's stub-1 experiment: pushing a
constant `0xFF` to the I2S TX FIFO came out of the WAV as
`L=127, R=-32768`. `127 = 0xFF >> 1` → 1-bit MSB shift. `-32768 = 0x8000`
→ canonical "MSB-set" value, suggesting channel misalignment. Between
those two observations, the I2S capture path is a prime suspect for
the bimodal quality issue, independent of any mixer bug.

## Hypothesis

The I2S capture in the harness (or the PIO-side I2S TX implementation)
is off by 1 bit and/or misaligned between L/R channels. That would
map legitimate mid-range mixer output to rail-ish or zero-ish values
depending on which bit position the sign bit lands in.

## Plan

1. Locate the WAV capture path in `picogus_diff_rp2040.rs` and the
   PIO1 SM3 TX setup / autopush logic.
2. Cross-check bit ordering: PicoGUS pushes a packed `(RIGHT<<16)|LEFT`
   32-bit word; I2S is typically MSB-first, 16-bit per channel; figure
   out exactly where each bit lands in our capture.
3. Add a stub that pushes a handful of known 32-bit patterns (e.g.
   `0x0001_0002`, `0x7FFF_8001`, `0x8000_0000`) and see how they come
   out of the WAV.
4. Fix whichever side is wrong.

## Work log

### Step 1 — found ground truth for the PIO timing

`target/picogus_refs/audio_i2s.pio` (standard `pico-extras` program).
Config: `shift_left=true` (MSB first), autopull threshold 32. Side-set
pin 0 = BCLK, pin 1 = LRCLK. Comment declares the FIFO word layout as
`| 31:16 ws=0 | 15:0 ws=1 |`.

Walking the program reveals **Philips one-BCLK-delay** timing:

- Bits 31..17 latched with LRCLK=1 (15 bits, inside `bitloop1`).
- Bit 16 latched with LRCLK=0 (immediately after LRCLK fell).
- Bits 15..1 latched with LRCLK=0 (15 bits, inside `bitloop0`).
- Bit 0 latched with LRCLK=1 (immediately after LRCLK rose).

So each 16-bit channel word is 15 bits on its "own" LRCLK side +
1 LSB bit on the *other* side. The LRCLK edges happen **one BCLK
before** the next word's MSB.

### Step 2 — confirmed the bug arithmetically

Our I2sCapture (`crates/mdpicoem-devices/src/i2s_capture.rs`) is a
**naive non-Philips receiver**: it commits 16 bits per LRCLK window
directly. The 16 bits captured during LRCLK=1 actually belong to:
LSB of previous LEFT (bit 0, 1 bit) + top 15 bits of current RIGHT
(bits 31..17, 15 bits). Similarly for LRCLK=0.

For firmware packing `(RIGHT<<16) | LEFT`, the naive receiver reads:
- `R_heard = (LSB_prev_LEFT << 15) | (RIGHT >> 1)`
- `L_heard = (LSB_prev_RIGHT << 15) | (LEFT >> 1)`

Plugging in the stub-1 observation (constant TXF=`0x000000FF`, so
RIGHT=0 LEFT=255):
- `R_heard = (1 << 15) | (0 >> 1) = 0x8000 = -32768` ✓
- `L_heard = (0 << 15) | (0xFF >> 1) = 0x7F = 127` ✓

Matches observed values exactly. The I2S framing diagnosis from
yesterday is now fully explained.

### Step 3 — Philips-aware fix for I2sCapture

Implemented in `crates/mdpicoem-devices/src/i2s_capture.rs`
(bump 0.7.0 → 0.7.1):

- Added `finalizing_channel: Option<Channel>`. On LRCLK edge when
  `bit_count == 15`, the 15-bit accumulator is held and
  `finalizing_channel` is set to the channel that was active during
  those 15 MSBs. On the first BCLK rising edge after the edge, that
  bit is shifted in as the word's LSB and the 16-bit sample is
  committed. The new channel only starts accumulating from the
  SECOND BCLK after the LRCLK edge.
- Added `pending_right` alongside `pending_left`. Frame pairing now
  emits whenever the mate channel arrives, regardless of order —
  necessary because pico-audio-i2s.pio sends RIGHT first per frame
  (bits 31..16 under LRCLK=1).
- Rewrote tests around a new `clock_philips_frame` helper that
  matches the PIO bit-ordering exactly. Added a
  `stub1_constant_0xff_decodes_correctly` test that replays the
  2026-04-22 stub-1 experiment: FIFO = `0x000000FF` must decode as
  `L=0x00FF, R=0x0000` (not the old `L=127, R=-32768`).
- All 35 mdpicoem-devices tests pass, including the new oracle.

### Step 4 — 4-second replay re-run

Concurrent-agent note: while implementing, commit `8498485 picogus:
I2S framing fix + UART/PWM diagnostic taps + replay driver` landed
on main with the *same diagnosis and almost-identical fix*. Converged
independently — strong cross-check that Philips one-BCLK delay is
the correct root cause. My local tree's fix matches HEAD after the
merge.

Ran with `PRE_ROLL=2.0 DURATION=4.0 POST_ROLL=0.1 PICOGUS_IDLE_CYCLES=2000
PICOGUS_PSRAM_PRESEED=1 OUT_WAV=/tmp/philips_fix_v1.wav`.
Wall time 631 s.

**WAV result (`/tmp/philips_fix_v1.wav`):**

| | | |
|---|---|---|
| Peak \|L\| | 41 | (tiny — no more rails!) |
| Peak \|R\| | 19 | |
| Non-zero L | 9 379 / 346 722 | (~2.7 %) |
| Non-zero R | 9 196 / 346 722 | |
| Unique L values | 44 | |
| Sign transitions L | **1** | (essentially monotonic) |

The bimodal "rails-or-zero" pattern is **gone**. Output is now in a
plausible linear range. But peak is very small (41 vs a 16-bit
full-scale of 32767, i.e. ~-58 dB) and there's essentially only ONE
sign transition across the full non-zero window — not music.

Inspection of the L channel: first non-zero sample is at frame
337 343 (= trace-time ~3.49 s, which is exactly when voice 1 first
activates per the trace). The burst runs for ~9 k frames and the
value slowly climbs: the first 40 samples go `-3, -4, -4, 36, 34, 34,
... 35, 35 ...`. The last 40 samples are `1, 1, 1, 1, ...` all
ones. So the envelope ramps up then clearly starts ramping down at
the end.

This strongly suggests we're only capturing the **attack phase** of
the first note. Voice 1 activates at 3.49 s; the 4-s duration caps
us at sim-time 3.80 s — only 300 ms of note. VolLeft = 21 at the
end of the run (per yesterday's dump), so the volume ramp never
reached target.

### Step 5 — 6-second replay aborted: trace has a 4–7s gap

The 6-second replay's `Final sim time` was still 3.798 s — identical
to the 4-second run. Investigation:

```
$ awk ... | bucket-count ns/1e9
0s: 514503
1s: 0
2s: 0
3s: 165
4-6s: 0  ← empty gap
7s: 66
8s: 66
9s: 76
10s: 71
...
24s: 919
25s: 831
26s: 1472
...
```

The Monkey Island trace has its bulk events at 24–29 s (probably the
first big musical phrase). Between 4 s and 7 s, the trace is **empty** —
no ISA writes at all. So `DURATION=6.0` caps at the first event past
6 s (the ~7 s cluster), breaks out, and `final_sim_ns` stays at
3.798 s.

This also explains why yesterday's 4-second run showed only ~300 ms
of voice playback: the ISA writes at 3.5 s are the note-on for voice
1, but the NEXT cluster of ramp/volume events is at 7–10 s. Our
"4-second replay" was capturing only the pre-attack silence + initial
note-on attack.

### Step 6 — 11-second replay scheduled (in progress)

`PRE_ROLL=2.0 DURATION=11.0 POST_ROLL=0.1` — spans up to event
cluster at 10.17 s (port 0x342–0x345 writes — looks like voice
register setup for multiple voices). Expect:
- More voice activations (currently only voice 1 is active).
- VolLeft to ramp further.
- Audible mid-range samples, not just early-attack near-zero.

Caveat: expecting ~25–40 minutes wall time based on the simulator's
0.006x ratio.

(running)




