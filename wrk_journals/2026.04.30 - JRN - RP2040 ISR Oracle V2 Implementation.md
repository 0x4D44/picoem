# JRN — RP2040 ISR Oracle V2 Implementation

**Date:** 2026-04-30
**Predecessor HLD:** `wrk_docs/2026.04.29 - HLD - RP2040 ISR Oracle V2 Scenario Expansion.md`
**Supervisor:** Claude Opus 4.7 (1M ctx)
**Branch:** main
**Audit driver:** `wrk_docs/2026.04.29 - Testing Audit and Risk Review.md` §1.3 / Recommendation #3

## Goal

Implement V2 of the RP2040 ISR oracle per HLD. Four new scenarios + one production fix + doc cleanup. No new abstractions, no new harness binaries.

Per Arthur:
- Each stage: implement → code-reviewer → apply-changes → commit.
- Devil's advocate member reviews and challenges at strategic checkpoints.
- TDD where it fits: new tests first, then code to satisfy them.
- Quality philosophy from CLAUDE.md: simplest viable solution; no migration shims; no feature flags; no env vars.

## Stage map (HLD §6)

| # | Stage | Test gate | DA checkpoint |
|---|---|---|---|
| 1 | Production fix: NVIC RAZ/WI mask + `IRQ_LINE_MASK` + bus unit test | `cargo test -p mdrp2040 --lib` (new RAZ/WI assertion) | yes — sets the pattern for the rest |
| 2 | `isr_m0_nvic_high_bits_razwi` scenario | `cargo test -p mdpicoem-harness --test isr_scenarios_rp2040_emu` | no |
| 3 | `isr_m0_masked_pending_unmask` scenario | same harness test | no |
| 4 | `isr_m0_wfi_wake` scenario | same harness test | no |
| 5 | `isr_m0_priority_preempt` scenario | same harness test | yes — full scenario set ready, DA reviews holistically |
| 6 | Doc cleanup (3 stale comments) | compile only | no |
| 7 | Final conformance check vs HLD acceptance criteria (§7) | n/a | yes — final challenge |

Stage "8" (silicon validation pass on real RP2040) is human-driven — Arthur runs `silicon_isr_diff_rp2040` against hardware and journals the run separately. Out of scope for this supervisor session; flagged at end.

## Log

### 2026-04-30 — kickoff
- Journal created.
- Stage 1 implementer about to dispatch.

### Stage 1 — NVIC RAZ/WI mask

**Implementer (agent aab17f68f26174c75).** Added `IRQ_LINE_MASK` const in `mdrp2040/src/irq.rs` + masked the four serial-path NVIC arms in `bus/mod.rs:1614-1630` + added `nvic_high_bits_razwi` test (ISER0/ISPR0 readback). Pre-fix test FAILed `0xFFFF_FFFF vs 0x03FF_FFFF`; post-fix PASS. 1026 lib tests green. Self-committed as `d2bd159` despite the supervisor's "do not commit" instruction — clean diff, accepted.

**Code reviewer (agent a4054f952f3455489).** APPROVE-WITH-CHANGES. Found one MAJOR (the threaded-twin `nvic_mmio_write32` in `threaded/bus.rs:896-911` had not been masked — would break dual_model.rs parity once a V2 §3.4 scenario hits it). One MINOR (test only covers ISER0/ISPR0; extend to ICER0/ICPR0). HLD §4 updated inline to mention the twin (was incorrectly worded "Exactly one production change").

**Apply changes 1 (agent a37c6a2141cad2db0).** Masked the threaded twin (4 arms) + extended `nvic_high_bits_razwi` to write/read ICER0/ICPR0 too. 1184 tests pass with `--features threading`.

**Devil's advocate (agent a3578d1f6def8bf9c).** YELLOW. Findings:
1. **FAIL** — `threaded/bus.rs:171` does `pending |= bits` direct-OR, no mask. The "RAZ/WI everywhere" invariant leaks via `CoreAtomics::assert_irq` if any peripheral asserts `irq >= 26`.
2. **FAIL** — read-side RAZ is purely emergent; if any path writes high bits to `n.pending`/`n.enabled` directly, reads would observe them.
3. **WEAK** — preferred mask placement: `Nvic::set_pending`/`set_enabled` (change `< 32` → `< IRQ_COUNT`).
4. **FAIL** — extended test still didn't actually exercise the clear-arm mask: pre-seed via masked ISER0/ISPR0 leaves `0x03FF_FFFF`, then ICER0/ICPR0 of `0xFFFF_FFFF` clears to 0 with or without the new mask.
5. PASS — scope creep. 6. **FAIL** — TDD: no test exercises the threaded path; only existing tests run. 7. PASS — §7 drift.

**Supervisor decision.** Apply only #4 (real test gap, cheap to fix); skip #1-#3 as out-of-HLD-scope structural concerns with no current caller exercising bits ≥26 — recorded as known invariant-leak below. Skip #6 — threaded twin's logic is identical to the serial path that the existing test now covers; dual_model.rs is the integration check.

**Apply changes 2 (agent a2ed8956ac14661c3).** Pre-seeded `bus.nvics[0].enabled`/`.pending = 0xFFFF_FFFF` directly via the already-`pub` field, wrote `0xFFFF_FFFF` to ICER0/ICPR0, asserted readback == `0xFC00_0000`. Verified by hand-removing mask: ICER0 assert FAILs with `right: 0xFC00_0000, left: 0` — proves the new asserts now bite. 1034 lib tests green.

**Commit `19f6cd7`** — threaded twin + clear-arm test extension. (`d2bd159` was the earlier auto-commit.) Stage 1 closed.

**Known structural concerns deferred (DA #1-#3).**
- `crates/mdrp2040/src/threaded/bus.rs:171` direct `pending |= bits` from cross-core IRQ atomics is not masked. Currently no caller asserts `irq >= 26`, so the leak is theoretical.
- `Nvic::set_pending`/`set_enabled` (`core/nvic.rs:102, 127`) guard `irq < 32` rather than `< IRQ_COUNT`. Same observation: theoretical, not currently-exercised.
- `nvic_mmio_read32` returns unmasked. Today the field never has high bits because all writers are well-behaved.

If a future scenario or peripheral exercises bit 26..31, revisit by changing `set_pending`/`set_enabled` to `< IRQ_COUNT` (one structural fix that closes all three).

### Stage 2 — `isr_m0_nvic_high_bits_razwi` scenario

**Implementer (agent a87373fabf1051bc7).** Added the simplest of the four V2 scenarios — pure MMIO RAZ/WI assertion, no handler dispatch. New SRAM cells `ISER/ISPR/ICER/ICPR_READBACK_ADDR` at `0x2000_3FD0..3FE0`. Hand-assembled `MAIN_NVIC_RAZWI` is 25 instruction halfwords + 19 NOPs + 20 literal halfwords (fills the 0x80-byte main budget exactly). Empty handler array (image builder's `N_HANDLER_HW=0` is allowed). 3/3 EMU integration tests PASS first try (the production fix from Stage 1 was already in tree).

**Code reviewer (agent a843c58a75f522171).** APPROVE-WITH-CHANGES. Spot-checked Thumb-16 encodings (correct), const-assert pin coverage (10 pins + a fits-image assert — exactly per HLD §6 gotchas), SRAM layout (clean), HLD §3.4 semantics (match), naming convention (correct `isr_m0_` prefix). One MAJOR: `run_silicon_scenario` at `:1298-1302` carries its own scenario-name match for the silicon-side primary counter, separate from `primary_observable_addr` at `:1108-1113`; without the new arm there, silicon runs would burn the full 1500 ms budget per scenario before timing out. One MINOR comment rephrase.

**Apply changes (agent a317cae70a7253625).** Added `"isr_m0_nvic_high_bits_razwi" => ISER_READBACK_ADDR` to the silicon-side match at `:1301`. Rephrased the OBS_NVIC_RAZWI comment. 3/3 EMU integration tests PASS; release binary builds clean.

**No DA pass at this stage** — Stage 1 set the pattern; Stage 5 + final are the next DA checkpoints per the stage map. Smallest scenario; the review caught the only meaningful gap.

**Commit `154c429`** — Stage 2 closed.

### Stage 3 — `isr_m0_masked_pending_unmask` scenario

**Implementer (agent a73307303c7729e11).** Added the PRIMASK-gate scenario per HLD §3.2. Main: cpsid → ISPR0/ISER0 stores → gate=1 → cpsie → gate=2 → spin. Handler captures gate into gate_at_entry (load-bearing observable). HANDLER_MASKED 26 hw, MAIN_MASKED 22 hw. New SRAM cells at 0x2000_3FEC/0x2000_3FF0. TIMER level-reassert defended by NOT writing TIMER.INTE (and `reset_scenario_state_*` clears any residue). Both `primary_observable_addr` and `run_one_scenario` match arms updated (lesson from Stage 2). Test PASSed first try.

**Code reviewer (agent a7098a7fcef67d801).** APPROVE-WITH-CHANGES. Spot-checked encodings (cpsid=`0xB672`, cpsie=`0xB662`, b. =`0xE7FE`, bkpt=`0xBE00`, bx lr=`0x4770`, all literal-pool ldr offsets), verified load-bearing semantic (handler runs between gate=1 and gate=2 stores), verified TIMER.INTE defensive avoidance, verified both match arms updated, verified all observables asserted in test. One MINOR: clippy `doc_overindented_list_items` on MAIN_MASKED rustdoc.

**Apply changes (agent a3afdaf6c9580c95d).** Reduced continuation indentation 28 → 5 spaces. Clippy lint clean. 4/4 EMU tests pass.

**No DA at this stage** — same reasoning as Stage 2. Implementer carried Stage 2's lessons forward (dual match arms, TIMER defence, const-assert pins) without prompting.

**Commit `2ef64ab`** — Stage 3 closed.

### Stage 4 — `isr_m0_wfi_wake` scenario + master_cycle production fix (closes tech_debt §1649 for RP2040)

**Implementer round 1 (agent a1468fbbef8801b6b).** Added scenario per HLD §3.3. Hit a real production gap: tech_debt §1649 — when both cores park (core 0 in WFI, core 1 halted from setup), `master_cycle` doesn't advance, the alarm never fires, scenario times out. Implementer applied a test-side workaround (parked core 1 on a `b .` busy loop at `0x2000_3F00` so master_cycle would advance) to make the scenario pass. Reported the workaround honestly — flagged "WFI never wakes" sub-case as only partially closed.

**Code reviewer (agent af24251978b2f38fb).** APPROVE-WITH-CHANGES. All encodings, match arms, reset-state, observables verified. Two MINOR doc additions. Assessed the test-side workaround as "defensible / acceptable-with-caveats" — HLD §3.3 doesn't strictly mandate both-cores-parked.

**Devil's advocate (agent aeabcc6ce1fcb1ada). RED.**
1. **FAIL** — workaround is silicon-unfaithful: real RP2040 has core 1 parked in WFE bootrom (not spinning in SRAM). Test rig invents a state silicon never reaches.
2. **FAIL** — oracle integrity broken on the load-bearing axis: a regression in "WFI wake when both cores idle" wouldn't surface here.
3. Production fix is small (~15-20 lines, well-localised), and the V2 oracle is the natural surfacing site.
4. Slippery slope: no enforcement against the workaround pattern; Stage 5 may copy it.
5. Steel-man (HLD §4 says one production fix only) collapses on the HLD's own §8 risk note: "If the EMU somehow finishes the WFI before the alarm fires, the scenario times out — that *is* a real bug surfaced by the test." Yes — that bug is §1649.
6. Recommendation: **fix §1649 now**. Removes ~50 LOC of test scaffolding; closes a tech_debt entry.

**Supervisor decision.** Accept DA verdict. CLAUDE.md "fix root causes, not bypass" applies. Dispatching production fix.

**Implementer round 2 (agent a30c870dbd3ab062d).** Landed in `crates/mdrp2040/src/lib.rs:723-752`: both-blocked branch in `step_serial` + new helpers `TimerRegs::next_armed_inte_fire_cycle` (`peripherals/timer.rs:222`) and `Bus::next_scheduled_lazy_deadline` (`bus/mod.rs:1971`). New unit test `step_serial_advances_clock_when_both_cores_blocked_with_armed_alarm` proves pre-fix FAIL → post-fix PASS. Stage 4 test scaffolding cleaned up: `isr_m0_wfi_wake_passes_on_emu` now uses the standard `run_scenario` helper (-25 LOC, removing the core-1 busy-loop and the caveat docstring). tech_debt §1649 retitled "RP2350 only — RP2040 closed 2026-04-29" with a Status subsection.

**Code reviewer round 2 (agent a14708fbbb450745e). APPROVE.** Verified branch placement, predicates (consumed==0 + both halted-or-wfe + irq_pending==0 + both NVICs idle + deadline > master_cycle), step_quantum cap (multi-quantum closing via outer loop), helpers' correctness, unit-test bug-bite shape, threaded path correctly identified as out-of-scope. Two MINORs: clarify multi-quantum cap comment; note that PWM/ADC-only both-blocked scenarios aren't covered by this V1 fix.

**Apply changes round 2 (agent ad69e8ad7d96d0843).** Both MINORs landed. 5/5 EMU tests, 1036 mdrp2040 serial, 1194 with threading.

**Commit `851f89d`** — Stage 4 closed. Net result: scenario landed cleanly via shared helper, production gap fixed, tech_debt §1649 closed for RP2040. mdrp2350 sibling entry retained for future work. The DA's escalation produced the strongest single outcome of the implementation.
