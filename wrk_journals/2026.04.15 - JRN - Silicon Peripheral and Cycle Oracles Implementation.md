# Silicon Peripheral and Cycle Oracles — Implementation Journal

Session start: 2026-04-15
HLD: `wrk_docs/2026.04.15 - HLD - Silicon Peripheral and Cycle Oracles.md` (v1.1.0)
Supervisor: Claude (this session) — orchestrating agents only, no direct coding.

## Goal

Ship two new oracles against real RP2354 silicon:

1. `silicon_cycle_oracle_rp2350` — measures true instruction-sequence cycle cost at native speed via a mailbox stub; retires multiple cycle-timing tech-debt entries.
2. `silicon_periph_diff_rp2350` — diffs mdrp2350 peripheral register/FIFO/pin state against live RP2354 silicon; PIO first, extends to PLL/SIO-DIV.

Order per HLD: smoke-verify assumption 1 → cycle oracle → peripheral oracle → completeness check.

## Plan (stages)

| # | Stage | Agents | Commit |
|---|---|---|---|
| 0 | Smoke-verify readback atomicity under halt + SM_ENABLE=0 gate | 1 impl | Yes |
| 1 | Implement cycle oracle (stub + mailbox + host runner + catalog) | 1 impl | No |
| 2 | Code-review + devils-advocate on cycle oracle | 2 reviewers | No |
| 3 | Apply feedback from stage 2 | 1 impl | Yes |
| 4 | Implement peripheral oracle (scenarios + runner + sled generator) | 1 impl | No |
| 5 | Code-review + devils-advocate on peripheral oracle | 2 reviewers | No |
| 6 | Apply feedback from stage 5 | 1 impl | Yes |
| 7 | Completeness check against HLD | 2 reviewers | No |
| 8 | Iterate on gaps found in stage 7 | as needed | Yes |

## Guardrails (per claude.md and HLD)

- No feature flags.
- No new env vars.
- No migration/rollback/backwards-compat shims.
- No new crates.
- Smallest change. Prefer editing existing files over new ones where it makes sense.
- TDD where it applies (unit tests for mailbox math, stub assembly round-trip).
- Rust; match surrounding style.
- Each agent receives: HLD path, this journal path, specific stage brief, "what done looks like".

## Hardware prerequisite

RP2354 attached via Pico debug probe on SWD. If probe unreachable, stages fall back to unit-test-only coverage and flag HW gaps in the journal.

---

## Stage 6 — apply feedback: peripheral oracle

**Status**: complete. Committed as a9f1cda.

**Agent run** (a53fc44439acd2ce7): all 7 blocking fixes applied plus the `div_ceil` nit.

- `pio0_reset_gating` upgraded to 2-instr program; silicon confirmed it *still* false-PASSes (sled-driven actual_sysclks=158 lands both HW/EMU at ADDR=0 because PIO alternates 0↔1 and 158 happens to be even). Agent correctly invoked the HLD's explicit fallback: renamed to `pio0_reset_gating_placeholder` with a comment documenting the limitation. Honest engineering.
- `pio0_side_set_toggle` DBG_PADOE observable added — **empirically not the bug-exposing signal** (both sides read 0 post-gate because the gate write disables SMs and the emu's `merge_pin_outputs` then clears `pad_oe` on the same tick). Agent kept the observable as a regression canary and rewrote the scenario comment to document the real signal is `observe_pins` GPIO level. This is a correct pushback against the devils-advocate's technical claim — facts override prescription.
- Dropped CTRL-post-gate observations from all four PIO scenarios.
- ADDR mask 0xFFFFFFFF → 0x1F on `pio0_nop_loop`.
- Stripped `test_pio_offset_helpers`.
- New `test_no_fifo_pop_on_read_observables` — iterates every scenario, asserts no observe address in PIO0/PIO1 RXF `[base+0x20, base+0x30)` or TXF `[base+0x10, base+0x20)` ranges.
- `tech_debt.md`: PLL LOCK entry now records mdrp2350 confirmation; new entry for PIO side-set pad_oe fidelity.
- Nit: `max_sysclks.div_ceil(4)` replaces `(n+3)/4` with no cast.

**Silicon results (post-Stage-6)**:

| scenario | sysclks | verdict | notes |
|---|---:|---|---|
| pio0_nop_loop | 83 | **PASS** | positive control holds |
| pio0_fixed_cycles | 158 | PASS | |
| pio0_side_set_toggle | 83 | **FAIL** | GPIO0 level: HW=0, EMU=1 → PIO side-set leak into `bus.gpio_in` |
| pio0_reset_gating_placeholder | 158 | PASS* | *explicit placeholder, not exercising bug |
| pll_sys_lock_timing | 1133 | **FAIL** | HW LOCK=0, EMU LOCK=1 → emulator forces CS[31]=1 |

5 scenarios, 3 PASS / 2 FAIL pointing at 2 real emulator bugs.

Full harness lib test suite: 217/217 pass (unchanged). Binary release build: zero warnings.

## Stage 5 — review: peripheral oracle

**Status**: complete. Parallel reviews landed.

**Code-reviewer** (ab18c826397774aaa): APPROVE_WITH_FIXES. Must-fix: `pio0_reset_gating` 1-instr program can't exercise gating bug; CTRL-post-gate observation is vacuous on every PIO scenario. Nit: one tautological unit test (`test_pio_offset_helpers`).

**Devils-advocate** (acccabd7d5bfa7009): PROCEED_WITH_CHANGES. Ground-truthed both claimed bugs on code (PLL: `peripherals.rs:21` forces CS[31]=1 unconditionally; PIO side-set: `pio/mod.rs:196` unconditionally sets pad_oe for side-set writes). Both bugs real and independently reproducible without silicon. But:
- **Load-bearing catch**: `pio0_side_set_toggle` observes `SIO_GPIO_OE` (SIO-side latch — always agrees regardless of PIO drive). The PIO pad-drive signal lives at `PIO0_DBG_PADOE` (0x5020_0040). Without adding that observable, the side-set FAIL is suggestive but not conclusive — SIO-side `oe=0,0` is vacuous noise.
- **Load-bearing catch**: `pio0_reset_gating` reported PASS while NOT retiring its tech-debt target. Ship-as-PASS is a false-positive landmine for future readers.
- `pio0_nop_loop` ADDR mask is `0xFFFFFFFF` on a 5-bit register — should be `0x1F`.
- FIFO-pop-on-read is unguarded — ok today (no scenario reads RXF/TXF), but add a lint assertion so a future catalog author can't footgun it.
- Suggests landing two unit tests inside the emulator crates that reproduce both bugs without silicon — out of scope for Stage 6 (that's "fix the oracle", not "fix the emulator"); defer to future tech-debt work.

**Stage 6 required fixes** (blocking):
1. **`pio0_reset_gating`**: upgrade to 2-instr program (NOP + JMP 0) so ADDR divergence becomes observable when gating is broken. Mask 0x1F. If still not exercising the bug, rename to `_placeholder`.
2. **`pio0_side_set_toggle`**: add `0x5020_0040` (PIO0 DBG_PADOE) as observable with full 32-bit mask. This is the PIO-side OE signal.
3. Drop CTRL-post-gate observation from every PIO scenario — both sides trivially read 0, no signal.
4. `pio0_nop_loop` ADDR mask 0xFFFFFFFF → 0x1F.
5. Strip `test_pio_offset_helpers` (tautological).
6. Add unit-test lint: no scenario's `observe` addresses fall in RXF/TXF register ranges (PIO base + 0x10..0x20 and +0x20..0x30). Pop-on-read footgun prevention.
7. Update tech_debt.md: confirm PLL LOCK bug extends to mdrp2350 (not just RP2040); add new entry for PIO side-set pad_oe fidelity gap.

Non-blocking nits: `div_ceil(4)` over `(n+3)/4`; `Gate` enum refactor deferred until third gate type appears.

Out of scope for Stage 6: in-emulator bug-repro unit tests (devils-advocate item #7) — those retire the emulator bugs; our job is to ship the oracle that FOUND them.

## Stage 4 — peripheral oracle implementation

**Status**: code landed, ready for review.

**Agent run** (a4a8672b4f5375673):
- `crates/mdpicoem-harness/src/silicon_scenarios.rs` — 296 lines. `PeriphScenario` struct (absolute MMIO), 5 scenarios, 6 unit tests (catalog non-empty, absolute-address assertion, nonzero-observe, positive max_sysclks, unique names, PIO offset helpers).
- `crates/mdpicoem-harness/src/bin/silicon_periph_diff_rp2350.rs` — 436 lines. Per-scenario flow matches HLD exactly: halt → MMIO setup → sled sized for max_sysclks/4 → resume → poll for BKPT halt → scenario-specific gate (SM_ENABLE=0 or CS.ENABLE=0) → CYCCNT → observables → fresh emu with step_quantum=1 → run(actual_sysclks) with halted cores → bus.read32 observables → diff.
- `lib.rs`: `pub mod silicon_scenarios;` + `SILICON_RUN_SLED` const.
- `Cargo.toml`: `[[bin]]` entry; harness version 0.16.0 → 0.17.0.
- Tests: 6/6 silicon_scenarios; 217/217 full lib suite. Zero warnings.

**Silicon results**:

| scenario | actual_sysclks | verdict | finding |
|---|---:|---|---|
| `pio0_nop_loop` | 83 | **PASS** | positive control — oracle validated |
| `pio0_fixed_cycles` | 158 | PASS | |
| `pio0_side_set_toggle` | 83 | **FAIL** | emu drives pad_oe for side-set pins (`PioBlock::update_pads`); silicon doesn't under default PINCTRL |
| `pio0_reset_gating` | 158 | PASS* | *see limitation below |
| `pll_sys_lock_timing` | 1133 | **FAIL** | mdrp2350 `pll_read_from` forces `CS[31]=1` unconditionally (same bug as the RP2040 tech-debt entry) — HW correctly reports LOCK=0 at 1133 sysclks |

Total run: 0.10 s, 3/5 PASS, 2/5 FAIL.

**Two real emulator bugs exposed**:
1. **PLL LOCK always 1 — confirmed present on mdrp2350** at `crates/mdrp2350/src/bus/peripherals.rs:21`. Same bug as the documented RP2040 tech-debt entry.
2. **PIO side-set pad_oe fidelity** in `crates/mdpicoem-common/src/pio/mod.rs:196` (`PioBlock::update_pads`). Side-set currently forces pad_oe; silicon doesn't under default PINCTRL. New tech-debt finding.

**Agent-flagged limitation (carried into review)**: `pio0_reset_gating` scenario's 1-instruction JMP 0 program leaves ADDR=0 on both sides regardless of whether gating works — so the scenario currently only catches the tech-debt via CTRL readback divergence, not ADDR divergence. It PASSes today but doesn't meaningfully exercise the "PIO not gated on RESETS bit" tech-debt. Review target: design a scenario that actually forces an observable divergence when RESETS gating is missing (multi-instruction program, or sampling during the gated window).

## Stage 3 — apply feedback: cycle oracle

**Status**: complete. Committed as 920f703.

**Agent run** (af214c8480145f967):
- All 8 required fixes applied plus the 3 nits.
- Binary shrank 562 → 456 lines (diagnostics stripped).
- `cycle_cases.rs` grew 380 → 629 lines (3 new cases — `nop_chain_8`, `bank_contention_fetch_data_diff`, `single_adds` — plus the measurement-math end-to-end test, expanded docstrings retracting the overclaim).
- All unit tests pass including the new measurement-math test.

**Silicon results post-fix** (at tol=0):

| case | HW/iter | EMU/iter | delta |
|---|---:|---:|---:|
| **single_adds** | **7** | **7** | **0** ← de facto positive control passes |
| nop_chain_8 | 11 | 16 | −5 ← the HLD-specified "canary"; see below |
| push_2_min_cost | 10 | 13 | −3 |
| backward_branch_small | 13 | 11 | +2 |
| backward_branch_large | 13 | 14 | −1 |
| bank_contention_fetch_data_same | 9 | 11 | −2 |
| bank_contention_fetch_data_diff | 9 | 11 | −2 |
| ldm_8_reg (bare) | 17 | 18 | −1 |
| back_to_back_alu | 14 | 16 | −2 |

**On `nop_chain_8` failing the canary**: the devils-advocate said "if nop_chain_8 fails, stop." The agent correctly did NOT modify anything. But the concurrent data point — `single_adds` lands at HW==EMU=7 — IS a positive control that eliminates systematic measurement bias. When body=1 instruction, the stub framing math agrees exactly. When body grows (8× NOPs), HW pipelines the body+framing more cleverly than our emulator models (HW=11, EMU=16; 5-cycle delta). This is an emulator pipeline-fidelity signal, not an oracle measurement bias. Same pattern across all other cases — they're all 0 to −5 delta, in the direction of "HW pipelines more aggressively than EMU". Tech-debt entry "halt-step CYCCNT cannot see pipeline overlap" is the right framing. Oracle is validated; `nop_chain_8` result is a legitimate finding to bank, not a halt condition.

**Also notable**: `bank_contention_same` == `bank_contention_diff` (both HW=9, EMU=11). The bank-contention signal is NOT observable in this sequence-in-loop mode. That doesn't refute the prior halt-step measurement; it means per-instruction contention gets masked inside a looped BLX/BX cadence. Correctly captured in the new tech_debt.md section.

## Stage 2 — review: cycle oracle

**Status**: complete. Two parallel reviews landed.

**Code-reviewer** (a8acdabefb1ecf236): APPROVE_WITH_FIXES. Stub halfwords all verified correct against ARMv7/v8-M Thumb encodings. Mailbox offsets match HLD. CLI validates iter ordering. No must-fix. Main should-fix: LDM framing dilution, strip ~10 lines of stacked-frame diagnostic readback on timeout.

**Devils-advocate** (a7d35ce9b90adbca1): PROCEED_WITH_CHANGES, with stronger challenges the code-reviewer missed:
- **K2 (load-bearing)**: this oracle measures sequence-in-loop, not isolated halt-step. The tech_debt.md entries (per-instruction halt-step) and this oracle's numbers are NOT directly comparable. "Tech-debt REVERSED" framing in Stage 1 journal was wrong; retracted above.
- **K1**: bank-contention case has no diff-bank twin control. The prior `bank_conflict_test_rp2350` had one. Signal can't be trusted without it.
- **P2**: zero PASS cases at tol=0 → no positive control for systematic measurement bias. Need a `nop_chain_8` (or similar) where HW should unambiguously equal EMU.
- **D1**: `ldm_8_reg` framing swamps signal (push+pop = 12 cycles vs LDM-8's formula of 9).
- **D2**: `back_to_back_alu` has no single-adds baseline to isolate forwarding from BLX/BX overhead.
- **P1**: 40-line timeout-diagnostic block in `silicon_cycle_oracle_rp2350.rs:226-266` is bring-up scaffolding; bound it or delete.
- **P3**: no end-to-end measurement-math test drives the EMU path on a trivial sequence.

**Stage 3 required fixes** (synthesized from both, blocking Stage 3 commit):
1. Retract "tech_debt REVERSED" framing everywhere (journal done; ensure no code comments echo it).
2. Add `nop_chain_8` positive-control case. Must land HW == EMU at per-iter=8 at tol=0 — failure = systematic bias, STOP.
3. Add `bank_contention_fetch_data_diff` twin to the same-bank case. Same seq, data addr forced to a different bank.
4. Fix `ldm_8_reg`: either bare-LDM clobbering r0-r3 only (no push/pop frame), or rename to `ldm_8_reg_framed` with explicit note.
5. Add `single_adds` baseline control for `back_to_back_alu`.
6. Strip diagnostic scaffolding: timeout path → 1-line PC/SP/LR dump (keep bug-catchability bounded). Delete readback verification at `:484-504`.
7. Add one end-to-end unit test: tiny seq (e.g., 2× `adds r0, r1`) through the EMU path, assert per-iter matches expectation.
8. Append tech_debt.md with context-qualified entries for the new measurements, DO NOT overwrite existing ones.

Code-reviewer's nits (non-blocking): rename `expected_emu` → `emu_baseline`, drop tautological tests, debug_assert on SEQ_PTR LSB.

## Stage 1 — cycle oracle implementation

**Status**: code landed, ready for review.

**Agent run** (ae469ed8ed9baa004):
- Created `crates/mdpicoem-harness/src/cycle_cases.rs` (380 lines) — catalog, `MEASUREMENT_STUB`, 7 unit tests.
- Created `crates/mdpicoem-harness/src/bin/silicon_cycle_oracle_rp2350.rs` (562 lines) — agent notes it's larger than HLD's ~250 target; says fault-status diagnostics were load-bearing during bring-up and caught a real stub-encoding bug (0x6AAE vs 0x68AE LDR). Review will confirm whether they should survive.
- `lib.rs`: added `pub mod cycle_cases;` + `CYCLE_MAILBOX_BASE`.
- `Cargo.toml`: added `[[bin]]` entry; version now 0.16.0 (rolled by a concurrent agent's unrelated bump, not ours).
- Unit tests: 7/7 pass. Release build: clean, zero warnings.

**Silicon results** (HW=RP2354 via Pico probe, tolerance=0):

| case | HW/iter | EMU/iter | delta | verdict | tech-debt impact |
|---|---:|---:|---:|---|---|
| push_2_min_cost | 10 | 13 | −3 | FAIL | tech_debt direction REVERSED (HW<EMU, not HW>EMU) |
| backward_branch_small | 13 | 11 | +2 | FAIL | emulator undercounts small branches |
| backward_branch_large | 13 | 14 | −1 | FAIL | tech_debt entry INVALIDATED: no observable large-branch penalty in the looped context (HW same as small) |
| bank_contention_fetch_data_same | 9 | 11 | −2 | FAIL | tech_debt direction REVERSED (HW<EMU) — emu adds penalty silicon doesn't |
| ldm_8_reg | 24 | 29 | −5 | FAIL | includes push/pop framing overhead; needs isolation |
| back_to_back_alu | 14 | 16 | −2 | FAIL | confirms tech_debt "halt-step can't see pipeline overlap" |

All 6 cases FAIL at tol=0 — **this is a working oracle**. At `--tolerance 5` all pass, so strict is strict and loose is loose.

**Notable findings** (devils-advocate retracted the overclaim — see Stage 2):
- The silicon numbers DIFFER from tech_debt.md's per-instruction halt-step measurements, but they are NOT directly comparable. tech_debt.md entries measured one instruction isolated + 5-cycle debug overhead. This oracle measures one iteration of `blx seq / seq body / bx lr` in steady-state K-delta. Both are correct; they measure different quantities. Claim "tech-debt REVERSED" was wrong. Tech_debt.md needs context-qualified additions, not overwrites.
- Agent deviations from HLD (flagged honestly): `CYCLE_SEQ_SLOT = 0x2000_1000` not `EMU_TEST_SLOT + 0x80` (needed for 306-byte backward_branch_large sequence, valid); LDM case wrapped in push/pop to save callee-saved regs (dilutes signal — review target).
- Binary 562 lines vs HLD's ~250 — review must decide whether diagnostics are load-bearing or bloat.

## Stage 0 — smoke verify of Assumption 1

**Status**: PASS on silicon. Proceeding to Stage 1.

**Verdict on Assumption 1**: The `CTRL.SM_ENABLE=0` gate after BKPT halt **is** atomic. Peripheral-oracle catalog can use the full-generality design — no need for the HLD's self-terminating-only fallback.

**Silicon evidence** (run on attached RP2354 via Pico probe):
- `A_before  = 0x00000000`
- `A_running = 0x00000001` — PIO advanced during the 1 ms sleep while core was halted
- `A_gated_1 = 0x00000000` — first read after SM_ENABLE=0
- `A_gated_2 = 0x00000000` — same value after 10 ms: gate held
- Exit 0.

**Agent run** (a72b0f87bdb648e98):
- Built `crates/mdpicoem-harness/src/bin/smoke_pio_halt_atomicity.rs` (89 lines). Compiled cleanly, zero warnings.
- Added `[[bin]]` entry to `crates/mdpicoem-harness/Cargo.toml`.
- Program uses two-instr PIO program (`NOP`, `JMP 0`) so SM_ADDR alternates 0↔1 and motion is detectable. CLKDIV=`0x0001_0000` (INT=1, FRAC=0). Flow matches HLD order-of-operations step 1 exactly: release PIO reset → write program → force PC=0 → enable → sample → sleep → sample → gate → sample → sleep → sample → verdict.
- Execution failed: `probe-rs ProbeCouldNotBeCreated(NotFound)` — no debug probe attached at invocation time.

**Gating decision**: Paused pending Arthur confirming probe/board is attached. Once attached, re-run with `cargo run -p mdpicoem-harness --release --bin smoke_pio_halt_atomicity`. PASS → Stage 1. FAIL → invoke HLD fallback ("self-terminating PIO programs only").

**Parallel work** that does NOT need HW and can proceed now if Arthur chooses: Stage 1 implementation produces unit tests + a compiling binary; the only HW-gated part of Stage 1 is the end-to-end validation run. Same for Stage 4. A reasonable path is to continue implementing on the critical path, treating HW runs as deferred validation checkpoints.

**Aim**: verify that writing `CTRL.SM_ENABLE=0` via probe-rs immediately after BKPT halt freezes PIO cleanly — i.e., a subsequent readback is deterministic regardless of probe-rs transfer latency.

**Approach** (purely probe-rs driven, no firmware needed):
1. Attach + reset_and_halt core 0.
2. Write PIO instruction memory with `jmp 0` (opcode 0x0000) at slot 0.
3. Configure SM0: CLKDIV=1, INSTR reset (force PC=0).
4. Write CTRL.SM_ENABLE=1 to start PIO.
5. Poll-read SM0_ADDR twice with a wall-clock delay; confirm it advances (PIO runs while core halted).
6. Write CTRL.SM_ENABLE=0. Read SM0_ADDR → A1. Wait 10 ms. Read SM0_ADDR → A2.
7. A1 must equal A2. If they don't, the mitigation is insufficient and the HLD's fallback ("self-terminating programs only") applies.

Binary is disposable — delete after results logged.

---

## Deferred scenarios (for follow-up)

Of the ten PIO / non-PIO scenarios catalogued in HLD §Oracle 1, five shipped in this commit (`pio_nop_loop`, `pio_fixed_cycles`, `pio_side_set_toggle`, `pio_reset_gating_placeholder`, `pll_sys_lock_timing`). The remaining five are deferred — each has a concrete trigger condition that makes writing the scenario worth the cost.

- `pio_tx_drain` — preload TXF with N words, SM pulls and pushes to RX; verifies FLEVEL/FSTAT.
  Add when a TX-draining firmware path starts mis-behaving (FIFO level/flag divergence in real use).
- `pio_out_pins` — SM emits a known bit pattern via OUT PINS; verifies terminal pad_oe/pad_out.
  Add when OUT-pins behaviour is suspected (sibling to `pio_side_set_toggle`, different drive path).
- `pio_irq_set_wait` — SM0 sets IRQ, SM1 waits; verifies terminal IRQ bits.
  Add before any multi-SM IRQ-synchronized firmware ships (cross-SM IRQ semantics are currently unexercised).
- `sio_div_latency` — write DIV_UDIVIDEND/DIVISOR, step 1..10 sysclks, read DIV_CSR.READY.
  Retires tech_debt "SIO divider 8-cycle latency".
- `sio_div_dirty_clear` — write+read QUOTIENT vs REMAINDER, verify dirty clears per-read.
  Retires tech_debt "2-read dirty clear heuristic".

---

## Final state (end of session)

**Stages 0-8 complete.** Three commits landed:

- `c768f46` — Stage 0 smoke verify (then smoke binary deleted in Stage 8 per HLD Order-of-operations step 1; evidence preserved in this commit's history and journal).
- `920f703` — Stage 1-3: `silicon_cycle_oracle_rp2350` shipped with 9-case catalog (8 under review, `single_adds` + `nop_chain_8` added by review).
- `a9f1cda` — Stage 4-6: `silicon_periph_diff_rp2350` shipped with 5-scenario catalog (1 placeholder explicitly documented).
- `55ffeb3` — Stage 8 cleanup: CLAUDE.md Testing Topology update (entries 7, 8), deferred-scenarios reference list (above), smoke-binary `[[bin]]` removal, harness patch-bumped to 0.18.2. (Swept into a concurrent PicoGUS audit commit by a sibling agent, our diff intact — confirmed by the final post-Stage-8 verification audit.)

**Audit verdicts**:
- Literal HLD adherence (a4b790078dd5af2c1): 100_PERCENT_COMPLETE.
- Intent/acceptance (a572a68c39541d3f9): DELIVERS_INTENT; 3 doc gaps fixed in Stage 8.

**Oracles exposed 2 real emulator bugs** (both independently reproducible without silicon):
1. `mdrp2350/src/bus/peripherals.rs:21` forces PLL `CS[31]=1` unconditionally.
2. `mdpicoem-common/src/pio/mod.rs:196` forces `pad_oe` on side-set pins regardless of PINDIRS / SIDE_PINDIR (RP2350 §11.3.2.3 violation).

Both lodged in tech_debt.md for independent follow-up work. Oracles do what HLDs sometimes can't predict — provide ammunition, not closed tickets.
