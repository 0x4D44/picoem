# 2026.04.24 — JRN — Single-voice dump (diagnose GUS path) + FM-trace prep

## Goal

Two prongs after Arthur's push-back ("pitch-shifted audio still doesn't sound musical"):

1. **Single-voice dump.** For each active voice, read its DRAM range and write
   an 8-bit mono WAV at the voice's natural playback rate
   (`WaveAdd × voice_tick_rate / 2^WAVE_FRACT`). If any voice has a recognisable
   melodic line, the GUS data-path is sound and the bug is in mixing/voice
   selection. If all voices sound like noise, the bug is in our DRAM or decode.

2. **FM-mode trace (deferred within this session).** Capture a DOS-game OPL
   trace with DOSBox-X as an alternative lower-complexity path if the GUS
   investigation dead-ends.

## Session 1 — single-voice dump

### What we know
- 27 active voices throughout the music section (trace sec 1..29.99).
- FFR across all 246 writes: max=123, median=13, mean=25.8. Those values are
  the 16-bit register content (low byte via 0x344, high byte via 0x345).
- voice_tick_rate ≈ 22866 Hz (IRQ rate at 27 voices, 370 MHz sys_clk).
- WAVE_FRACT = 10 (confirmed in `gus-x.cpp:51`).
- WaveAdd = FFR (when force_44k = 0, which matches `fixed 44k output: 0`
  on UART).
- byte_rate per voice = FFR × 22866 / 1024 (bytes/sec of DRAM consumed).

### Plan
- Extend `wrk_scratch/dump_voice_wave.py` to:
  a. Track FFR writes per voice over time.
  b. Pick a representative FFR (mode / max — whichever dominates).
  c. Write an 8-bit unsigned-PCM WAV at that voice's natural rate.
- Dump the voices with the largest DRAM windows first (those are most likely
  to contain melodic content, not one-shot percussion samples).
- Visualise with `wav_shape.py` and spectral-peak-find with `fft_peaks.py`.

### Work log

**Built `wrk_scratch/dump_all_voices.py`** — per-voice FFR/WaveCtrl
timeline tracking + sample dumping. First run revealed:

- 27 active voices claimed, but only **13 voices actually have
  WaveStart/WaveEnd programmed** (the rest are silent).
- 5 distinct sample-index regions, heavily shared across voices:
  - `[0..7326]` (voices 8, 12)
  - `[7340..28594]` (voices 1, 3, 7)
  - `[28602..32678]` (voice 4)
  - `[32714..76190]` (voices 0, 5, 6)
  - `[93498..98197]` and `[98198..102897]` (voices 2, 9, 10, 11) — **had
    zero DRAM content under the initial parse, looked like garbage**.

All active voices set WaveCtrl bit 2 (`0x04 = 16BIT`). So DRAM is
supposed to be interpreted as 16-bit LE samples.

### Session 2 — DRAM bank-switch mystery

Dumped the unique regions as raw 8-bit bytes and as 16-bit LE words.
Result was confusing: byte range `[0..31]` read as 16-bit LE gives a
smooth monotonic curve (`26957 → 30202`, coherent audio); byte range
`[14680..14711]` reads as 16-bit LE gives huge alternating values
(noise). Voice 1 points at `[14680..57188]` physical — it would be
playing back noise.

Tracked the DRAM upload via port `0x347`: **102,898 total 0x347 writes,
but only 65,536 unique addresses under picogus' literal firmware
interpretation of register `0x44`** (DRAM address MSW). That's 37,362
overwrites — and at each overwritten address the two values differ, so
the second value is destroying the first. Something is very wrong.

Looked at the firmware:

```c
case 0x44:  // MSW Peek/poke DRAM position
    myGUS.gDramAddr = (0xffff & myGUS.gDramAddr)
                    | ((uint32_t)myGUS.gRegData >> 8) << 16;
```

Picogus extracts `dram_high` from the **HIGH byte** of `gRegData`. All
r0x44 writes in the trace are `write16 0x344 val=0x0001` or `=0x0000`
— i.e. the LOW byte is the meaningful bank byte. Under picogus'
interpretation the high byte of a `val=0x0001` OUTW is zero — so the
write is a no-op and `dram_high` stays zero.

**Ran the replay with the opposite interpretation** (`dram_high =
gRegData & 0xFF`):

```
B (low byte): 102898 writes, 102898 unique addrs, max=102897, bank1=37362
A (picogus):  102898 writes, 65536  unique addrs, max=65535,  bank1=0
```

Under B each of the 102,898 bytes lands at a unique address spanning
0..102897. That matches exactly what the game wanted to upload.

### Session 3 — 16-bit sample validation under B

Re-dumped voice 1 and voice 0 treating DRAM under interpretation B:

```
voice 1 [7340..28594] first 16 samples as 16-bit LE:
[-19160, -20808, -22096, -22904, -23240, -23216, -22888,
 -22232, -21184, -19792, -18184, -16544, -14896, -13248,
 -11504,  -9784]

voice 0 [32714..76190] first 16 samples as 16-bit LE:
[-28537, -28835, -29278, -29903, -30515, -30875, -30972,
 -31005, -31019, -31140, -31407, -31705, -31939, -32241,
 -32495, -32666]
```

**Both are smooth monotonic curves** — unambiguously coherent audio,
the rising/falling portions of sample waveforms. This is clearly the
real music data the game uploaded.

### Finding

**The picogus firmware mishandles `r0x44` when written as a 16-bit
`OUTW 0x344, val`.** The firmware's `case 0x44` extracts `dram_high`
from gRegData's high byte — which for an OUTW is the value written
to port `0x345` (always zero in the trace, per x86 OUTW byte order).
The game's **real intent** is that `dram_high` comes from the byte at
port `0x344` — the low byte of the OUTW.

This is either:

1. **A divergence between picogus and DOSBox-X**: DOSBox-X's
   `write_gus(port, val, iolen=2)` likely handles 16-bit writes
   atomically with a different register-byte mapping than picogus
   does when it splits into two 8-bit writes.
2. **A trace-tool quirk**: picogus-tap logs `val` as DOSBox-X sees
   it; if DOSBox-X normalises OUTW val-ordering before handing to
   `write_gus`, the trace's `val` is already post-normalisation and
   picogus' byte-at-a-time handling sees a different byte than the
   one DOSBox-X wrote to `gRegData >> 8`.

Either way, **our emulator faithfully runs picogus firmware; picogus'
`case 0x44` drops the game's bank-1 writes; the resulting audio
mixes bank 0 + trashed bank 0 at half resolution — exactly the
low-frequency garble we observed in WAVs.**

### Options

1. **Trace-side fixup in the replay harness.** When we see `write16
   0x344 val=V` while gRegSelect is `0x44`, translate to `OUT 0x345,
   (V & 0xFF)` so picogus receives the bank byte at the port its
   `case 0x44` reads from. Clean, no firmware patch.
2. **Patch picogus firmware** to extract `dram_high` from low byte
   when the write is a 16-bit write. Requires rebuild and re-verify.
3. **Recapture trace** with forced 8-bit DOSBox-X writes. Probably
   impossible without modifying DOSBox-X's CPU emulator.

Going with option 1 next.

### Session 4 — spectral validation of the fix

Re-dumped all 13 active voices treating DRAM under interpretation B.
Coverage:

| voices      | sample range       | present    |
|-------------|--------------------|------------|
| 8, 12       | [0..7326]          | 7326/7326  |
| 1, 3, 7     | [7340..28594]      | 21254/21254|
| 4           | [28602..32678]     | 4076/4076  |
| 0, 5, 6     | [32714..76190]     | 18735/43476|
| 2,9,10,11   | [76212..102897]    | 0/??? — past upload end |

Spectral peak-finding on the fully-present voices (Goertzel,
0.0–N seconds of the dumped WAV, 1-Hz resolution):

| voice | region size | fundamental peak | note    |
|-------|-------------|------------------|---------|
| 1     | 21254       | **436 Hz**       | ~A4 (440) |
| 0     | 43476       | **266 Hz**       | ~C4 (262) |
| 8     | 7326        | **222 Hz**       | ~A3 (220) |

These are clean musical notes in the A-minor scale — the key of
Monkey Island's theme. **The audio data is correct; picogus'
mis-handling of r0x44 was the only thing between us and music.**

Voices 2, 9, 10, 11 still read past the uploaded DRAM (physical
addresses 152K–206K) — those are either truly-silent voices in the
captured moment or the trace was truncated before higher banks were
uploaded. Either way they don't block the audio breakthrough.

