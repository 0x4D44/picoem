# Journal: BXNS Minimal Security State Implementation

**Date:** 2026-04-13
**HLD:** `wrk_docs/2026.04.13 - HLD - Minimal Security State for BXNS.md`

## Plan

7-stage pipeline: TDD tests → implement → code review → devil's advocate → apply feedback → commit → HLD compliance check.

## Stage 1+2: TDD Implementation

**Status:** In progress

Dispatching implementation agent. The changes are tightly coupled (tests need the new `secure` field and method to compile), so one agent handles both tests and implementation as a TDD cycle.

Files to modify:
- `crates/mdrp2354/src/core/mod.rs` — `secure: bool` + `transition_to_nonsecure()`
- `crates/mdrp2354/src/core/execute.rs` — bit[2] check in BX handler
- `crates/mdrp2354/src/tests.rs` — 3 BXNS unit tests

**Result:** All 3 new BXNS tests pass. 13 pre-existing failures in ldr_w/str_w
from concurrent work — not ours. Verified diffs: mod.rs entirely ours,
execute.rs has one stray POP timing change from concurrent work, tests.rs has
our 3 tests plus concurrent modifications. Clean separation.

## Stage 3+4: Code Review + Devil's Advocate (parallel)

**Status:** In progress

Launching two review agents in parallel:
- Code reviewer: formal review using code-reviewer skill
- Devil's advocate: independent challenge, edge cases, correctness

**Code reviewer result:** No bugs. Implementation matches HLD. Suggestions:
comment on EXC_RETURN/BXNS interaction, R13-as-source test, minor test
readability improvements.

**Devil's advocate result:** Encoding verified correct. Key findings:
- SPSEL=1 path untested in swap logic (important — actionable)
- EXC_RETURN doesn't encode security bit (known, add FIXME comment)
- Rm=15/13 UNPREDICTABLE (pre-existing in BX, not our issue)
- Handler mode register scrubbing missing (HLD explicitly excludes)

**Triage — actioning 3 items:**
1. Add SPSEL=1 BXNS test (exercises PSP swap path)
2. Add FIXME at EXC_RETURN generation in exceptions.rs
3. Assert old MSP preserved in bxns_from_secure test

## Stage 5: Apply Review Feedback

**Status:** Complete

Applied 3 items:
1. Added `bxns_with_psp_active` test — exercises PSP/SPSEL=1 swap path
2. Added FIXME comment at `exceptions.rs:76` for EXC_RETURN S-bit
3. Added `msp_ns` preservation assert in `bxns_from_secure`

All 4 BXNS tests pass.

## Stage 6: Commit

**Status:** Complete

Commit `89a3796`: "Add BXNS instruction and minimal security state for Phase 4 boot"

Selectively staged only our changes (107 insertions across 4 files),
excluding concurrent work in execute_thumb32.rs, POP timing in execute.rs,
and cycle-count changes in tests.rs.

## Stage 7: HLD Compliance Verification

**Status:** Complete — all items PASS

Every HLD section verified against code:
- 4.1 Core State: secure field + init ✓
- 4.2 BXNS Decode: bit 2, !link, self.secure guards ✓
- 4.3 Register Swap: debug_assert, 8 swaps, SP sync bracketing ✓
- 4.4 Ordering: after set_pc, before return ✓
- 5 Known Limitations: FIXME comment present ✓
- 6 Tests: 3 required + 1 bonus (PSP path) ✓
- 7 Files Changed: exact match ✓

## Summary

Feature complete. 107 lines added across 4 files. 4 tests, all passing.
Key review findings addressed (PSP path test, FIXME comment).
Not actioned: Rm=15/13 UNPREDICTABLE (pre-existing), handler mode register
scrubbing (HLD explicitly excludes), EXC_RETURN+BXNS interaction test (niche).
