# PicoGUS PSRAM Dispatch Debug — Supervisor Journal

Date: 2026-04-16
Supervisor: Claude (Opus 4.7, 1M context) — team lead, no direct edits/tests
Journal owner: Arthur
Follows:
- `wrk_journals/2026.04.16 - JRN - PicoGUS ISA Ingestion Debug.md`
- `wrk_journals/2026.04.16 - JRN - PicoGUS End-to-End Bring-up.md`

## Goal

Close in on **why the PicoGUS firmware never dispatches DRAM writes to
PSRAM** despite 127 K valid autopushes reaching the PIO0 RX FIFO.

End state: a definitive, data-backed answer to *whether* the blocker is
(1) capture-loss of critical setup events (0x343 register-select,
0x344 address word) **or** (2) the firmware reaching the dispatch
stage but the DMA→PIO1 TXF0 path silently failing.

## Starting state

Three PIO bugs landed earlier today:
- INT routing (`4944d3d`)
- Per-SM alias dispatch (`b239366`)
- ISA mux timing (`81873b9`)

Net effect: 127 K ISA events autopushed with correct addr/data (25 % of
514 K fired). PSRAM still 0 non-zero bytes. PIO1 SM0 TX FIFO never
written. Two live theories:

1. **Capture loss** — critical setup events among the missing 75 %, so
   firmware sees DRAM writes with stale/no register context.
2. **DMA-side gap** — firmware reaches dispatch, but DMA → PIO1 TXF0
   path silently fails (channel config quirk, trigger alias, etc.).

## Supervisor principles

- Protect context: delegate reading/writing/testing to agents.
- Every implementer agent writes an **HLD first**. Supervisor reviews
  and iterates until sign-off *before* any code is written.
- Each landing: impl → `code-reviewer` skill → `code-review-apply-
  feedback` skill → commit (after Arthur's nod).
- Touch nothing outside `picogus_diff_rp2040.rs` + diagnostic counters
  on `mdrp2040` bus/DMA unless the HLD justifies it.
- Diagnostic-only — zero behaviour change in emulator hot path.

## Phase plan

- **Phase 1 — HLDs in parallel** (both agents drafting simultaneously):
  - Agent 1 (Capture-Coverage): design diagnostic to classify
    captured-vs-fired ISA events by (port, reg-context).
  - Agent 2 (DMA-Dispatch): design diagnostic to observe whether any
    DMA channel ever targets PIO1 TXF0 (`0x5030_0010`) and whether it
    triggers.
- **Phase 2 — HLD review.** Supervisor iterates with each agent until
  design is signed off.
- **Phase 3 — Implementation.** Merged into a single impl agent to
  avoid conflicts on `picogus_diff_rp2040.rs`.
- **Phase 4 — Code review + apply feedback.**
- **Phase 5 — Run + interpret.** Surface results and pick next direction.

## Phase log

### Phase 1 — HLDs dispatched (parallel)

**Status:** round 1 returned; iteration round 2 in flight.

Two design agents spun up in parallel, both under strict "HLD only, no
code" discipline. Both returned HLDs inline (Plan subagent is
read-only; supervisor persisted to `wrk_docs/`):
- `2026.04.16 - HLD - PicoGUS Capture Coverage Diagnostic.md`
- `2026.04.16 - HLD - PicoGUS DMA Dispatch Diagnostic.md`

### Phase 2 — HLD review (signed off)

Round 1 revisions returned; both agents addressed all MUST-FIX /
SHOULD-FIX points. Supervisor applied one micro-fix to the capture
HLD (pin `last_autopush_word` update site to line 585 only, not
line 686 — 686 is `exec_push` explicit-PUSH, doesn't bump
`autopush_count`). Both HLDs final under `wrk_docs/`:

- Capture Coverage: uses `autopush_count` delta + new
  `last_autopush_word` field for decode verification; three-way
  bucketing (captured / captured-misattributed / dropped); post-roll
  orphan reconciliation.
- DMA Dispatch: per-channel counters for each trigger alias (6 of
  them, named after the register); sticky `ever_wrote_pio1_txf_mask`
  survives firmware re-programming; `dreq_observed_mask` leverages
  existing DREQ gating in `Dma::tick` (dma.rs:481–497) to
  distinguish TREQ-never-asserted from engine-gap verdicts.

### Phase 3 — Implementation + review + apply-feedback (complete)

Merged impl landed ~937 lines across 4 target files. Versions bumped
to 0.11.0 / 0.15.0 / 0.27.0 after review pointed out the public-API
additions deserved minor bumps. Code-reviewer skill returned REQUEST
CHANGES — caught a real classifier double-count bug in the
`delta==1 mismatch with pending` path that would have silently broken
HLD §7(4)'s `captured + misattr + orphans == autopush_count`
invariant. Apply-feedback fixed it (one bucket per push; pending now
carries `(ClassKey, decile)` so drift credits go to the right
decile); added 8 classifier unit tests + invariant assertion.

### Phase 4 — Diagnostic run (complete)

Ran `picogus_diff_rp2040` with `monkey_island_theme.trace`, duration
5 s (capped at 3.798 s simulated, 64 s wall). Output captured at
`/tmp/picogus_diag_run.txt`.

#### Theory 2: **DEAD**

Firmware reaches DMA dispatch. DMA CH0 programmed with
`WRITE_ADDR=0x50300010` (PIO1 TXF0), TREQ=8 (PIO1-TX0), EN=1,
`ever→TXF=0x1`, xfers=**143 324**, DREQ seen YES. Verdict:

> `ENGINE SERVED, PIO DIDN'T CONSUME (CH0 xfers=143324) —
> investigate PIO1 SM0 pull/shift or TX FIFO sink plumbing`

CH1 = 0x50300020 → SRAM (PSRAM RX path, 2 triggers), CH2 also
configured for TXF0 (chain-to, never triggered).

PIO1 SM0 PC=0x19, running, autopull enabled, PULL_THRESH=8. FSTAT
says TX empty. PSRAM CS falling edges = 2 (i.e. 2 SPI frames ever
started). PSRAM buffer all zero. So 143 K DMA byte-writes to PIO1
TXF0 are going *somewhere* but not driving the PSRAM SPI program.
This is a **new class of bug** not on either theory.

#### Theory 1: **inconclusive — diagnostic decode bug**

Reconciliation invariant holds:
`captured(0) + misattr(126 863) + orphans(0) = 126 863 == autopush_count`.

But `captured = 0` everywhere and `misattr = 126 863` everywhere
is a tell. 100 % misattribution means every physical push decoded
to an (addr, data) pair that didn't match the event we just fired.

Root cause: **the decode formula in the harness is wrong**. PIO0
SM0 `SHIFTCTRL=0x012b0000` → `IN_SHIFTDIR=0` (shift left). The
autopushed ISR layout with left-shift across two INs (10 then 8
bits) is:

```
ISR = (addr & 0x3ff) << 8 | (data & 0xff)
```

So correct decode is `data = word & 0xff; addr = (word >> 8) & 0x3ff`.

The HLD and implementation used the right-shift formula (`addr =
(word >> 14) & 0x3ff; data = (word >> 24) & 0xff`), which is wrong
for this SM.

Secondary signal worth keeping: clustering table shows **zero
captures in deciles 0–4** (firmware boot), then captures ramp to
20–40 % in deciles 5–9. Theory 1 as framed ("setup events lost
because PIO can't keep up") may be partially alive for the
second-half captures, but we can't tell until the decode is fixed.

### Phase 5 — Two parallel workstreams in flight

**Workstream A — decode fix (small, self-contained).**
- Dispatched: one impl agent (TDD, small change), then code review +
  apply-feedback. After that, supervisor re-runs the harness and
  interprets the real distribution.

**Workstream B — TXF0 drop investigation (the real prize).**
- Explore agent mapped the path end-to-end. Each hop looks correct:
  - `dma.rs:611-620` — DMA dispatches to `bus.write8` for DATA_SIZE=0.
  - `bus/mod.rs:1138-1145` — `Bus::write8` byte-replicates and calls
    `PioBlock::write32(TXF_offset, replicated, 0)`.
  - `pio/mod.rs:486-490` — `PioBlock::write32` on TXF arms calls
    `self.sm[n].tx_fifo.push(val)` **and discards the return value**.
  - `PioFifo::push` returns `false` silently when `count >= depth`.
- Explore agent's leading theory: the TX FIFO fills faster than SM0
  drains it, and `push()` silently drops when full. DMA's
  `transfers_issued` increments regardless, giving the illusion of
  143 K transfers against an empty-looking TX FIFO.
- DREQ should gate against this (`tx_dreq` returns `!tx_fifo_full`),
  but either DREQ is asserting spuriously or something in the DMA/SM
  interleave races the FIFO-full condition.
- **Next step: instrument `PioFifo::push` to count drops, rerun, and
  confirm/deny the theory before fixing.**

### Phase 6 — TX-FIFO push-drop theory **REFUTED** (complete)

Instrumented `PioFifo` with `push_success` / `push_drop` counters.
Diagnostic output:

```
PIO1 SM0 TX: push_ok=143324     push_drop=0
```

**Zero drops.** All 143 K DMA byte-writes successfully landed in the
PIO1 SM0 TX FIFO. The DMA → `bus.write8` → byte-replicate →
`PioBlock::write32` → `tx_fifo.push` path is working correctly.

But PIO1 SM0 TX FIFO level at end = 0 (empty) — so SM0 consumed all
143 K pushes. Yet:
- CS falling edges: 2 (should be tens of thousands)
- MOSI (GPIO3) pad_out = 0
- PSRAM non-zero: 0

**New hypothesis:** PIO1 SM0's program fires 2 SPI transactions
successfully (initial PSRAM init / chip-id reads, perhaps), then
enters a state from which it can't escape — possibly:
- Waiting on MISO (GPIO0) feedback that our PSRAM model never
  drives.
- Waiting on an IRQ/cross-SM signal that never asserts.
- SM0's program has a loop we don't understand that gates further
  work on some external condition.
- Our PIO model fails to wire sideset/out to GPIOs for *some*
  program shapes even though CS worked for the first two frames.

### Phase 7 — Investigate why SM0 stalls after 2 CS frames

Plan: dispatch an Explore agent to:
1. Dump PIO1 SM0's program (raw INSTR_MEM backing) and disassemble.
2. Identify the PC=0x19 instruction and what gating condition SM0
   faces there.
3. Inspect the PSRAM model (`mdpicoem-devices`) — does it drive MISO
   in response to SPI commands, or is MISO stuck at 0?
4. Cross-check PIO1 SM3 (also enabled per CTRL=0x9 = SM0+SM3)
   since SM3 clkdiv=131 is very slow (could be generating I2S
   timing) — maybe SM0 is waiting on SM3's IRQ.

### Phase 5 — Re-run with corrected decode (complete)

After the left-shift decode fix landed (harness `0.27.1`,
`(addr, data) = (word >> 8 & 0x3ff, word & 0xff)`), clean Theory 1
data:

| Port class | fired | captured | drop% |
|---|---|---|---|
| 0x343 `<- 0x43` (DRAM-low reg) | 102 898 | 24 705 | 76.0 % |
| 0x343 `<- 0x44` (DRAM-high reg) | 102 898 | 24 320 | 76.4 % |
| 0x344 `addr_lo` | 205 836 | 26 206 | 87.3 % |
| 0x344 `addr_hi` | 205 836 | 31 100 | 84.9 % |
| 0x347 `<- data` (DRAM byte) | 102 898 | 20 459 | 80.1 % |

Reconciliation invariant clean:
`captured(126 848) + misattr(15) + orphans(0) = 126 863 == autopush`.

**Theory 1 is dead.** Setup events (`0x343 <- 0x43/0x44`) capture at
~24 %, data events (`0x347`) at ~20 %, 16-bit address writes (`0x344`)
at ~13-15 %. All roughly comparable — no differential loss that would
make the firmware see data writes without address context. The 80 %
overall drop is a uniform effect, dominated by:

1. First 50 % of the trace has **zero** captures (firmware boot +
   PSRAM init — PIO0 SM0 isn't enabled yet).
2. During the live period (deciles 5–9), ~38 % of fires are
   captured; the remaining ~62 % are lost to PIO clkdiv=2 and
   firmware ISR drain rate not keeping up.

Both effects are expected given the hardware model. Neither class is
specifically disadvantaged.

So: the firmware **does** see the setup sequences (24 % is plenty of
`0x43 + 0x44 + addr + data` triplets to program the DRAM address
register), but PSRAM is still empty. The blocker is downstream of
capture — at the DMA→TXF0 sink or the PIO1 SM0 pull.





Supervisor reviewed both drafts and dispatched revision prompts back to
each agent. Core feedback:

**Capture Coverage HLD:**
- MUST-FIX: correlation by `(after − before)` delta alone is ambiguous
  under push-late scenarios (setup push lands in next event's drive
  window → misattribution across port classes, smearing the *exact*
  setup-vs-data signal we need). Resolution: add `pub last_autopush_word:
  u32` on `StateMachine` (2-line library change, no feature flag) and
  decode/match against fired event.
- MUST-FIX: `drive_write_cycle` hold is 37 sysclks (WRITE_ADDR_HOLD=12 +
  WRITE_DATA_HOLD=25 per `81873b9`), not the "~74" the draft cited.
- SHOULD/NITs: update files-changed list; bucket `0x345` by exact value;
  specify post-roll orphan reconciliation.

**DMA Dispatch HLD:**
- MUST-FIX: counter names like `trig_al1_count_count` read as typos;
  rename to `trig_ctrl`, `trig_write_addr`, `trig_trans_count`,
  `trig_al2_trans`, `trig_al3_read_addr`, `trig_multi`.
- MUST-FIX: DREQ observability is load-bearing for the "triggered but
  never served" verdict (need to distinguish firmware-fault from
  emulator DREQ-gap). Require inspection of `dma.rs` to confirm whether
  a DREQ path exists; add `dreq_observed_mask: u32` sticky flag; update
  verdict classifier.
- SHOULD/NITs: explicit alias-2/3 sticky-mask semantics; cite the
  `pub pio` / `pub gpio_in` precedent line numbers; tighten acceptance
  criterion 5 to name the TREQ_SEL value on failure.

