# Journal — Tracing Infrastructure Implementation

**Date**: 2026-04-16
**HLD**: `wrk_docs/2026.04.16 - HLD - Workspace Tracing Infrastructure.md`
**Goal**: Add `tracing` crate across the workspace per the HLD.

## Plan (from HLD)

1. Add workspace dependencies to `Cargo.toml`
2. Rename bespoke MMIO trace (`trace_enabled` → `mmio_trace_enabled`, etc.)
3. Instrument mdrp2350 and mdrp2040 cold paths
4. Add subscriber init (shared `harness_tracing_init()` + TUI apps)
5. Update CLAUDE.md with Logging & Tracing section
6. Instrument remaining subsystems (incremental, as needed)

## Progress

### Stage 1 — Workspace dependencies
- **Status**: Complete (commit 3e80c02)
- Added `[workspace.dependencies]` for `tracing` + `tracing-subscriber`
- Wired into all 6 default-member crates
- `cargo check` clean

### Stage 2 — MMIO trace rename
- **Status**: Complete (commit 3e80c02, combined with Stage 1)
- Renamed `trace_enabled` → `mmio_trace_enabled`, etc. in both bus crates
- Updated all call sites (bus, tests, harness binaries, decode.rs comment)
- Zero old-name references remain in any `.rs` file
- 1432 tests pass (893 + 539)
- Devil's advocate confirmed: no hidden references, rename scope is complete

### Stage 3 — Instrument cold paths
- **Status**: Complete (commit c0303ef)
- mdrp2350: exceptions, bus faults (set_bus_fault helper), clock tree, emulator init, spinlocks
- mdrp2040: same coverage, plus set_bus_fault helper consolidation
- Code review caught 2 blockers (B1: inconsistent bus fault coverage, B2: warn too noisy) + 3 suggestions — all applied
- PIO SM enable deferred (lives in mdpicoem-common, Stage 6 territory)
- 1391 tests pass

### Stage 4 — Subscriber init
- **Status**: Complete (commit c4cc921)
- Shared `harness_tracing_init()` in harness lib.rs (try_init, stderr, default warn)
- Wired into all 26 harness binaries
- TUI apps: inline subscriber init to stderr before TUI setup
- Default level: warn. Override via RUST_LOG.

### Stage 5 — Update CLAUDE.md
- **Status**: Complete (commit 2de05b5)

### Final audit
- **Completeness auditor**: 10/10 items PASS. Minor gap: `warn!` missing from CLAUDE.md level guide.
- **Devil's advocate** found:
  - CLAUDE.md level guide claimed DMA/PIO/FIFO instrumentation that doesn't exist → fixed
  - mdrp2040 emulator construction info missing step_quantum/sys_clk_hz → fixed
  - 3 HLD instrumentation points unimplemented (PIO, DMA, peripheral init) → intentional Stage 6 deferral
  - HardFault at info not warn → intentional B2 review fix
  - Spinlock missing core field, bus fault missing rw/size → requires plumbing, deferred
- All fixes applied, both debug and release builds clean, all tests pass

### Stage 6 — Remaining subsystems (deferred)
- **Status**: Deferred (per HLD: "as those areas are worked on")
- DMA channel start/complete
- PIO SM enable/disable (in mdpicoem-common)
- Peripheral init (UART, SPI, I2C, ADC)
- Core lockup (HardFault-in-HardFault)

## Summary

Implementation complete through Stage 5. Commits:
1. `3e80c02` — workspace deps + MMIO trace rename
2. `c0303ef` — cold path instrumentation (both crates)
3. `c4cc921` — subscriber init (26 harness binaries + 2 TUI apps)
4. `2de05b5` — CLAUDE.md documentation
5. (pending) — audit fixes (CLAUDE.md accuracy + mdrp2040 construction info)
