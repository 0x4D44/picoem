# test_silicon Orchestrator Implementation Journal

Date started: 2026-04-15
HLD: `wrk_docs/2026.04.15 - HLD - test_silicon Orchestrator and Coverage Expansion.md` (v1.1.0)

## Workflow

Supervisor (Claude Opus 4.6 with 1M context). Implementation delegated
to agent teams per stage:

- **Implementer** — does the work (Rust code, tests, config)
- **Code-reviewer** — `code-reviewer` skill, audits implementation
- **Devil's-advocate** — adversarial review, looks for what the others
  missed (state leakage, edge cases, oracle-design flaws)
- **Apply-feedback** — `code-review-apply-feedback` skill, applies
  reviewer + devil's-advocate input

Per stage: implement → review (parallel: code-reviewer + devil's-advocate)
→ apply-feedback → spot-check via `git status`/`git diff` summaries
→ commit. No manual debugging from supervisor.

## Stage list

| # | Stage | Status |
|---|---|---|
| 0 | HLD update (gap fixes from devil's-advocate read) | DONE |
| 1 | Per-core CYCCNT smoke binary | pending |
| 2 | Library-API extraction (refactor 3 existing oracles) | pending |
| 3 | test_silicon orchestrator | pending |
| 4 | Clock-reprogram scenarios + custom_sled | pending |
| 5 | silicon_dualcore_diff_rp2350 oracle | pending |
| 6 | silicon_isr_diff_rp2350 oracle | pending |
| 7 | Final HLD compliance audit | pending |

## Stage 0 — HLD revision (DONE)

5 critical fixes + 4 minor flags applied as v1.1.0:

1. Probe-session resilience for week-long soak (reattach, watchdog,
   give-up threshold, error log).
2. `bank_conflict_test` recast to K-delta protocol (not "cheap").
3. Cross-oracle state-cleanup contract (per-oracle exit cleanup).
4. Concretised ISR `image` layout (vector slots, code offsets, MSP).
5. Soak-mode quiet default + `--verbose` opt-in.
6. Cycle-vs-frequency semantic note for clock-reprogram.
7. M33 secure-state CPACR/VTOR addressing.
8. Workspace-dependency policy.
9. CLAUDE.md update step added to order-of-operations.

Committed before agent work begins.

## Stage notes (filled in as work progresses)

(populated per stage)
