# 2026-04-28 — Four-Track Test Infrastructure Push

Supervisor session. Arthur is asleep. Probes are running fuzz (RP2354: `test_silicon --soak 24h`; RP2040: `probe_diff_rp2040 --fuzz`). Goal is to push four parallel tracks of test-infrastructure work to 100% complete, using subagents for legwork, code-reviewer + code-review-apply-feedback for quality control, and TDD where it fits.

## The four tracks

1. **RP2040 IRQ plumbing in `CortexM0Plus::step`** — Wire `Bus::irq_pending` + `tick_peripherals` + pending-exception dispatch. Unblocks `silicon_isr_diff_rp2040` (oracle exists; emulator side stalled).
2. **`test_silicon_rp2040` orchestrator** — Port `test_silicon` shape to RP2040: probe_diff_rp2040 + silicon_periph_diff_rp2040 (+ silicon_isr_diff_rp2040 once #1 lands), soak mode, shuffle, watchdog, reattach.
3. **Lift `is_m0plus_safe` Thumb-32 filter on QEMU side** — Admit `BL`/`MRS`/`MSR`/`DSB`/`DMB`/`ISB` if QEMU handles them correctly, so we get a host-side oracle for the M0+ Thumb-32 subset.
4. **Extend `silicon_periph_diff_rp2350` scenario catalogue** — UART/SPI/I2C end-to-end transfers, ADC sample-rate, PWM duty cycle, WATCHDOG bite. Validation piggybacks on next probe-equipped soak.

## Approach

- **Phase A — Scoping** (parallel, 4 agents): Each agent investigates one track and reports a concrete plan + risks. Dispatch in one batch.
- **Phase B — Implementation** (parallel where independent): Implementation agents execute against the scoping plans. TDD for tracks where tests exist as forcing functions (Track 1 has `silicon_isr_diff_rp2040` and the Phase 1/2 mdrp2040 unit tests; Track 3 has `qemu_diff_m0plus` itself).
- **Phase C — Review** (parallel, 4 agents using `code-reviewer` skill): Independent review of each track's diff.
- **Phase D — Apply feedback** (parallel, 4 agents using `code-review-apply-feedback` skill).
- **Phase E — Final verification** (single agent): independent end-to-end confirmation that everything builds, tests pass, no scope cuts.

## Constraints I'm holding

- Don't compete with the probe-equipped fuzz runs on the harness binaries. Tracks 2/3/4 will rebuild `mdpicoem-harness`, which on Windows would fail if it tried to overwrite a running probe binary. The active runs were copied to `/tmp/probe_2040.exe` and `fuzz-runs/<pid>.exe` per the runbook, so `target/release/<bin>.exe` is free to be relinked. Confirm before each rebuild.
- No destructive git ops. Atomic commits per track when work is verified.
- Don't read code myself except when an agent's summary doesn't fit the file's reality and I have to redirect.
- Update this journal at every track transition.

## Status

- 21:00 local — Tasks 1–5 created. Phase A scoping agents dispatched in parallel.
- 21:25 local — All four scoping reports back.

## Phase A scoping outcomes

### Track 1 — IRQ plumbing already complete

The Plan agent's audit shows the plumbing is **already in place** as of HLD V5:
- `Bus::irq_pending: u32` — `crates/mdrp2040/src/bus/mod.rs:395`, getter at :1973.
- `Bus::tick_peripherals(cycles)` — `crates/mdrp2040/src/bus/mod.rs:1917-1969`, ticks UART/SPI/I2C/ADC/PWM/WATCHDOG_TICK and folds `nvic_bits`.
- `CortexM0Plus::step` calls `try_take_any_pending_exception` BEFORE `decode_execute` at `crates/mdrp2040/src/core/mod.rs:276`.
- `try_take_any_pending_exception` arbitrates PendSV (#14) / SysTick (#15) / NVIC IRQs at `crates/mdrp2040/src/core/mod.rs:330-375`.
- `bus.irq_pending → nvics[0/1].set_pending` drain at `crates/mdrp2040/src/lib.rs:791-801` once per quantum.
- Slow path `step_serial` at `crates/mdrp2040/src/lib.rs:771-777` calls `tick_peripherals → tick_systick → drain_pending_irqs_to_cores`.
- Cold IRQ entry (push 8-word frame, sync banked SP, set IPSR, vector fetch, EXC_RETURN ∈ {0xF1, 0xF9, 0xFD}) — full M0+ implementation at `crates/mdrp2040/src/core/exceptions.rs`.
- Binary banner at `crates/mdpicoem-harness/src/bin/silicon_isr_diff_rp2040.rs:104`: *"V5 IRQ plumbing is complete; EMU-side scenarios should pass."*

The user's framing ("plumbing is missing") came from stale doc comments at `crates/mdpicoem-harness/src/isr_scenarios_rp2040.rs:18-28`, which predate V5. Track 1 reduces to: (a) refresh stale comments; (b) add unit tests in `crates/mdrp2040/src/tests.rs` near the existing IRQ-drain coverage at lines 2497-2670 to forcing-function-lock the IRQ + PendSV/SysTick tail-chain behaviour; (c) silicon validation deferred to next probe slot.

### Track 2 — orchestrator port, with library extractions

Five-step plan: (A) lift shared infra to `crates/mdpicoem-harness/src/test_silicon_common.rs`; (B) extract periph runner to `crates/mdpicoem-harness/src/silicon_periph_rp2040.rs`; (C) extract probe_diff runner to `crates/mdpicoem-harness/src/probe_diff_rp2040_lib.rs`; (D) extend `isr_scenarios_rp2040::run_against` with `order` parameter and `exclude` field; (E) write `crates/mdpicoem-harness/src/bin/test_silicon_rp2040.rs`. Per-oracle 60s watchdog implemented as in-library `deadline: Option<Instant>` to avoid Windows USB thread-affinity issues. ISR oracle expected to FAIL until silicon validation, but orchestrator does not gate on it.

### Track 3 — clean lift

`is_m0plus_safe` at `crates/mdpicoem-harness/src/bin/qemu_diff_m0plus.rs:292-315` is a blanket "no wide" reject (line 296), not a bit-pattern test. `tech_debt.md:597-613` confirms it's "I never got around to it", not architectural divergence. QEMU's `cortex-m0` model executes BL/MRS/MSR/DSB/DMB/ISB. Plan: factor `m0plus_admits_wide` shared helper (mirroring `is_m0plus_silicon_safe` from `probe_diff_rp2040.rs:212-256`); update QEMU prologue to write `hw1` + BKPT at slot+4; dispatch `execute_one_wide_with_bus` on the EMU side; add a `generate_fuzz_t32_m0plus_wide` helper in `crates/mdpicoem-harness/src/thumb32_gen.rs`.

### Track 4 — 8 new scenarios

`PeriphScenario` definition at `crates/mdpicoem-harness/src/silicon_scenarios.rs:110-130`. Catalogue is the const slice at lines 1265-1591. Plan: add 8 scenarios — UART TX FIFO fill/RIS, UART RX loopback 4-byte, SPI loopback 4-byte, I2C master enable+IC_RAW_INTR_STAT, ADC continuous sample-rate, PWM slice0 duty CC observed, WATCHDOG bite REASON, WATCHDOG SCRATCH persists. New consts: WATCHDOG base+offsets, UART RIS/IFLS, SPI SSPSR/SSPCPSR, I2C IC_CON/IC_STATUS/IC_RAW_INTR_STAT, ADC DIV. Generous sysclk windows (4× nominal) to absorb silicon variation. WATCHDOG bite scenario has reset-survival risk — fallback is "TIME counted down without firing" if `run_scenario_with_retry` doesn't handle the reset.

## Sequencing

- Tracks 1, 3, 4 are pairwise non-conflicting on file paths. Dispatching them in parallel as Phase B.
- Track 2 modifies `lib.rs`, `isr_scenarios_rp2040.rs`, and other harness files; Track 3 also modifies `lib.rs`. Doing Track 2 sequentially after Tracks 1+3+4 land to avoid lib.rs merge conflicts and to use Track 1's refreshed isr_scenarios_rp2040 comments as a base.
- 21:30 local — Phase B (Tracks 1, 3, 4) dispatched in parallel.
- 21:55 local — Phase B for Tracks 1/3/4 returned. Cross-track substring conflict caught: Track 4's `uart0_rx_loopback_4bytes_fr_ris` was a substring of existing `uart0_rx_loopback`, breaking `test_silicon::test_real_catalogues_are_substring_unique`. Renamed to `uart0_rx_4byte_loopback_fr_ris` (supervisor edit, 2 lines).
- 22:05 local — Phase C reviews for Tracks 1/3/4 + Phase B for Track 2 (the orchestrator port) dispatched in parallel.
- 22:25 local — All four returned. Track 2 landed clean (482 lib tests pass, 28 test_silicon tests, 20 test_silicon_rp2040 tests). Phase C surfaced these required items:

## Phase C review findings — required changes

**Track 1** (1 required):
- `crates/mdrp2040/src/tests.rs:3433` — comment architecturally misleading. Reword tail-chain comment to reflect unstack-then-redispatch implementation order (HLD V5 §5.3).

**Track 3** (1 required):
- `crates/mdpicoem-harness/src/lib.rs` `m0plus_admits_wide` — tighten SYSm acceptance to the architected set `{0, 3, 5, 8, 9, 16, 20}` matching `execute_wide.rs:148-193`. Current "reject 17/19/>=0x80" rule is broader than the emulator accepts and would let architectural-divergence cases slip through.

**Track 4** (3 required):
- PWM scenario `silicon_scenarios.rs:1465-1477` — window math inverted; comment says no wrap but math implies ~2 wraps. Either reduce `max_sysclks` ≤ 8000 (no wrap) or rewrite comment + tighten observable to expect wrap.
- I2C scenario `silicon_scenarios.rs:1383-1411` — `IC_CON` discriminator value 0x61 vs reset 0x65 is dead — observe array doesn't include `IC_CON`. Add `(I2C0_BASE_RP2350 + I2C_IC_CON, 0xFF)` so a write-rejected scenario actually fails.
- WATCHDOG `silicon_scenarios.rs:1416-1432` — REASON observable is sticky across power cycles per datasheet §4.7.5; soak-flake risk. Drop REASON observable (CTRL.TIME mirror already proves countdown advanced) or pre-clear via W1C in setup.

## Phase D — apply feedback dispatched

Three apply-feedback agents (Tracks 1/3/4) plus one Phase C review agent for Track 2 dispatched at 22:30.

- 23:05 local — Phase D for Tracks 1/3/4 returned. All required items fixed; selected suggestions applied per skip-criteria. Final test counts:
  - Track 1: 3 tests pass (cold IRQ entry / tail-chain / PRIMASK), MMIO write consistency added, NVIC pending intra-loop assertion added.
  - Track 3: m0plus_admits_wide tightened to architected SYSm set {0,3,5,8,9,16,20}; M0PLUS_SYSM exported as canonical pub const; BL fuzz offset narrowed from ±2^20 to ±2^12 to fit QEMU microbit's 16 KiB SRAM (Q5 resolved); 483 lib tests pass.
  - Track 4: PWM scenario fixed (max_sysclks 20_000→8_000 for deterministic CTR, no wrap, INTR observable dropped); I2C IC_CON discriminator added to observe array; WATCHDOG REASON dropped from observable (sticky cross-reset hazard); tech_debt.md entry added; 33 silicon_scenarios tests + substring-uniqueness pass.

- 23:05 local — Track 2 review surfaced these required items:
  - **R1 — signal preservation in soak**: `Mismatch / ProbeError / UndefOnSilicon` all collapse to `Verdict::Fail`, distinguished only by `detail` prefix. `Summary::failing_cases` keys on `(oracle, case)` — second event overwrites first via smallest-seed dedup. SKIP: probe errors inside `Ok(outcomes)` don't trigger `reattach_with_retries`. Heartbeat doesn't separately count degraded transport.
  - **R2 — `run_against` return-type inconsistency**: ISR returns `Box<dyn Error>`, periph + probe_diff return `Box<dyn Error + Send + Sync>`. Unify on the latter for cross-thread composition.
  - **R3 — sled comment/assertion inconsistency** at `silicon_periph_rp2040.rs:330` vs `:864` (PC-relative bytes vs imm8 halfwords).
  - Plus suggested: per-scenario error fold-into-FAIL-outcome (resilience), Summary key allocation, Arm-error retry classification (Q2).

- 23:08 local — Track 2 Phase D dispatched.
- 23:30 local — Track 2 Phase D returned. All 3 required + 6 suggested + 2 questions applied.
  - **R1 implemented as Option A** (extended Verdict enum). New `Verdict::Skip` and `Verdict::Degraded` variants. `Stats` gained `skip_count` and `degraded_count`; `Summary` gained `skipping_cases` and `degraded_cases` BTreeMaps with smallest-seed dedup. Probe_diff mapping: `Mismatch → Fail`, `ProbeError → Degraded`, `UndefOnSilicon → Skip`. Heartbeat prints all four counters. Orchestrator forces `reattach_with_retries` on per-iteration degraded rate ≥ 25% (with `MIN_DEGRADED_SAMPLE = 20` floor to avoid noise on tiny iterations).
  - **R1 cross-cutting impact**: 8 M33 oracles got exhaustive `Verdict::Skip | Verdict::Degraded` match arms folding into existing fail counter (no behaviour change — M33 oracles don't emit those today). Files: `bank_conflict_test_rp2350`, `silicon_isr_diff_rp2350`, `silicon_periph_diff_rp2350`, `silicon_cycle_oracle_rp2350`, `silicon_dualcore_diff_rp2350`, `cycle_cases.rs`, `isr_scenarios.rs`, `test_silicon.rs`. `riscv_probe_spike` and the OneROM oracles use local `Verdict` enums and weren't affected.
  - **R2** unified `run_against` errors on `Box<dyn Error + Send + Sync>` (ISR was the outlier). 
  - **R3** sled comment now explains both `-3 halfwords` / `-6 bytes` lenses.
  - Suggested S1/S2/S3/S4/S6 + Q2/Q4 applied. Q1, Q3, S5 deferred per criteria.
  - **Version bumped** `mdpicoem-harness` 0.37.1 → 0.38.0 (new public Verdict variants → minor bump per CLAUDE.md).
- Final test counts after Phase D Track 2: `cargo test -p mdpicoem-harness --lib` 489 pass (was 483+); `--bin test_silicon` 28 pass; `--bin test_silicon_rp2040` 24 pass (was 20+; +4 degraded-rate trigger tests); `--bin probe_diff_rp2040` 4 pass. `cargo check -p mdpicoem-harness --all-targets` clean.
- 23:30 local — Phase E final verification agent dispatched.
- 23:50 local — Phase E returned: **PASS on all four tracks**.

## Phase E verification outcomes

- Track 1 PASS: 3 new tests present and passing; tail-chain comment correctly cites HLD V5 §5.3 unstack-then-redispatch order; PRIMASK test uses MMIO VTOR write + intra-loop pending assertion. mdrp2040 lib total 853 pass.
- Track 2 PASS: Verdict::Skip + Verdict::Degraded present; per-iteration degraded-rate trigger (25% / 20-sample floor) wired; probe_diff outcome mapping confirmed (Mismatch→Fail, ProbeError→Degraded, UndefOnSilicon→Skip); all three `run_against` signatures use `Box<dyn Error + Send + Sync>`; M33 oracles fold Skip|Degraded into existing fail counter (no behaviour change). Cargo.toml 0.38.0. Lib 489 pass / 1 ignored; test_silicon 28; test_silicon_rp2040 24; probe_diff_rp2040 4.
- Track 3 PASS: `m0plus_admits_wide` uses positive SYSm admit list; `pub const M0PLUS_SYSM: &[u8] = &[0, 3, 5, 8, 9, 16, 20]` exported; BL fuzz offset bounded to ±2^12 with QEMU microbit SRAM-layout comment. qemu_diff_m0plus 10 pass.
- Track 4 PASS: 8 scenarios in catalogue; PWM observable is CTR + CSR only (no INTR); max_sysclks 8_000 (deterministic, no wrap); I2C observable includes IC_CON discriminator; WATCHDOG observable is CTRL.TIME mirror only (REASON dropped); unused REASON consts removed; ADC RROBIN comment present; tech_debt.md entry added. silicon_scenarios 33 pass; substring-uniqueness pass.
- Cross-track PASS: rename complete, version bumped, no stale references.
- Build hygiene PASS: `cargo check -p mdrp2040 --all-targets` clean, `cargo check -p mdpicoem-harness --all-targets` clean. Workspace `cargo check --workspace --all-targets` fails on `epio-sys` (missing submodule) — pre-existing per CLAUDE.md (epio-sys excluded from default-members), not a regression.
- Verification noted one cosmetic detail: `Stats.skip` / `Stats.degraded` rather than `skip_count` / `degraded_count` (no functional effect).

## Summary

All four tracks of the test-infrastructure push are landed, reviewed, fixed, and re-verified. The work is **uncommitted** per Arthur's standing directive — supervisor leaves the staging decision to him in the morning.

### Net deliverables

- **mdrp2040**: 3 new forcing-function unit tests (cold IRQ entry, PendSV/SysTick tail-chain priority, PRIMASK masking) locking in V5 IRQ-dispatch behaviour. ARMv6-M doc refresh in three sites.
- **mdpicoem-harness**:
  - New library modules (`test_silicon_common`, `silicon_periph_rp2040`, `probe_diff_rp2040_lib`) extracted from previously-monolithic binaries, re-used by the new `test_silicon_rp2040` orchestrator.
  - New binary `test_silicon_rp2040` — RP2040 silicon orchestrator with soak mode, Fisher-Yates shuffle, 60s in-library deadline watchdog, hourly heartbeat, drop-and-reattach on transient probe errors, and per-iteration degraded-rate trigger (≥25% / ≥20 cases).
  - `Verdict` enum extended with `Skip` and `Degraded`; cross-cutting fold-in for 8 M33 oracles.
  - `qemu_diff_m0plus` now admits the M0+ Thumb-32 subset (BL/MRS/MSR/DSB/DMB/ISB) — previously unit-test-only on the QEMU oracle. Shared `m0plus_admits_wide` helper + canonical `M0PLUS_SYSM` const eliminate duplication with the silicon oracle.
  - 8 new `silicon_periph_diff_rp2350` scenarios for UART/SPI/I2C/ADC/PWM/WATCHDOG.
  - Cargo.toml 0.37.1 → 0.38.0.
- **tech_debt.md**: WATCHDOG REASON modelling + reset-survival entry.

### What's deferred

- Silicon validation of all four tracks against real hardware. The probes are busy with the overnight soak Arthur set up in the previous turn; first opportunity is after he frees them in the morning. `test_silicon_rp2040` orchestrator can take over the RP2040 probe slot on the next pass.
- One-shot live QEMU smoke run for the new Thumb-32 wide encodings. Defer to next QEMU-available slot.
- Q1 (between-iteration reset frequency in soak), Q3 (iter_seed u64 wrap), S5 (RESETS_BASE constant placement) from the Track 2 review — explicitly skipped per criteria.

### Suggested commit shape (when Arthur approves)

Five atomic commits, one per track plus the cross-cutting Verdict change:

1. `mdrp2040: add forcing-function tests for IRQ cold entry, tail-chain, and PRIMASK; refresh stale V5 doc comments`
2. `mdpicoem-harness: extend Verdict with Skip/Degraded; propagate exhaustive arms across M33 oracles` (this is the cross-cutting one — separate it so the M33 changes are reviewable independently)
3. `mdpicoem-harness: lift M0+ Thumb-32 filter on qemu_diff_m0plus oracle; share m0plus_admits_wide / M0PLUS_SYSM`
4. `mdpicoem-harness: extend silicon_periph_diff_rp2350 catalogue with 8 UART/SPI/I2C/ADC/PWM/WATCHDOG scenarios; tech_debt entry for WATCHDOG REASON`
5. `mdpicoem-harness: add test_silicon_rp2040 orchestrator + library extractions (test_silicon_common, silicon_periph_rp2040, probe_diff_rp2040_lib); bump 0.38.0`

Order matters: #2 lands the Verdict variants that #5 consumes; #5 lands the orchestrator; #1, #3, #4 are independent. If #2 and #5 must be one commit, that's fine — they're tightly coupled.
