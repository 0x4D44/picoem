# Phase 6 PIO Implementation Journal

**Date:** 2026-04-13
**LLD:** `wrk_docs/2026.04.13 - LLD - Phase 6 PIO.md` (V3, 0.3.0)
**Supervisor:** Claude (team lead)

## Status: IN PROGRESS — Stage A (review fixes applying)

## Plan

| Stage | Description | Status |
|-------|-------------|--------|
| A | Module structure + registers + bus dispatch | Review fixes in progress |
| B | Clock divider + instruction decoder + executor | Pending |
| C | Autopush/autopull + GPIO integration | Pending |
| D | Waveform integration tests | Pending |

## Stage A Log

### Implementation (complete)
- 4 new files: `pio/mod.rs`, `pio/sm.rs`, `pio/fifo.rs`, `pio/decode.rs`
- Bus integration: PIO dispatch in read32/write32, gpio_in move from SIO to Bus
- lib.rs cleanup: removed PioInterface/GpioChange, wired gpio_read/gpio_read_all
- SIO cleanup: removed gpio_in field
- 20 new tests, 428 total passing

### Code Review (2 reviewers: compliance + devil's advocate)

**5 fixes required before commit:**

| # | Issue | Severity | Found by |
|---|-------|----------|----------|
| 1 | `is_full()` returns true at depth=0 — FSTAT reports disabled FIFO as full | HIGH | Both |
| 2 | SM_RESTART flushes FIFOs — real hardware preserves FIFO contents | HIGH | DA only |
| 3 | read8/read16 missing PIO dispatch — sub-word reads return 0 | HIGH | Both |
| 4 | PIO step not wired into Emulator::step() | MEDIUM | Both |
| 5 | Dead `irq_force` field — stored but never read | MINOR | CR |

**Declined fixes (not blocking):**
- DBG_CFGINFO value matches LLD — leave as-is
- INPUT_SYNC_BYPASS alias — stub register, negligible
- sideset_pindirs field — Stage B can add
- FDEBUG two-stage match — cosmetic
- Test gaps (FJOIN_RX, SM3 registers) — nice-to-have

### Key Observation
The devil's advocate caught Issue 2 (SM_RESTART flushing FIFOs) that the code
reviewer missed. This validates having both perspectives — the DA's focus on
"what does real hardware actually do" caught a semantic error that pure code
inspection missed.
