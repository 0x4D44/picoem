# 2026.05.04 - Final Audit Feedback Cleanup

## Initial pass

Arthur asked for the final audit-feedback cleanup without committing. I read
the apply-feedback skill, `CLAUDE.md`, both OneROM HLDs, the current worktree
status, and the audit findings.

Decisions:

- The `cargo fmt --check` blocker is valid. I reproduced the failure before
  editing; rustfmt wants formatting-only changes in several harness files.
- The fixture-generalization HLD is a real referenced artifact. It is present
  but untracked; since this pass must not commit, I am retaining it and will
  call it out explicitly in the final handoff rather than rewriting references.
- The fixture README lacked fire-32-a provenance and hashes. I am adding the
  generated fixture, the source JSON, and the source SeaBIOS SHA-256 values.
- The old `ResolvedAddrOutOfRange` tech-debt subsection is stale after the
  Stage 4B/4C address matcher fix. I am deleting that subsection, leaving the
  new resolved Stage 4C summary at the top of `tech_debt.md`.
- `PioAddressShape` can derive its GPIOBASE window from `addr_pins` and
  `max_gpio`, while preserving the single-PIO-window assertion. I am making
  that small code change and adding a focused test that `chip_pins` no longer
  influences the shape.

Emotional temperature: steady but cautious. The worktree has other agents'
modified files and rustfmt will touch some of them; this is acceptable for the
formatting blocker, but I need to keep semantic changes narrow.

## Validation pass

Changes applied:

- Ran rustfmt after reproducing the `cargo fmt --check` failure.
- Added fire-32-a fixture provenance and SHA-256 values to the fixture README.
- Deleted the stale `ResolvedAddrOutOfRange` unreachable-runtime tech-debt
  subsection.
- Changed `PioAddressShape::from_fixture` to choose GPIOBASE from the parsed
  address-pin maximum and kept the single-window assertion.
- Added a focused unit test proving `chip_pins` no longer drives
  `PioAddressShape`.

Validation:

- `mdtimeout 600 cargo fmt --check` passed.
- `mdtimeout 1200 cargo test -q -p picoem-harness pio_address_shape` passed
  with 5 matching tests.
- `mdtimeout 1200 cargo clippy` passed.
- `mdtimeout 1200 cargo test -q` passed.
- `mdtimeout 1200 cargo test -q --features threading` passed.
- `git diff --check` passed.

Residual notes:

- The fixture-generalization HLD is still untracked but present; it is a
  required release artifact because committed code/docs refer to it.
- I did not commit and did not stage anything.

Emotional temperature: calmer after the full cargo gates passed. Remaining
concern is mostly release containment: the untracked HLD and pre-existing
untracked journals need to be handled deliberately by whoever packages the
change.
