# Journal: PIO Idle Skip Implementation

**Date:** 2026-04-14
**HLD:** `wrk_docs/2026.04.14 - HLD - PIO Idle Skip.md` (v1.0.0)
**Supervisor:** Claude (Opus 4.6)
**Goal:** Recover the ~65 MHz Phase 6 PIO tax on the basic paced_bench workload by adding a fast-path early-return in `PioBlock::step` / `step_n` when no SM is enabled. HLD gate: `paced_bench_rp2350 --seconds 5` Avg MHz ≥ 80 (current baseline ~38).

## Plan

Single logical change, one commit. No artificial staging — the HLD already scopes a minimal, internally-consistent diff.

### Pipeline

1. **Implementation** — one agent, TDD flow: write both new tests first (T1 + T2), then add field + helper + `reset` fix + early-outs, then migrate 4 production + 9 test call sites, then re-run all tests. Operates in a shared feature-branch worktree at `.claude/worktrees/pio-idle-skip`.
2. **Review + Devil's Advocate in parallel** — two agents independently audit the diff. Code-reviewer uses the `code-reviewer` skill; Devil's Advocate does an adversarial HLD-compliance read.
3. **Apply feedback** — one agent using the `code-review-apply-feedback` skill, synthesises both reviews.
4. **Verifier** — builds, tests, runs `paced_bench_rp2350 --seconds 5`, runs `qemu_diff_m33 --fuzz 1000`. Reports against the four HLD success criteria.
5. **Supervisor commit** — I commit once the gate is clean.
6. **Compliance audit** — an agent cross-references HLD v1.0.0 against the committed code. Separate Devil's Advocate looks for escaped gaps.
7. **Iterate** if either pass 6 audit fails.

I stay supervisory. I will not read code, run tests, or debug. I review agent reports, update this journal, and redirect as needed.

### Quality gates (from CLAUDE.md)

- Smallest change that satisfies the HLD — no over-engineering.
- No env vars, no feature flags, no conditional compilation.
- No migration / rollback / backwards-compat — this is a dev sandbox.
- Warnings-clean build.
- Existing tests migrated (not duplicated); two new tests added per HLD.

## Status log

Appended as work progresses.

### 2026-04-14 — Worktree + implementation

Worktree created at `.claude/worktrees/pio-idle-skip` (branch `pio-idle-skip`) from `main@b68583f`.

Implementation agent (TDD flow) — clean report:

- **Step (a) — tests first**: T1 (`idle_block_step_is_noop`) compiled and passed at HEAD (confirms HLD's "strict no-op" claim: `clock_tick` already short-circuits on `!enabled`). T2 (`disable_clears_pin_outputs`) failed to compile — no `set_sm_enabled` yet. Expected TDD signal.
- **Step (c) — guards added mid-migration**: **26 tests regressed** because the 9 enable-site test sites still bypassed the mask → fast-path skipped SMs the tests thought were running. Cleared once the 9 sites were migrated (steps d + e). This is the exact "Forgotten call-site migration" failure mode the HLD flagged, which means the mask invariant is working as designed.
- **Diff size**: 107 insertions, 13 deletions, single file (`crates/mdpicoem-common/src/pio/mod.rs`).
- **Final tests**: `mdpicoem-common` 75 passed / 1 failed (pre-existing pacer flake, stash-verified unrelated) / 0 ignored — +2 net vs 73 baseline. `mdrp2350` 566/0/2, `mdrp2040` 228/0/0 — zero deltas vs branch HEAD.
- **XOR borrow dance**: `self.set_sm_enabled(i, !self.sm[i].enabled)` is a two-borrow compile error; agent hoisted to `let toggled = !self.sm[i].enabled;` as directed.
- **Deviations from HLD**: none. Noted that HLD text says `clock_acc` but field is `clkdiv_acc` — test asserts the correct field name.

Working tree has single uncommitted file. No commit yet.

Next: parallel reviewers (code-reviewer skill + devil's advocate).

### 2026-04-14 — Reviews: both SHIP, no blockers

**Code-reviewer:** SHIP. Confirmed the only remaining raw write to `sm[i].enabled` workspace-wide is the one inside `set_sm_enabled` itself (independent grep). Build warnings-clean. Both new tests load-bearing (removing the merge call inside `set_sm_enabled` would break T2). Single NIT — wording on the XOR site's borrow-motivation comment — explicitly "wouldn't block on it".

**Devil's advocate:** SHIP. Walked all 10 adversarial angles:
- Escaped paths: zero.
- `reset` ordering: no staleness window; `StateMachine::reset` only reachable via `PioBlock::reset` (SM is `pub(crate)`).
- `merge_pin_outputs` sideset cleanup: the top-of-loop `if !sm.enabled { continue; }` covers pin_values AND sideset paths in one block — no residual bits.
- T2 coverage: would fail if the `merge_pin_outputs` call inside `set_sm_enabled` were removed — proper regression probe.
- `step_n` guard placement: `tick_peripherals` runs at quantum end, no interleaved CPU stepping can flip `enabled` mid-call. HLD claim holds.
- Non-idle path byte-identical to HEAD modulo one predicted-false branch at entry.
- Doc comments on field + helper explain the invariant without HLD references — ages well.
- Bench firmware doesn't touch PIO registers; mask stays 0 throughout bench — no practical risk to the fast path.
- `git status`/`diff --stat`: single file touched, no scope leak.
- All 9 test-site migrations accounted for (original line 720 was a read, correctly unchanged; its write on original line 711 migrated).

No apply-feedback round needed — the single NIT is cosmetic and CLAUDE.md guidance ("don't add comments unless the WHY is non-obvious") argues against churn.

Next: verifier agent — build + tests + `paced_bench_rp2350 --seconds 5` + `qemu_diff_m33 --fuzz 1000`.

### 2026-04-14 — Verifier: 4/4 gates PASS

| Gate | Result | Measured |
|---|---|---|
| Tests green | PASS | common 75/1(pacer flake)/0; mdrp2350 566/0/2; mdrp2040 clean. New tests both present and passing. |
| Warnings-clean release build | PASS | `Finished release profile`, zero `warning:` lines. |
| paced_bench ≥ 80 MHz | PASS | Median **82.5 MHz** single-core (runs 82.5 / 82.6 / 82.3, spread 0.3). Dual-core secondary: 43.3 MHz/core (1.55× baseline). |
| qemu_diff_m33 --fuzz 1000 | PASS | 39,643 / 39,643 passed (harness expanded 1000 → 39,643 across classes), seed 1776203321962360500. |

Single-core recovery 38 → 82.5 MHz = **2.17× lift**, clears the ≥80 commit-go gate. Landed at the low end of the projected ~80-110 MHz window — the 65 MHz Phase 6 tax isn't fully recovered (~44.5 of 65 MHz recovered, ~69%), implying a secondary bottleneck is now next on the critical path. Out of scope for this HLD.

Safe to commit. Proceeding to supervisor commit.
