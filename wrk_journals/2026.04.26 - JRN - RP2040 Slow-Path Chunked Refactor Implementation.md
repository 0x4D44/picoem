# 2026-04-26 — JRN — RP2040 Slow-Path Chunked Refactor Implementation

## Plan

Implement the design captured in `wrk_docs/2026.04.26 - HLD - RP2040 Slow-Path Chunked Peripheral Advance V5.md`. Worktree: `D:\language\mdrp2354-perf`, branch `perf/rp2040-opl3`.

Stages, each its own commit:

1. **Stage 1 — TDD test + audits**: add §8.4 dual-core SysTick positive test; observe it FAILs on `main` (proving the bug); re-run §8.1-§8.3 audits; commit the test as a pre-refactor gate.
2. **Stage 2 — Chunked refactor**: `Bus::tick_peripherals` signature change, new `tick_systick(c0, c1)`, rename `tick_pio_and_route_irqs_single` → `tick_pio_and_route_irqs(cycles)`, restructure `step_serial` slow branch (move `update_gpio` out of inner loop, add `c0_total`/`c1_total` accumulators), update test fixture, bump `mdrp2040` Cargo version 0.19.0 → 0.19.2. Single focused commit. After this commit, the §8.4 test from Stage 1 must PASS.
3. **Stage 3 — Validation**: quantum sweep at q=1/4/16/64; cake-gate on chunked path; record numbers; revert if q=4 wall reduction <10%.

Per stage:
- Implementation agent (general-purpose) writes the code.
- Code-reviewer agent reads via the code-reviewer skill.
- Devil's advocate agent challenges both the implementation and the review.
- Apply-changes agent consolidates feedback via code-review-apply-changes skill.
- Supervisor (me) commits at stage boundary; agents do not commit.

After Stage 3: completeness audit comparing HLD V5 line-by-line against the landed code.

## Status

- [ ] Stage 1 — implementation
- [ ] Stage 1 — review
- [ ] Stage 1 — commit
- [ ] Stage 2 — implementation
- [ ] Stage 2 — review
- [ ] Stage 2 — commit
- [ ] Stage 3 — validation
- [ ] Stage 3 — commit (or revert)
- [ ] Final — completeness audit

## Notes

- HLD has been through 5 iterations (V1 → V5) of multi-agent review. V5 is the source of truth for implementation.
- Pre-refactor SysTick bug is documented at HLD §5.2.1 (`crates/mdrp2040/src/lib.rs:730-749`); already-correct threaded reference at `crates/mdrp2040/src/threaded/emulator.rs:624-629`.
- Default `step_quantum` is already 4 (landed in `mdpicoem-harness` 0.37.1 earlier today).
- Per CLAUDE.md "Multi-agent environment" — if agents hit build errors in code they didn't touch, wait + retry. Don't fix unrelated breakage.
