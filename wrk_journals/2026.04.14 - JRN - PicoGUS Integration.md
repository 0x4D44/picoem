# PicoGUS Integration — Implementation Journal

Date started: 2026-04-14
HLD: `wrk_docs/2026.04.14 - HLD - PicoGUS Integration.md`
Supervisor: Claude (Opus 4.6)
Journal owner: Arthur

## Plan

Sequential delivery of six HLD stages:

1. XIP flash model in `mdrp2040`
2. External SPI PSRAM model
3. DOSBox-X GUS-write tap + trace format
4. Trace replayer + GPIO injection (harness binary)
5. I2S output capture → WAV
6. End-to-end MIDI demo

Per-stage workflow:

1. **Implementation agent** — TDD where sensible: tests first, then
   code, ensuring warnings-clean build and green test run.
2. **Code-reviewer agent** — uses `code-reviewer` skill, produces
   review notes.
3. **Apply-feedback agent** — uses `code-review-apply-feedback` skill,
   addresses review.
4. **Devils-advocate agent** — challenges the finished work: edge
   cases, over-engineering, HLD drift, testability.
5. Supervisor commits the stage, updates this journal.

Final phase: comparison agent audits the whole delivery against the
HLD; iterate until 100%.

## Supervisor principles

- Protect context: delegate reading, writing, testing, debugging.
  Receive <200-word structured summaries from agents.
- Do not modify code directly. Guidance only.
- Commit after each stage with co-author trailer.
- Journal is the source of truth for cross-stage context.

## Known external dependencies

- **DOSBox-X patched build** — required to actually generate the ISA
  trace from the target MIDI. The patch itself (Stage 3) is written
  and committed; *building* a patched DOSBox-X and running it against
  `d:\language\mdsc55\tests\doom2_map01_running_from_evil.mid` is an
  external step. Stage 4 replay is validated with a synthetic
  hand-crafted trace meanwhile.
- **PicoGUS firmware binary** — must be vendored under
  `third_party/picogus/`. Version pin pending Stage 2 research.
- **Target MIDI** — `d:\language\mdsc55\tests\doom2_map01_running_from_evil.mid`
  (Arthur-provided).

## Research open items

- PSRAM mode (SPI vs QPI) and exact command subset used by PicoGUS
  firmware. Research task delegated at kickoff; findings recorded in
  Stage 2 entry.

## Stage log

(Entries appended as stages complete.)

---

## Stage 0 — Setup

**Complete.** Journal skeleton created. Research agent dispatched for
Stage 2 prep. Stage 1 team briefed.

### Research findings — PSRAM protocol (Stage 2 input)

- **Chip:** apmemory APS6404L-3SQR-SN (8 MB QSPI pseudo-SRAM).
- **Mode:** single-SPI only. Firmware never issues `0x38` (enter QPI).
- **Command set actually used (complete truth set for modelling):**
  - `0x66` — Reset Enable (50 μs busy-wait follows)
  - `0x99` — Reset (100 μs busy-wait follows)
  - `0x02` — Write (24-bit address + data)
  - `0x0B` — Fast Read (24-bit address + 8 dummy cycles + data)
- **Pinout (PicoGUS v4.0.0):** GPIO0=MISO, GPIO1=CS, GPIO2=SCK,
  GPIO3=MOSI. Driven by **PIO1** (single state machine, DMA-fed).
- **PIO sideset:** bit1=SCK, bit0=CS.
- **Fudge factor:** at high SPI rates, reads need +1 SCK cycle before
  data; firmware auto-detects via `test_psram` at boot.
- **Firmware version to pin:** `v4.0.0` (2026-04-06).
  `rp2040-psram` submodule SHA `7786c93e`.

No QPI state machine, no READ-ID, no wrap-boundary config to model.
Four commands + fixed timing. Stage 2 is smaller than feared.

---

## Stage 1 — XIP flash in mdrp2040

**Start/End:** 2026-04-14 (single session)
**Status:** complete, committed
**Agents:** impl-stage1, reviewer-stage1, apply-feedback-stage1,
fix-missing-builder, devils-advocate-stage1

### Outcome

- `mdpicoem_common::Memory` gains `with_flash(rom, sram, flash)`
  ctor + `load_flash` clamp-into-pre-sized-buffer semantics.
  `with_sizes` preserved for mdrp2350.
- `mdrp2040::Bus` routes reads in `0x10000000..0x10200000` and three
  XIP aliases (`0x11/0x12/0x13`) to the 2 MB flash buffer. Writes
  silently ignored. No cache / fetch-timing model (per HLD).
- `mdrp2040::Emulator::load_flash(&[u8])` + `EmulatorBuilder::flash(Vec<u8>)`.
  Oversize inputs silently clamped to 2 MB.
- Version bumps: `mdpicoem-common` 0.2.0 → 0.3.0, `mdrp2040`
  0.2.1 → 0.3.0.
- 12 new tests (3 common memory, 5 bus XIP, 4 firmware smoke).
- `cargo test -p mdrp2040` — 240/240 pass. `cargo test -p
  mdpicoem-common` green modulo pre-existing `pacer` flake.

### Process notes / lessons

The implementation agent reported `lib.rs` changes had landed but
they hadn't — the initial reviewer erroneously confirmed presence
by reading git log rather than running the build. Apply-feedback
caught it by running `cargo build`. Fix agent added the ~13
missing lines (`EmulatorBuilder::flash` method + wire-up in
`build()`); `Emulator::load_flash` was already present.

**Lesson:** reviewers must run the build, not just read code. Will
bake into future reviewer briefs.

### Devils-advocate findings (logged in `tech_debt.md`, non-blocking)

1. `mdrp2040app` CLI has no `--flash` flag — HLD wording drift.
2. `Memory::load_flash` branch on `xip.is_empty()` — resize-once
   footgun on mdrp2350 (no current call site hits it).
3. XIP reads past loaded length don't mirror within the 2 MB alias.

None block Stage 2. Smoke test (`mdrp2040app/tests/smoke.rs`)
exercises the full bootrom→flash→GPIO25 path, so functional
coverage is real.

---

## Stage 2 — External SPI PSRAM model

**Start/End:** 2026-04-14 / 2026-04-15
**Status:** complete, committed
**Agents:** impl-stage2, reviewer-stage2, apply-feedback-stage2,
devils-advocate-stage2

### Outcome

- New module `crates/mdrp2040/src/peripherals/` with `psram.rs`.
- `Psram` owns `Box<[u8; 8 << 20]>` + a `Phase` state machine (Idle /
  Cmd / WriteAddr / WriteData / ReadAddr / ReadDummy / ReadData /
  SilentNop). Pins hardcoded GPIO0=MISO, GPIO1=CS, GPIO2=SCK,
  GPIO3=MOSI per PicoGUS v4.0.0.
- Models exactly the 4-command APS6404L subset firmware uses: 0x66,
  0x99, 0x02, 0x0B (with 8-cycle dummy). No QPI, no READ-ID, no wrap
  config.
- Hooked into `Emulator::update_gpio()`: `psram.tick(pads)` runs after
  the PIO/SIO merge, optionally splicing MISO into `gpio_in` bit 0.
- Version bumps: `mdrp2040` 0.3.0 → 0.4.0, `mdpicoem-common` 0.3.0 →
  0.4.0 (new `PioBlock::any_sm_enabled` accessor).
- `cargo test -p mdrp2040` — 256/256 pass (240 baseline + 14 PSRAM + 2
  PIO-driven integration).

### Critical mid-stage fix — quantum-granularity integration

Initial implementation called `tick_pio(consumed)` (bulk) then
`update_gpio()` (once) per `Emulator::step()`. With step_quantum=64,
PSRAM only saw the final pin snapshot of each quantum → every
PIO-driven SCK edge slipped. Reviewer caught the architectural gap;
apply-feedback agent replaced the tail with a gated interleave:

```rust
if pio_idle {
    self.tick_pio(consumed);
    self.update_gpio();
} else {
    for _ in 0..consumed {
        self.tick_pio(1);
        self.update_gpio();
    }
}
```

`pio_idle := !pio[0].any_sm_enabled() && !pio[1].any_sm_enabled()`.
`paced_bench_rp2040` before/after: 118.4 → 119.1 MHz avg (within
variance). Added two PIO-driven tests proven to fail against the old
bulk path.

### Process notes / lessons

- Reviewer actually ran `cargo build`/`cargo test` this time (Stage 1
  lesson internalised) and caught the quantum-granularity bug that
  unit tests missed.
- Devils-advocate spotted the missed `mdpicoem-common` version bump;
  supervisor applied the one-line fix.

### Devils-advocate findings (logged in `tech_debt.md`, non-blocking)

1. PSRAM PIO-integration tests cover only 1 SCK edge per quantum —
   wouldn't catch a regression to "update_gpio twice per step".
   Needs a multi-edge-per-quantum stress test.
2. Enable-then-disable mid-quantum drops edges via the fast-path
   predicate. Pathological in real firmware; flagged for completeness.

### Notable design decisions

- **`Box<[u8; 8 << 20]>` via `vec! → into_boxed_slice → try_into`:**
  direct `Box::new([0u8; N])` blows the 1 MB Windows debug stack
  because the 8 MB array is materialised on the stack pre-move. Worth
  remembering if we add larger backing stores.
- **`pio_idle` gate:** the naive interleave regressed paced_bench by
  ~49% on pure-ALU workloads. Gating recovered the full throughput.
  Safe because when both PIO blocks are disabled, pin state cannot
  change during the peripheral-tick window.

---

## Stage 3 — DOSBox-X tap + trace format

**Start/End:** 2026-04-15 (single session)
**Status:** complete, committed
**Agents:** impl-stage3, reviewer-stage3, apply-feedback-stage3,
devils-advocate-stage3

### Outcome

- `third_party/dosbox-x-picogus-tap.patch` — 103-line unified diff
  against DOSBox-X master `f43ce61d`. Hooks `read_gus` / `write_gus`
  in `src/hardware/gus.cpp`. Env-gated via `PICOGUS_TAP_FILE`.
  Emits CSV: `# picogus-tap v1\nns,port,value,kind\n<rows...>`.
- `third_party/README.md` — apply / build / use instructions, pinned
  SHA, known limitations.
- `crates/mdpicoem-harness/fixtures/sample_gus.trace` — 19-event
  hand-crafted fixture covering all 4 `kind` values. Used by Stage 4
  replayer tests.

### Design choices

- **Hook `read_gus` / `write_gus`** (the `Bitu(port, iolen)` entry
  points in gus.cpp) rather than the wrappers — captures every GUS
  I/O path with zero duplication.
- **`PIC_FullIndex()` × 1e6 → u64 ns** with a monotonicity clamp
  (`static last_ns`) to defend against pause/resume backwards steps.
- **Reentrant guard** around the 16-bit read recursion so each
  logical access logs exactly one row.
- **Env var carve-out** — HLD bans env vars in *our* Rust code;
  DOSBox-X is third-party, debug-only, and lacks a cleaner
  mechanism. Documented in README.

### Devils-advocate findings (tech_debt, non-blocking)

1. **GUS DMA channel traffic may not be captured.** The tap covers
   `read_gus` / `write_gus` but GUS DRAM DMA transfers may bypass via
   `GUS::DMA_Callback` depending on DOSBox-X internals. If bypassed,
   patch samples never reach PicoGUS's PSRAM in replay → Stage 6 is
   silent. **Must resolve before Stage 6.** Three mitigation options
   in tech_debt.md.
2. Reentrant guard not exception-safe (low priority).

---

## Stage 4 — Trace replayer + GPIO injection

**Start/End:** 2026-04-15 (across a rate-limit-interrupted session)
**Status:** complete, committed
**Agents:** impl-stage4 (rate-limited mid-task), impl-stage4-finisher,
reviewer-stage4, apply-feedback-stage4, devils-advocate-stage4,
blockers-fix-stage4

### Outcome

- `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs` (~1080 LOC)
  — single-binary harness that parses the PicoGUS CSV trace, advances
  the `mdrp2040::Emulator` to each event's target cycle, and pokes the
  Bus's new `external_gpio_in_override` / `external_gpio_in_mask`
  fields to synthesise an idealised ISA write waveform. Reads are
  skipped per HLD.
- CLI: `--flash`, `--trace`, `--duration`, `--post-roll` (default
  0.5 s for post-event firmware drain).
- `ReplaySummary` reports events fired / skipped / stalled, final
  cycle count (including post-roll), wall/sim ratio.
- Emulator constructed with `step_quantum=1` for per-cycle ISA
  waveform precision — essential because the synthetic cycle is only
  ~37 sysclks wide.
- Defensive: one-shot stall warning if the emulator wedges (e.g. no
  firmware loaded, cores HardFault to halt).

### Cross-crate infrastructure change (B1 fix)

Added `external_gpio_in_override: u32` + `external_gpio_in_mask: u32`
fields to `mdrp2040::Bus`. `Emulator::update_gpio` applies them AFTER
the SIO/PIO merge and PSRAM MISO splice — last-writer-wins on
masked pins. This is what lets the replayer's waveform survive
`update_gpio` calls within a step_quantum=1 loop.

`mdrp2040` version bump 0.4.0 → 0.5.0.

### Tests

- 3 new `mdrp2040` unit tests covering the override: default merge,
  override-wins-over-SIO, reset clears override.
- 4 new `picogus_diff_rp2040` tests: post-roll advances sink,
  post-roll zero = None, stall counter increments, end-to-end with
  real `Emulator` sink.
- 10 prior parser / replay / GPIO-poke tests retained.
- Totals: `mdrp2040` 259/259, `picogus_diff_rp2040` 17/17.

### Process notes / lessons

1. **First implementation agent hit Anthropic rate limit mid-task.**
   Recovered via a finisher agent that ran the build, found a
   dead-code warning + a `replay()` hang on no-firmware (cycle counter
   never advanced → spin forever), and fixed both.
2. **Devils-advocate caught 3 Stage-6 blockers the reviewer missed.**
   All three were masked by tests using a mock `IsaSink` that
   bypassed `update_gpio` entirely. Lesson: reviewers must also check
   that tests exercise the integration boundaries their assertions
   claim to cover, not just the assertions themselves.

### Deferred to tech_debt (not blocking Stage 5)

1. **Pin mapping cross-check vs polpo/picogus v4.0.0** — resolved in
   Stage 5 (vendored pin constants into `picogus_pins.rs`, verified
   from firmware source). I2S on GPIO16-18, no collision with ISA.
2. **write16 → two write8s** semantically incorrect for GUS 16-bit
   registers. Defer until Stage 6 surfaces a real failure.
3. Stale comment near `drive_pins` (1-minute tidy) — fixed in Stage 5
   as part of pin-constant refactor.

---

## Stage 5 — I2S capture to WAV

**Start/End:** 2026-04-15 (single session)
**Status:** complete, committed
**Agents:** impl-stage5, reviewer-stage5, apply-feedback-stage5,
devils-advocate-stage5, cycle-counter-fix-stage5

### Outcome

- `crates/mdpicoem-harness/src/i2s_capture.rs` — Philips-I2S decoder
  (16-bit stereo, MSB-first, LRCLK low=left) + hand-rolled 44-byte
  canonical PCM WAV writer. Edge-triggered: shift on BCLK rising,
  finalise on LRCLK transition.
- `crates/mdpicoem-harness/src/picogus_pins.rs` — authoritative
  single source of truth for pin constants. Cross-checked against
  PicoGUS v4.0.0 firmware source: I2S DOUT=16, BCLK=17, LRCLK=18;
  PSRAM 0-3; ISA IOW=4, IOR=5, AD0..9=6..15; other ISA signals on
  19/21/26/27/28.
- Stage 4's worry that I2S would collide with ISA pins 26-28 was
  unfounded — different pins entirely.
- `picogus_diff_rp2040` binary: new `--out <path>` flag (default
  `crates/mdpicoem-harness/oracles/picogus_<trace_stem>.wav`) +
  `CapturingSink<S>` wrapper that ticks I2S after each inner step.
  Summary prints WAV path, file size, frame count, LRCLK edges,
  inferred sample rate.

### Critical mid-stage fix — cycle counter

Initial implementation had `I2sCapture.tick(pads)` increment a local
`cycle: u64` counter by 1 per call. With `step_quantum=1`, each
`Emulator::step()` advances one *instruction* (1–4 sysclks), not one
sysclk. Mock-sink tests masked this because their `step(n)` always
advanced exactly `n`. Devils-advocate caught it: inferred sample rate
would be multiplied by average-CPI (≈2×), so WAV header would be
stamped ~70–90 kHz instead of 44.1 kHz.

**Fix:** `tick` signature changed to `tick(pads, now_cycles: u64)`.
`CapturingSink::step` reworked to advance until `inner.cycles() ≥
target`, threading `inner.cycles()` into every tick. Dropped the
local `cycle` field entirely. Added:
- `capturing_sink_stamps_edges_in_sysclks_not_ticks` — synthetic 4-CPI
  mock sink, asserts inferred rate matches sysclk-based formula.
- `capturing_sink_cycle_matches_emulator_cycles` — real `Emulator`
  parked on `B .` (3-cycle loop), 8 steps, asserts observed cycle
  timestamps match `emu.cycles()` exactly (3, 6, …, 24).

### Tests

- 8 i2s_capture unit tests (decode round-trip, WAV format, empty
  WAV, sample rate inference, pad-mask scoping, default path,
  rejects-directory, creates-parent-dirs).
- 4 picogus_pins tests (disjointness + firmware-match assertions per
  PSRAM/ISA/I2S).
- 4 picogus_diff_rp2040 tests including the 2 new cycle-counter
  tests.
- `cargo test -p mdpicoem-harness --lib` — 217/217 pass.
- `cargo test -p mdpicoem-harness --bin picogus_diff_rp2040` — 21/21
  pass.

### Devils-advocate findings (non-blocking follow-ups)

1. **`CapturingSink` perf:** `inner.step(1)` in a loop at
   `step_quantum=1` for 10 seconds = ~1.25 billion cycle-iteration
   operations. Rough estimate 60–120 s wall-time for a full MIDI.
   Acceptable for offline; might need a fast-path for PIO-quiescent
   intervals if Stage 6 is painful.
2. **Frame buffer unbounded** (~10 MB per minute of audio).
   Acceptable for 10-s HLD snippets; cap + warn before long soaks.

Version bump: `mdpicoem-harness` 0.17.0 → 0.18.1 (concurrent agent
landed a second bump).

---

## Stage 6 — End-to-end MIDI demo

**Start/End:** 2026-04-14 (single session)
**Status:** plumbing complete; subjective ear-test acceptance blocked
on external work (documented, not blocking further stages)
**Agents:** impl-stage6

### Delivered this session

- **PicoGUS v4.0.0 firmware pinned + fetchable.** Queried the GitHub
  release API for `polpo/picogus@v4.0.0` (tag published 2026-04-06),
  captured the asset manifest + SHA256, downloaded
  `picogus-v4.0.0.zip` (812,278 bytes,
  sha256=27f34281b9a620ae12d9e704d810d198a12e3d655172cd0d8144b4382bb5b38a),
  extracted `picogus.uf2`, and converted to `picogus-v4.0.0.bin`
  (906,752 bytes, family 0xe48bff56, base 0x10000000, 3542 UF2
  blocks).
- **`third_party/picogus/VERSION`** — committed pin with upstream
  URL, SHA256, asset manifest, psram submodule SHA. Survives
  firmware fetch; the blobs are `.gitignore`d under a new rule block.
- **`third_party/picogus/README.md`** — how-to-stage (automated via
  `scripts/picogus_demo.sh --prepare` and manual), licensing note
  (GPL, not redistributed by default), and the crucial caveat that
  our synthetic 8-byte bootrom stub cannot boot real SDK firmware
  past boot2.
- **`third_party/picogus-demo-runbook.md`** — end-to-end walkthrough
  from zero to `output.wav`: DOSBox-X patch/build, firmware staging,
  trace capture, harness invocation, reference-WAV capture (DOSBox-X
  Ctrl+F6), comparison. Enumerates known failure modes and what
  Arthur needs to figure out externally (DOS MIDI player, gus.conf
  tuning, bootrom gap).
- **`scripts/picogus_demo.sh`** — bash driver with `set -euo
  pipefail`. Two modes: `--prepare` (idempotent fetch + verify +
  UF2-to-bin convert via inline Python) and default demo (prereq
  check + cargo-run the harness). Portable sha256 helper
  (sha256sum / shasum fallback). Exits 0 on success, 1 on user
  error, 2 on fatal internal error. Smoke-tested all three paths.
- **HLD Stage 6 acceptance clarifier** — added a paragraph under
  the existing "Acceptance" line documenting what Stage 6 delivers
  here vs what requires Arthur's external work (patched DOSBox-X,
  MIDI playback, bootrom).
- **`tech_debt.md`** — three new entries under "PicoGUS Integration
  — Stage 6 follow-ups": the bootrom gap (HIGH, blocks acceptance),
  the firmware-not-committed-by-design note, and the DOS MIDI
  player non-pin.
- **`.gitignore`** — added `third_party/picogus/*.{zip,uf2,bin,exe}`
  and `UPSTREAM_README.md` so the fetched blobs never sneak into a
  commit.

### Smoke test results

Both required smoke tests pass:

```
$ cargo run -p mdpicoem-harness --release --bin picogus_diff_rp2040 -- \
      --trace crates/mdpicoem-harness/fixtures/sample_gus.trace \
      --out /tmp/picogus_smoke.wav --post-roll 0.01
Loaded trace: 19 events, span 1500000 ns (0.002 s)
warning: emulator stalled at cycle 8209 ...
Events total: 19 | Writes fired: 16 | Reads skipped: 3
WAV: 44 bytes (no I2S output detected — expected, no firmware)
exit 0
```

Stretch smoke (firmware loaded, sample fixture as fake trace):

```
$ bash scripts/picogus_demo.sh --trace <sample> --out <wav> --post-roll 0.01
Loaded flash: 906752 bytes from ...picogus-v4.0.0.bin
warning: emulator stalled at cycle 8209 ...
exit 0
```

Firmware loads cleanly. The stall at cycle 8209 surfaces the
bootrom gap: M0+ lands on boot2's first instruction (`PUSH {LR}` at
0x10000000) but boot2 then attempts to call into the (non-existent)
SDK bootrom function table and wedges.

### Blockers / what Arthur needs next

1. **Real RP2040 bootrom.** Tracked in `tech_debt.md`; HIGH priority
   if Stage 6 acceptance is the goal. Recommended: vendor the open
   Raspberry Pi bootrom from
   <https://github.com/raspberrypi/pico-bootrom-rp2040>, add
   `--bootrom <path>` to `picogus_diff_rp2040`, wire it through
   `Emulator::load_bootrom`. 1–2 hours once started.
2. **Patched DOSBox-X build.** Stage 3 patch is in place; Arthur
   must apply + build. Runbook covers this.
3. **DOS MIDI player selection.** Not pinnable from our side
   (licensing). CLM.EXE is my guess; any GUS-aware player works.
4. **Capture the ISA trace** of
   `doom2_map01_running_from_evil.mid` (30 KB, verified present at
   `d:\language\mdsc55\tests\`) under the patched DOSBox-X, then
   run `scripts/picogus_demo.sh --trace <path>` and compare to the
   Ctrl+F6-captured reference WAV.

### Handoff state

Everything inside the Rust + docs boundary is done. The demo driver
is functional end-to-end against a sample trace, with the firmware
boot gap called out loudly everywhere a user might hit it (runbook,
README, tech_debt). No new crate dependencies, no new env vars, no
Rust code changes this stage. The Stage 6 delivery is cleanly
separable from the optional Phase 8 bootrom work: once Arthur or a
future session vendors the real bootrom and adds the `--bootrom`
flag, the demo script will run unchanged and the ear-test
acceptance becomes reachable.

---

## Final audit

**Verdict:** infrastructure complete, acceptance pending external
work. All six HLD stages conformant or with documented-and-justified
deviations. Every in-scope item delivered.

| Stage | Verdict |
|---|---|
| 1 — XIP flash | Conformant |
| 2 — SPI PSRAM | Deviates (0x38 QPI-enter omitted — firmware doesn't use it; documented) |
| 3 — DOSBox-X tap | Deviates (env var carve-out for third-party; documented) |
| 4 — Trace replayer | Conformant |
| 5 — I2S capture | Conformant |
| 6 — End-to-end demo | Infrastructure complete, ear-test external |

### Build + test snapshot (audit run)

- `cargo test -p mdrp2040`: **259/259**
- `cargo test -p mdpicoem-common`: **78/79** (1 pre-existing pacer flake, unrelated)
- `cargo test -p mdpicoem-harness --lib`: **217/217**
- `cargo test -p mdpicoem-harness --bin picogus_diff_rp2040`: **21/21**
- Smoke `picogus_diff_rp2040 --trace sample_gus.trace --out /tmp/smoke.wav --post-roll 0.01`: exit 0, 44-byte WAV

### External-dependency blockers for ear-test

1. **Real RP2040 bootrom** (HIGH, `tech_debt.md`). Our synthetic
   8-byte stub can't boot real Pico-SDK firmware past boot2. Vendor
   Raspberry Pi's open bootrom + add `--bootrom` flag. ~1-2 h.
2. Patched DOSBox-X build (external to us; runbook covers).
3. DOS MIDI player choice + `gus.conf` tuning (Arthur's call).

### Supervisor sign-off

Six stages implemented sequentially. Each stage: impl → code-reviewer
→ apply-feedback → devils-advocate → commit. Devils-advocate caught
real issues in Stages 2, 4, 5 that reviewers missed — validating the
extra pass. Journal kept in sync throughout. 13 commits total on
this branch (6 stages + intermediate fixes + concurrent work
entangled).

Cumulative new Rust LOC: ~3000 across `mdrp2040::peripherals::psram`,
`mdpicoem-harness::{i2s_capture, picogus_pins}`, and
`picogus_diff_rp2040`. Plus third-party patch + runbook + demo
script.

Work is complete to the scope the HLD declares in-scope. Handed off
to Arthur.


