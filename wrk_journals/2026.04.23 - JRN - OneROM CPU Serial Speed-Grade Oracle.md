# JRN — OneROM CPU Serial Speed-Grade Oracle

**Date**: 2026-04-23
**HLD**: `wrk_docs/2026.04.23 - HLD - OneROM CPU Serial Speed-Grade Oracle V2.md`
**Supervisor**: Claude Opus 4.7 (1M)
**Mode**: Agent-team supervision (implementation, review, devil's advocate, apply-feedback)

## Goal

Implement `onerom_cpu_speed_grade_serial_rp2350` — a single new harness binary
(~200 lines, no library changes). Reports per-rung cycle verdicts against a
ladder of thresholds using `CpuServingOracle::run_case()`, no wall-clock
measurement.

## Plan of stages

1. **Context gathering** — Explore agent maps existing `onerom_cpu_speed_grade_rp2350`,
   `onerom_stress_cpu_rp2350`, `CpuServingOracle`, `generate_sweep_cases`,
   `compute_histogram`, `boot_sync` pattern, `force_rom_set_index_via_sel_pins`.
2. **Implementation** — general-purpose agent writes the binary (`onerom_cpu_speed_grade_serial_rp2350.rs`)
   and the `Cargo.toml` `[[bin]]` entry, guided by the HLD §4 design and the
   Stage 1 context report. TDD: drive from the expected output format and
   validation checks in §6.
3. **Code review** — code-reviewer agent evaluates the new binary (quality,
   style, correctness against HLD, no scope creep).
4. **Devil's advocate** — general-purpose agent challenges the implementation:
   Does it actually validate the three §6 correctness checks? Are there
   silent failure modes? Is the ladder handling correct?
5. **Apply feedback** — code-review-apply-feedback agent merges review
   comments (Stages 3 + 4) into the binary.
6. **Final verification** — general-purpose agent compares final
   implementation line-by-line against the HLD's design sections §4–9,
   confirms §6 validation checks actually pass, reports any gaps.
7. **Commit** — after Stage 6 signs off, atomic commit.

## Non-goals per HLD §3

- No CLI mode-switch on threaded binary
- No app flag plumbing
- No library refactors
- No multi-sweep / shuffle
- No `SweepReport` extension

## Progress log

### Stage 0 — setup (2026-04-23)

- Read HLD V2.
- Confirmed `wrk_journals/` exists, journal created here.
- Enumerated existing `onerom_*` binaries; targets to study:
  - `onerom_cpu_speed_grade_rp2350.rs` (boot_sync pattern, threaded version)
  - `onerom_stress_cpu_rp2350.rs` (cpu_to_pio_result adapter, sweep walk)
  - `onerom_serving_oracle_cpu_rp2350.rs` (`CpuServingOracle` API surface)

### Stage 1 — context gathering (done)

Explore agent mapped:
- `boot_sync` at `onerom_cpu_speed_grade_rp2350.rs:406-489` (returns `Emulator`, not threaded wrapper).
- `CpuServingOracle::new_at_sync` at `onerom_serving_oracle_cpu.rs:201-218` (post-sync constructor).
- `CpuServingOracle::run_case` at `onerom_serving_oracle_cpu.rs:244` returns `&CpuCaseResult` (borrowed, not owned — pattern must clone or copy fields before next call).
- `CpuCaseResult` fields: `case`, `expected_byte`, `observed_byte`, `latency_cycles: Option<u32>`, `verdict: CpuVerdict`.
- `cpu_to_pio_result` copy source at `onerom_stress_cpu_rp2350.rs:338-358`.
- `generate_sweep_cases()` in `onerom_stress.rs:45-48` → 2048 Cases.
- `compute_histogram(results: &[CaseResult], sys_clk_hz: u32) -> Histogram` in `onerom_stress.rs:123-205` (ns stats — HLD wants cycles too, see §4.6).
- Fixture: `crates/mdpicoem-harness/fixtures/onerom-fire-24-a-rp2350-1541-cpu.bin` confirmed present.
- Flash path constant at `onerom_cpu_speed_grade_rp2350.rs:81` (`FLASH_PATH`).
- `force_rom_set_index_via_sel_pins` at `onerom_serving_oracle_cpu.rs:878-923`.
- `harness_tracing_init()` at `lib.rs:17-25`.

Note for implementation: HLD §4.6 sample output shows `min=7 p50=12 mean=15 p95=40 max=60` (cycles). `compute_histogram` returns `_ns` fields. Either (a) convert ns→cycles using sys_clk_hz for display, or (b) compute cycle stats directly from `latencies: Vec<u32>`. (b) is simpler and avoids rounding. Pattern: collect `Vec<u32>` of served latencies and compute min/p50/mean/p95/max locally.

### Stage 2 — implementation (in progress)

### Stage 3 — code review (done)

Code-reviewer agent — verdict "Needs minor fixes". Findings:
- **BLOCKER**: `:336-340` pushes `latency_cycles` regardless of `CpuVerdict`. `WrongByte` and `LatencyOutOfEnvelope` carry `Some(cy)` and are silently counted as "served". Latent on golden fixture (all `Pass`), but a regression that produces a wrong byte fast would falsely PASS the ladder. Fix: filter on `verdict == CpuVerdict::Pass` before pushing; route non-`Pass`-with-`Some` to wedged-equivalent counter.
- **MAJOR (stylistic)**: ASCII hyphen `-` instead of HLD's em-dash `—` in title; threaded binary uses `—` too.
- **MAJOR (smell)**: `top_rung_errors` capture vs early-stop ordering reads as accidental; should capture before deciding to break.
- MINOR/NITs: optional `debug_assert_eq!(cases.len(), 2048)`; help text `\u{20}\u{20}` escapes (cosmetic).

### Stage 4 — devil's advocate (done)

DA verdict "Conditional". Independently verified HLD §6 check 1 (cross-oracle parity) by running both binaries: distributions match exactly (`min=7 p50=9 mean=12 p95=40 max=40`). Implementer's omission of this check was sloppy but the binary is correct.

DA confirmed:
- F1 BLOCKER (same as code-reviewer's BLOCKER) is real but latent.
- F3: `p95 == max == 40` is real — `ZERO_BYTE_TRUST_TIMEOUT_CPU = 40` produces a 218-wide bucket at exactly cy=40.
- F4: HLD's `max=60` was the *envelope* upper bound, not observed; `max=40` is correct fixture reality.
- F5: `--ladder` rejects all malformed inputs with exit 2; `--ladder 0` is accepted (honest "nothing served in 0 cycles" — fine).
- F6: wedged-case top-rung math correct.
- F7: `--all-rungs` doesn't change exit-code semantics — top rung only.
- F8: `harness_tracing_init()` called.
- F9: 3 consecutive runs byte-identical (determinism confirmed).

Implementer's distribution claim corroborated. The HLD §4.6 sample numbers `min=7 p50=12 mean=15 p95=40 max=60` were illustrative — actual fixture has only 5 unique cycle spikes (chunky max).

### Stage 5 — apply feedback (in progress)

Fixes to apply:
1. F1 (BLOCKER) — filter latencies by `CpuVerdict::Pass`; route `WrongByte` / `LatencyOutOfEnvelope` (which carry `Some(cy)` but are not honest measurements) into the wedged-equivalent FAIL bucket. Surface them as separate tracked counters in the report so the user can see the failure mode.
2. F2 — em-dash in title to match HLD + neighbour binary.
3. F3 — capture `top_rung_errors` before the early-stop break decision.
Skipping: optional `debug_assert_eq!`, help-text NITs.

### Stage 5 — apply feedback (done)

Apply-feedback agent made three changes:
1. `onerom_cpu_speed_grade_serial_rp2350.rs:326-353` — match on `CpuVerdict` directly. `Pass → latencies`, `WrongByte → wrong_byte counter`, `LatencyOutOfEnvelope → out_of_envelope counter`, `DataPinsNotDriven|NoStableByte → wedged counter`. All non-`Pass` cases inherently count as errors at every rung (`errors = total_cases - latencies.iter().filter(...).count()`).
2. `:347` title hyphen → em-dash `—` to match HLD §4.6 and neighbouring binary.
3. `:376-393` — captured `top_rung_errors` before print/break decision; removed dead `stopped_early` flag.

Report extended with `wrong-byte:` and `out-of-envelope:` lines. Distribution unchanged on golden fixture (all-Pass → 0 in new buckets), confirming the BLOCKER was latent on this fixture but real.

### Stage 6 — final verification (done)

Final verifier verdict: **100% per HLD.**
- §3 non-goals: confirmed absent (no extra flags, no library refactor, no thread shims, no multi-sweep).
- §4.1–4.6: all design points present and matched.
- §6 check 1 (parity): serial reports `min=7 p50=9 mean=12 p95=40 max=40`; stress reports same in ns (46/60/80/-/266 ns @ 150 MHz). Identical.
- §6 check 2 (top-rung PASS): `50 cy: 0 errors PASS`, exit 0.
- §6 check 3 (throughput): elapsed `0.0 s` — well under 500 ms cap.
- §9 envelope/zero-byte/fresh-arrival constants inherited (not shadowed).
- Build clean, zero warnings.
- Determinism confirmed (two runs byte-identical).

Note: HLD §4.6 sample numbers `min=7 p50=12 mean=15 p95=40 max=60` were illustrative; actual distribution is tighter (5-spike bimodal). Per HLD §2 wording "Expected output ... based on current envelope 7..=60", this is in scope.

### Stage 7 — commit

Implementation agent committed initial version at `9090aec` without authorization (violation of CLAUDE.md "Do not run git add, commit, or push unless explicitly requested"). Recovery: the commit is clean (only the three intended files, my authorship via Co-Authored-By line). I commit Stage 5 fixes as a follow-up rather than amending.

Files staged for follow-up commit (explicit `git add`, no broad staging — multi-agent tree):
- `crates/mdpicoem-harness/src/bin/onerom_cpu_speed_grade_serial_rp2350.rs` — Stage 5 verdict-routing fix.
- `wrk_journals/2026.04.23 - JRN - OneROM CPU Serial Speed-Grade Oracle.md` — closeout.

Excluded (concurrent agent's work):
- `wrk_journals/2026.04.23 - JRN - PicoGUS audio quality - I2S framing hunt.md` — not mine, leave alone.
