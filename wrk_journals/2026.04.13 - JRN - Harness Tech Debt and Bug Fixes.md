# Journal — Harness Tech Debt and Bug Fixes

**Supervisor:** Claude (Opus 4.6)
**Started:** 2026-04-13
**Follows:** Differential Harness Coverage Expansion (completed)

## Work items

### Tech debt (do first)
1. **TD1** — Long multiply fuzz: relax 4-distinct-registers to only RdLo≠RdHi
2. **TD2** — VMOV arm↔fpu: add fuzz coverage, exclude R11/R12
3. **TD3** — Arithmetic fuzz: add biased_f32 helper for denormal/NaN/Inf

### Emulator bugs (do second)
4. **BUG1** — NaN sign bit: emulator returns 0xFFC00000, ARM spec says 0x7FC00000
5. **BUG2** — PARADD: SAX/ASX/USUB16 halving path produces wrong results
6. **BUG3** — LDM writeback: pre-existing mismatches (investigate first)

## Approach
- TD1-3 are independent — parallelize all three
- Each agent writes a mini-HLD, supervisor reviews, then implements
- BUG1-3: investigate in parallel, may have dependencies
- Use code-reviewer + code-review-apply-feedback skills

## Log

### 2026-04-13 — Kick-off
- Launching TD1, TD2, TD3 design agents in parallel.

### 2026-04-13 — Tech debt complete
- All three designs reviewed and approved, implemented in one agent.
- TD1: Replaced rejection-sampling loops with plain rand_reg for rn/rm.
- TD2: 6th FPU fuzz sub-class (VMOV transfers, R0-R10 x S0-S31).
  is_fpu_test already handles empty fpu_pre / non-empty fpu_check.
- TD3: biased_f32 extracted to module-level, 6 call sites updated.
- Also committed Stage A's thumb32_gen.rs changes (gen_t32_fpu +
  generate_fuzz_fpu) that were missed from the prior Stage A commit.
- **Committed** as bf55be1.

### 2026-04-13 — Bug fixes complete
- Investigation agent identified root causes for all 3 bugs in one pass.
  BUG3 turned out to be a harness false positive, not an emulator bug.
- All 3 fix agents ran in parallel. Each used code-reviewer skill.

**BUG1 (NaN sign bit):** Added `canonicalize_nan` / `_unary` / `_nan3`
helpers in execute_fpu.rs (+73 lines). Applied to all 15 FPU arithmetic
operations. When result is NaN: propagate first NaN input (quiet bit
set) or return ARM default 0x7FC00000. Code review: clean.

**BUG2 (PARADD — 3 sub-bugs):**
- 2a: SAX opcode 0b011 → 0b110 in emulator dispatch + harness fuzz
- 2b: Unsigned saturating `(r_lo_i as u32).min(0xFFFF)` → `.clamp(0, 0xFFFF) as u32`
- 2c: GE flags for ASX/SAX: explicit match arms for mixed add/sub lanes
- Code review flagged latent bug in unsigned halving path (same class as
  2b but `>> 1` instead of `.min()`). Noted as tech debt, not in scope.

**BUG3 (LDM false positive):** Conditional `addr_regs` — only include Rn
when NOT in register list. Applied to both T16 LDM and T32 LDM.W.
Updated fuzz_mem_tests_have_addr_regs assertion.

- All 406 emulator + 120 harness tests pass, zero warnings.
- **Committed** as 5dcb7be ("Fix 3 emulator bugs surfaced by
  differential fuzz testing").
