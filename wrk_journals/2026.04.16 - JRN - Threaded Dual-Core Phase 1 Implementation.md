# Threaded Dual-Core Phase 1 Implementation — Journal

Date started: 2026-04-16
Worktree: `.claude/worktrees/threading-phase0`
Branch: `worktree-threading-phase0`
Parent LLD: `wrk_docs/2026.04.16 - LLD - Threaded Dual-Core Phase 1 V3.md`

## Scope

Three standalone types in `crates/mdrp2350/src/threaded/`:
1. SharedMemory — AtomicU32 SRAM + immutable ROM/XIP
2. AtomicGpio — atomic pin out/oe state
3. ExclusiveMonitors — per-core monitors with cross-thread snoop

## Implementation order

1. SharedMemory (bulk of work — alias handling, CAS loops, ROM/XIP)
2. AtomicGpio (small — SET/CLR/XOR mirroring SIO layout)
3. ExclusiveMonitors (small — set/clear/check/snoop/any_valid)
4. Module wiring (mod.rs + lib.rs declaration)
5. Code review + devil's advocate
6. LLD parity check

## Log

### 2026-04-16 — Start

- Phase 0 complete (commits a16b572..6335248)
- Phase 1 LLD V3 reviewed and committed (3fe726b, 97c4483)
- Dispatching implementation agents

### 2026-04-16 — Implementation complete (8c15c19)

All three types implemented in parallel by three agents:
- SharedMemory: 15 tests, SRAM alias handling, CAS loops, ROM/XIP read
- AtomicGpio: 3 tests, SET/CLR/XOR for both OUT and OE
- ExclusiveMonitors: 7 tests, word-aligned encoding, any_valid fast path

936 tests total, all passing. No regressions.

### 2026-04-16 — Review feedback applied (20908f5)

**Code reviewer** found:
- Missing pub use re-exports in mod.rs (REQUIRED, fixed)
- Missing Default impls (SUGGESTED, fixed)
- sram_idx word-granular docs (SUGGESTED, fixed)
- ROM_BASE=0 assumption docs (SUGGESTED, fixed)
- ROM/XIP read8/read16 test gaps (SUGGESTED, fixed: 4 new tests)

**Devil's advocate** found:
- AtomicGpio bank bounds (HIGH — added debug_asserts to all 10 methods)
- Missing mod.rs re-exports (MEDIUM — already caught by reviewer)
- Verified: write16 alias logic correct (traced XOR example step-by-step)
- Verified: read16 uses addr & !3 (correct per LLD)
- Verified: ROM boundary read works correctly
- Verified: ExclusiveMonitors encoding symmetric across byte offsets
- Verified: strong compare_exchange used (not weak)

940 tests total after review feedback.

### 2026-04-16 — LLD parity check complete

31/31 LLD requirements verified PASS with file:line evidence.
29 threaded tests + 911 existing = 940 total.

### Branch summary

Commits on `worktree-threading-phase0` for Phase 1:
```
20908f5 threading Phase 1: review feedback — re-exports, defaults, asserts
8c15c19 threading Phase 1: SharedMemory, AtomicGpio, ExclusiveMonitors
97c4483 docs: Phase 1 LLD V3 — add Phase 3 integration checklist
3fe726b docs: Phase 1 LLD — SharedMemory, AtomicGpio, ExclusiveMonitors
```

Phase 1 is complete. Types are standalone and ready for Phase 3
integration (WorkerBus/SharedState). Next: Phase 2 (SpinBarrier +
ThreadedSio + ThreadedPio + Loom tests).
