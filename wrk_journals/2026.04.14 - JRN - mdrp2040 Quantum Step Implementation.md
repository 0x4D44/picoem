# mdrp2040 Quantum Step — Supervisory Journal

Date: 2026-04-14
Supervisor: main conversation (Opus 4.6)
HLD: `wrk_docs/2026.04.14 - HLD - mdrp2040 Quantum Step.md` (v1.1.0)

## Context

Following creation of `paced_bench_rp2040`, measured a 3.1× throughput
gap (78.8 → 247 MHz unpaced) caused by per-instruction peripheral
ticks in `mdrp2040::Emulator::step()`. Root cause traced to the
unimplemented `step_quantum` field (tech_debt.md:137-148). Arthur
authorised the fix; this supervises an agent team to implement it.

## Plan (stages)

1. **Stage 0:** HLD updates (4 self-review corrections). *Self, not
   delegated.*
2. **Stage 1:** Implementation. One agent, TDD-style: update tests to
   use `step_quantum(1)` first (still pass under current behavior),
   then restructure `step()`, confirm all tests pass.
3. **Stage 2:** Code review (code-reviewer skill).
4. **Stage 3:** Devil's advocate review (adversarial agent challenging
   both the implementation and the code-reviewer's findings).
5. **Stage 4:** Apply combined review feedback
   (code-review-apply-feedback skill).
6. **Stage 5:** Final HLD-vs-implementation verification. Iterate if
   gaps found.
7. **Stage 6:** Performance measurement via `paced_bench_rp2040`.

Commit + journal update after each stage.

## Stage 0 — HLD corrections applied

Bumped HLD to v1.1.0. Four corrections:

1. Removed wrong claim about `qemu_diff_m0plus` constructing an
   `Emulator` with quantum 1 — it bypasses `Emulator` entirely.
2. Removed incorrect "core 1 runs sooner" note from Risks — current
   `step()` already wakes-then-steps within a single call.
3. Expanded Test impact section: three exception-entry tests in
   `tests.rs` need `step_quantum(1)`, not just PIO tests. Added note
   that `pio_tests.rs:382` has a misleading docstring post-change.
4. Added pacer-granularity note for `mdrp2040app/src/sim.rs`:
   effective granularity becomes 64 cycles (bounded overshoot); not
   observable at the 500 ms MHz-update cadence.

Status: **complete**.
