# Oracle Fix Team — 2026-04-21

**Lead**: Claude (Opus 4.7) as team lead.
**Background**: OneROM regression fix lineup (`611b176`, `7b19fc4`, `0ed4060`) landed on `main` earlier today. Post-merge audit found no other code regressions from the reverse-merge, but surfaced four latent/pre-existing issues worth fixing now that the OneROM work is closed out.

## Scope (4 items, 3 tracks)

| Track | Issue | Est. effort | Agent |
|---|---|---|---|
| **A** | probe-rs 0.31 `ApV2Address` panic breaking periph/dualcore/isr/bank oracles + `probe_diff_rp2040` ARM attach error | 1–4h | TBD |
| **B** | Cycle oracle 4 pre-existing FAILs (`nop_chain_8` −3, `push_2_min_cost` −2, `bank_contention_fetch_data_same/diff` −1 each) | 4–8h | TBD |
| **C** | `pacer::tests::test_pacer_behind_detection` flaky in release mode (burn loop optimized away) | 30 min | TBD |

## Process

1. Plan agents write HLDs in `wrk_docs/`.
2. Lead reviews each HLD; agents iterate until lead signs off.
3. Implementation agents work in isolated worktrees, use `/code-reviewer` skill, then `/code-review-apply-feedback`.
4. Lead consolidates, verifies on hardware where possible, merges.
5. Journal updated at each phase transition.

## Timeline

### 2026-04-21 Setup

- Team structure defined.
- Plan agents dispatched in parallel for Tracks A, B, C.
- Journal initialized (this file).

### 2026-04-21 Track C HLD — signed off

- Plan agent returned. Root cause correctly identified: LLVM folds `Σ i for i in 0..10_000` to closed-form arithmetic series at release, collapsing the burn loop. `black_box(dummy)` outside the loop pins only the final value.
- Good find: latent twin `test_pacer_begin_end_quantum` has same bug, masked by weaker assertion (`emulation_ns > 0`, satisfied by `rdtscp` pair alone).
- Chosen fix: move `black_box` **inside** the loop body for both tests. Option (a). ~30-min fix.
- HLD saved to `wrk_docs/2026.04.21 - HLD - Track C Pacer Test Fix.md`.
- Implementation agent dispatched in isolated worktree. Agent told to use `/code-reviewer` + `/code-review-apply-feedback`, run 20× release rerun for flake verification, patch-bump `mdpicoem-common` to 0.13.2, commit but **not merge** (lead consolidates).

### 2026-04-21 Track C — landed on main

- Implementation agent returned. Commit `28728f5` in agent worktree.
- 20/20 release reruns of `test_pacer_behind_detection` — no flake.
- `cargo test --release -p mdpicoem-common` full suite: 113/113 PASS (up from 112/113). Debug mode also 113/113.
- Code-reviewer returned 2 optional suggestions; both declined per minimal-change philosophy. No blocker feedback.
- Cherry-picked onto main as `b1a428d` (main had moved forward to `73cfc41` under another agent; cherry-pick clean).
- Note: agent worktree was based on `ede95a1` (pre-OneROM merge); the HLD file wasn't visible to it but the prompt summary was sufficient. No functional impact — worth a guardrail in future: explicit instruction to `git fetch` and branch from the CURRENT main tip in the worktree.

### 2026-04-21 — concurrent-agent note

- While Track C was in flight, another agent landed `73cfc41` (paced_bench default step-quantum 256) and has an uncommitted edit to `crates/mdpicoem-harness/src/bin/paced_bench_rp2350.rs` pending in the main clone. Not touching that file.

### 2026-04-21 → 04-22 — Tracks A & B HLDs signed off

- **Track B HLD**: Option B (per-case tolerance widening) chosen. Root causes correctly attributed to three structural M33 pipeline effects (dual-issue NOP fold, store-to-load write-buffer forwarding, LDR→use forwarding). Prior 2026.04.16 HLD §9 already recommended this approach for the `bank_contention_*` cases; this HLD formalises and extends to `nop_chain_8` + `push_2_min_cost`. Zero emulator changes; blast radius = 2 files + tech_debt.md. Lead addendum: output format changed to `PASS (known Δ=<delta>, tol=<N>)` — shows both actual delta and budget at a glance.
- **Track A HLD**: Option C (vendored probe-rs 0.31 fork + `[patch.crates-io]`) chosen. Root cause precisely located at `communication_interface.rs:474` `unreachable!()`, triggered by `(ApAddress::V2, SelectCache::DPv1)` cross-product when initial DPIDR read returns non-DPv3 version. Confirmed present in master + two open upstream issues (#3872, #3257) with no fix merged. Option B (chip string swap) correctly rejected after registry-normalisation read. RP2040 "ARM specific error" correctly scoped out — V1-only APs can't hit this panic; spin up Track A.1 separately.
- Two implementation agents dispatched in parallel worktrees: `aee3266e66a28dabb` (Track A) + `ad87dce15590f1058` (Track B). Both instructed to `rebase origin/main` first so they see the Track C pacer fix (`b1a428d`).
- HW validation (probes attached to Windows host) is lead's responsibility post-merge, not the agents'.

### 2026-04-22 — Track A restart (lead error)

- Track A agent `aee3266e66a28dabb` stopped correctly per its guardrails: HLDs were on my filesystem but not committed, so its worktree (which sees only committed state) couldn't find them. The agent also noticed concurrent commits `69854c7` and `990f1a7` bumping `mdpicoem-harness` from 0.35.1→0.35.2 and almost misread those as a probe-rs bump — good instinct, correctly flagged for alignment.
- Fix: committed all three HLDs + this journal to main as `e940638`. probe-rs itself stays at 0.31.0 (verified via `Cargo.lock` and `crates/mdpicoem-harness/Cargo.toml`).
- Re-dispatched Track A as agent `a059c4a53885e86f1` with an explicit note clarifying the 0.35.2 bump is for our own harness crate, not probe-rs.
- Lesson learned: commit HLDs immediately after sign-off, before dispatching any implementation agent. Added to process for future waves.

### 2026-04-22 — Track B landed on main

- Implementation agent `ad87dce15590f1058` returned with `6fb6c95`. Worktree reset to `990f1a7` (local main tip pre-HLD commit) to pull in recent harness bumps; HLD was committed inside their branch too. When I cherry-picked, git saw the HLD already on main (via `e940638`) and handled the no-op cleanly.
- Cherry-picked as `ebf4f2e`. 5 files changed, 261 insertions, 117 deletions.
- Test results post-merge on main: `cargo test --release -p mdpicoem-harness --lib` → 377/377 (incl. new `test_per_case_tolerance_overrides_cli_tolerance`). Release build of `silicon_cycle_oracle_rp2350` clean. No emulator changes.
- HW validation of Track B: deferred until Track A lands (probe attach is the blocker for running `silicon_cycle_oracle_rp2350` on silicon right now; the binary can't attach cleanly via probe-rs 0.31 until Track A patches it). This is acceptable — Track B's correctness is verified by unit tests (the tolerance logic) and the emulator-side cycle values are unchanged from what's already on silicon today.
- Code-reviewer: 1 optional doc comment applied (note `CycleCaseResult::to_outcome` dead-code gap vs `run_against`), 2 nits declined — good judgment.

### 2026-04-22 — Track A landed on main

- Implementation agent `a059c4a53885e86f1` returned 4 commits: `f418a6c` (vendored probe-rs 0.31.0, 242k insertions, 457 files, licence files included), `ffb0126` (23-line surgical patch to `select_ap_and_ap_bank` adding the (V2, DPv1) arm with in-place DPv3 cache upgrade + sentinel WARN), `8d18fab` ([patch.crates-io] wire-up), `bd902bd` (RUNBOOK / tech_debt / upstream PR draft).
- Cherry-picked to main as `d31f90b` … `9dc122b`.
- Code-reviewer approved; 3 non-blocking polish suggestions declined per scope discipline.
- `cargo tree -p mdpicoem-harness | grep probe-rs` confirms the patched path source is active: `probe-rs v0.31.0 (D:\language\mdrp2354\third_party\probe-rs-0.31.0-mdrp-patched)`.

#### Hardware validation

- **Track A: partial success.** The `unreachable!()` panic is gone; sentinel WARN fires exactly once per session as designed. `probe_diff_rp2350 --fuzz 10`: 400/400 PASS (no regression on ISA-level path). But **`silicon_periph_diff_rp2350` still doesn't attach** — patch eliminated the panic but exposed a deeper probe-rs / DP-state issue. `probe_verify_rp2350` returns `Arm(WrongApType)` after the sentinel fires; `silicon_periph_diff_rp2350` returns "An ARM specific error occurred." This matches HLD §8.1's explicitly-named risk: "the cache upgrade masks a deeper DPIDR-read bug." The patch is doing its job; the bug was deeper than just the one `unreachable!()`.
- **Track B: full success via test_silicon orchestrator.** Cycle oracle now reports 9/9 PASS with the new per-case tolerance annotation rendered crisply: `PASS elapsed=23ms known Δ=-3 tol=3` etc. The 4 previously-FAIL cases now show their documented known deltas inline.
- **Track C: unchanged.** Pacer test already passing 113/113 (including the 20× flake verification that landed earlier today).

#### Net outcome

- `test_silicon` cycle oracle: 9/9 PASS (was 5/9 at session start — +4 via Track B's tolerance format).
- `test_silicon` periph/dualcore/isr/bank: still failing, but now with soft ARM errors instead of panic crashes — strictly better failure mode; orchestrator's reattach cascade no longer aborts the whole session.
- `probe_diff_rp2350 --fuzz 4000` (ISA oracle): unchanged at 100% (Track A did not touch the path this uses).
- `probe_diff_rp2040`: still fails (explicitly out of scope — Track A.1 follow-on recommended in HLD §5 and §11 Q2).
- All unit tests pass: mdpicoem-common 113/113, mdrp2350 1389/1389, mdrp2040 514/514, mdpicoem-harness 377/377.

#### Follow-ons identified (not in scope this wave)

1. **Track A.2 — periph/dualcore/isr ARM-error downstream of cache upgrade**: the sentinel firing once per session means the HLD's pessimistic path IS being taken, and `WrongApType` downstream suggests probe-rs's AP-probing logic doesn't recover cleanly after our forced DPv3 upgrade. Needs investigation — possibly: (a) the real RP2354 DP is returning DPv1 genuinely (multidrop effect?), or (b) probe-rs needs a second workaround in the AP discovery path. A Track A.1 HLD should open this.
2. **Track A.1 — probe_diff_rp2040 ARM error**: separate from A.2, RP2040-specific, V1-only AP path. Scoped out of Track A by design.
3. **Upstream PR submission**: `wrk_docs/2026.04.21 - DRAFT - probe-rs PR body.md` is ready for review and submission against probe-rs/probe-rs#3872.

### 2026-04-22 — Team wave close-out

- All three tracks landed on main: Track C (b1a428d), Track B (ebf4f2e), Track A (d31f90b..9dc122b).
- Net main state: `9dc122b`.
- Tasks #5–10 all `completed`. #11 (journal) this close-out.
- Two follow-on HLDs identified (A.1, A.2); not opened this wave.

