# mdpicoem Workspace Restructure — Journal

Date started: 2026-04-14
Supervisor: Claude (Opus 4.6, 1M context)
Blueprint: `wrk_docs/2026.04.14 - HLD - mdpicoem Workspace Restructure.md` (v1.1.0)

## Mission

Restructure the RP2354 emulator workspace to support both RP2350 and
RP2040 as independent library crates. Full plan in the HLD. This journal
tracks supervisor decisions, agent dispatches, quality gates, and
findings.

## Roles

- **Supervisor (me):** plan, dispatch agents, verify results, commit
  between phases, maintain this journal. Does not write implementation
  code or run long debug loops myself.
- **Implementer:** general-purpose agent writing code for a phase.
- **Reviewer:** agent using `code-reviewer` skill on implementer's output.
- **Apply-feedback:** agent using `code-review-apply-feedback` skill to
  land reviewer findings.
- **Devil's advocate:** agent challenging the result — looks for
  shortcuts, unverified assumptions, missed edge cases, oversimplifications.

Not every phase needs the full roster. Trivial phases (Phase 0, 3) may
skip review cycles. Risky phases (Phase 4 M0+ CPU, Phase 5 RP2040 bus)
get the full treatment.

## Quality gates per phase

1. `cargo build --workspace` clean (no warnings).
2. `cargo test --workspace` green.
3. Phase-specific runtime check (e.g., `mdrp2350app` opens post-rename;
   `paced_bench` within 2% of baseline post-extract).
4. Reviewer findings triaged and either addressed or deliberately
   deferred with rationale in this journal.
5. Devil's advocate has no open objections, or objections captured and
   resolved.
6. Atomic commit with descriptive message.

## Protected-context discipline

- I don't read raw agent outputs into my context unless necessary.
- Agents report in bounded summaries (<500 words typically).
- Long build/test outputs stay inside the agent's context.
- Large file reads stay inside the agent's context; I skim or ask
  pointed questions.
- Journal is my persistent state — when I need to recall a decision, I
  check the journal, not scrollback.

## Phase log

### Phase 0 — capture paced_bench baseline
Status: completed

**Baseline: 32.9 MHz emulated** (median of 3 x 15s runs, variance 0.6%).
Host/emu ratio: 93.35 (already over the HLD §12 <33 target on master;
existing state, not a regression concern).

Command: `paced_bench --unpaced --seconds 15` (default `basic` workload:
2-insn tight loop `ADDS R0,R0,#1 ; B .-2` at `0x2000_0000`).

**HLD correction needed:** HLD says baseline runs against
`roms/benchmark.bin`. Wrong — paced_bench has a hardcoded workload, not
a firmware input. The benchmark.bin file belongs to mdrp2354-app. The
HLD's Phase 0 and "Validation gate" paragraphs will be corrected inline
during Phase 1's doc sweep.

**Phase 2 gate: post-extract paced_bench within 2% of 32.9 MHz**
(i.e. >= 32.24 MHz).

### Phase 1 — rename crates + doc sweep
Status: completed

**Implementer**: 64 files moved (35 pure renames + 29 renames-with-content),
4 deletes (stub crates), 9 in-place modifications. 840 tests pass, clean
release build, `mdrp2350app blinky` TUI launches successfully.

**Reviewer (code-reviewer skill)**: PASS. Zero stale `mdrp2354` code
references outside legitimate categories (physical-chip / firmware-encoded /
historical-doc). Cargo.lock clean, no backwards-compat shims, no feature
flags added. Flagged pre-commit action: stage `roms/rp2040/.gitkeep`.

**Devil's advocate**: caught one real regression + 2 minor inconsistencies.
Also ran `paced_bench` post-rename: **32.8 MHz median** (0.3% below the
32.9 MHz baseline — well within the 2% gate).

**Findings addressed (apply-feedback)**:
1. `rust-version` lift was non-functional — `[workspace.package]` had the
   field but no crate had `rust-version.workspace = true`, so the 1.88
   MSRV silently dropped. Added the inheritance opt-in to all 4 renamed
   crates. `cargo metadata` confirms all now report `rust_version: 1.88`.
2. `src/main.rs:6` banner: "RP2354 emulator initialised" → "RP2350
   emulator initialised" (crate-role context, not silicon).
3. `crates/mdrp2350/src/lib.rs:62` doc comment: same fix.

**Decisions logged**:
- TUI window title changed from `"mdrp2354 showcase"` → `"mdrp2350app showcase"`.
  Developer-facing TUI, not user branding; crate-name reading is appropriate.
  Both reviewers accepted; devil's advocate flagged it as a judgement call
  worth documenting — done here.
- The root `src/main.rs` is outside the workspace (virtual workspace, no
  root `[package]`). It's the "sanity-check stub" per CLAUDE.md. Banner
  fix still applied for consistency, but the file is effectively orphaned
  and won't be built by `cargo build --workspace`. Pre-existing state,
  not a Phase 1 concern. Candidate for later cleanup (could move into
  `mdrp2350app` or delete entirely).

**Perf**: 32.8 MHz (from 32.9 MHz baseline, 0.3% delta). Rename did not
regress inlining or codegen.

**Post-phase state**: `cargo build --workspace` clean, `cargo test
--workspace` 840 pass / 7 ignore (unchanged from pre-rename).

### Phase 2 — extract mdpicoem-common
Status: completed

**Implementer**: 6 sub-steps (2.0 setup, 2.1 Peripheral, 2.2 clock+pacer,
2.3 Memory, 2.4 PLL math, 2.5 Fifo+Divider, 2.6 pio, 2.7 version+gate).
All green at each step. 840 tests pass, mdrp2350 → 0.3.0.

**Structural decisions**:
- `bank_for_address` → free function in `crates/mdrp2350/src/memory.rs`;
  `Memory` struct in common has no `bank_for_address` method. Chip code
  uses `crate::memory::bank_for_address(addr)`.
- Spinlock helpers (`spinlock_read`/`spinlock_write` in `sio/mod.rs`)
  **not** extracted. Reviewer flagged this against the HLD's top-level
  "What moves where" row; I re-read the HLD's Phase 2 sub-step 2.5
  which explicitly says "err on the side of NOT extracting" when the
  helpers decode register offsets rather than being pure bitmap ops.
  The current methods decode SIO register offsets before touching the
  bitmap — decision stands. RP2040 may need its own copy; that's fine.
- PIO test split: 60 `#[test]` in Phase 1 pio/mod.rs → 51 primitive
  tests in common + 9 chip-through-Bus tests in new
  `mdrp2350/src/pio_tests.rs`. Zero tests lost.
- `StopReason` enum stayed in chip crate (not referenced in code outside
  its declaration; HLD didn't explicitly require moving it).

**Reviewer (code-reviewer)**: PASS-with-notes. Flagged `StateMachine`
over-promotion. Verified 60/60 PIO test preservation. `#[inline(always)]`
counts identical pre/post (5 in pacer; none in memory — HLD's inline
preservation mandate was moot for Memory since they weren't annotated
in Phase 1 either).

**Devil's advocate**: ran paced_bench across both worktrees. Post-Phase-2
**40.9 MHz median** vs Phase 1 worktree **19.2 MHz** on the same host
(different from the supervisor's Phase 1 number of 32.8 MHz — host
was under different load). The 2× gap is real but the A/B is
contaminated by host load/thermal/cold-cache effects. Caveat: perf
numbers are host-sensitive. The >=32.24 gate passed on the implementer's
host at 42.2 MHz.

**Findings addressed (apply-feedback)**:
1. **`StateMachine` visibility**: reverted all fields + `StallKind` to
   `pub(crate)` in common. Only `PioBlock::sm` stays `pub` (its inner
   fields are `pub(crate)`, so invariants are enforced by module). Added
   `StateMachine::enabled()` accessor — the only field actually read
   cross-crate (6 sites in `pio_tests.rs`). Added module-level doc
   comment on `sm.rs` listing the invariants. `PioBlock::instr_mem` and
   `irq_flags` reverted to `pub(crate)` — no cross-crate users found.
2. **`.claude/` gitignored**: `/.claude/` added to root `.gitignore`.
   `git status` confirms no longer untracked.

**Surprise**: the blast radius of the over-promotion was just 6 call
sites reading `.enabled`, not the 21 fields the implementer originally
promoted. The full `pub` surface was defending against a need that
didn't exist.

**Post-phase state**: `cargo build --workspace` clean; `cargo test
--workspace` 840/7; paced_bench gate PASS at implementer's 42.2 MHz.

### Phase 3 — stub mdrp2040
Status: completed

**Implementer**: Created `crates/mdrp2040` (388 new lines: Cargo.toml,
lib.rs, core/mod.rs, bus/mod.rs, tests/smoke.rs). RP2040 memory sizes:
ROM 16 KB, SRAM 264 KB, no onboard flash. All non-trivial methods are
`todo!()` with phase-scoped messages. 5 smoke tests pass.

**Phase 2 gap uncovered**: `mdpicoem_common::Memory::new()` was left
no-arg with hardcoded RP2350 sizes — HLD had specified a sized
constructor. Implementer added `Memory::with_sizes(rom, sram)`
additively; `mdpicoem-common` bumped 0.1.0 → 0.2.0. `mdrp2350` is
untouched (still calls `Memory::new()`). Acceptable minimal fix;
future cleanup could migrate `mdrp2350` to `with_sizes` and retire
the no-arg variant, but not urgent.

**Reviewer + DA (combined pass)**: PASS-with-notes. Three fixes
applied:
1. `Emulator::load_flash` now delegates to `Bus::load_flash` (which
   was already implemented). Was `todo!()` — caused inconsistency with
   working Bus path.
2. `CortexM0Plus` stub gained `current_instr_addr`, `pending_fault`,
   and a minimal `Fault::HardFault` enum (pub(crate),
   `#[allow(dead_code)]`). Architecturally required for Phase 4;
   adding now avoids struct-layout churn.
3. Reset doc comment tightened: "word 0 / word 1" → "offset 0 / offset 4"
   (byte offsets match what `rom_read32` receives).

**Post-phase state**: 845 tests pass (840 baseline + 5 smoke), clean
release build. `mdrp2040::Emulator` has full API shape; Phase 4 fills
in CPU, Phase 5 fills in bus/peripherals.

### Phase 4 — M0+ core

#### Phase 4.A: Registers + decoder + Thumb-16 executor
Status: completed

**Implementer**: ~2400 lines of new M0+ code. All 7 checkpoints cleared
in one agent call.
- `crates/mdrp2040/src/core/registers.rs` (235 lines) — ARMv6-M register
  set (no banked regs, no FAULTMASK, no BASEPRI; 2-bit CONTROL).
- `crates/mdrp2040/src/core/decode.rs` (111 lines) — group dispatch +
  `is_wide` (only accepts `0b11110` prefix; other wide forms fault).
- `crates/mdrp2040/src/core/execute.rs` (844 lines) — every M0+
  Thumb-16 executor. IT/CBZ/CBNZ rejected as UNDEFINED. No wide-path.
- `crates/mdrp2040/src/core/mod.rs` rewritten (167 lines).
- `crates/mdrp2040/src/tests.rs` (1151 lines) — 105 new unit tests.
- `crates/mdrp2040/src/bus/mod.rs` (+38 lines) — 6 no-timing
  read/write delegate methods to `Memory::peek*/poke*`. Scope creep
  noted; Phase 5 replaces with real fabric.

Not yet: Thumb-32 subset (BL/MRS/MSR/DSB/DMB/ISB), exception
entry/return, BKPT/SVC handler bodies, unaligned fault, Emulator::step
integration — all Phase 4.B.

**Reviewer (code-reviewer skill)**: PASS-with-notes. Critical finding:
**CPS bit-swap bug** in `thumb16_misc` at the CPS encoding — code
checked `opcode & (1 << 0)` for `affect_i`, but ARMv6-M A6.7.38 says
bit 1 = I, bit 0 = F. Canonical encodings `CPSIE i` = 0xB662, `CPSID i`
= 0xB672 were silently no-op on PRIMASK. Test `cpsie_clears_primask`
was using 0xB661 (CPSIE f) — passed for the wrong reason. Same bug
exists in mdrp2350 (code inherited from it); logged as tech debt, not
fixed in 4.A scope.

**Devil's advocate**: stress-tested cycle counts (no oracle — any
`return N` change would be invisible), test trivially-passing
patterns, encoding spot-checks (all 7 verified correct), Phase 4.B
landmines (silent T=0 branches, unaligned loads will flip when fault
model lands).

**Findings addressed (apply-feedback)**:
1. CPS bit-swap fixed in mdrp2040: `opcode & (1 << 1)` for `affect_i`.
   F bit ignored on M0+ (no FAULTMASK; UNPREDICTABLE). Tests updated
   to canonical encodings + new `cps_with_only_f_bit_is_noop_on_m0plus`.
2. Strengthened `rev_subop_0b10` and `it_encoding` UNDEFINED tests to
   assert no register clobber (not just fault flag).
3. Added `mul_preserves_c_and_v` test.
4. Added 3 cycle-count oracle tests (`cycles_taken_branch_is_3`,
   `cycles_simple_dp_is_1`, `cycles_ldm_is_1_plus_count`) so Phase 5
   recalibration surfaces as test failures, not silent drift.
5. Added `TODO(Phase 4.B)` comments at 4 sites where Thumb bit is
   silently stripped (BX, BLX, POP{PC}, MOV/ADD to PC).
6. `tech_debt.md` gained a "Core Correctness" section logging the
   mdrp2350 CPS bug.

**Post-phase state**: 955 tests pass (950 + 5 new in apply-feedback),
clean workspace build, zero warnings in mdrp2040.

#### Phase 4.B: Thumb-32 subset + exceptions + integration
Status: completed

**Implementer**: ~700 lines new across `execute_wide.rs` (207),
`exceptions.rs` (197), `bus/ppb.rs` (104), plus modifications to
`core/mod.rs`, `core/decode.rs`, `core/execute.rs`, `bus/mod.rs`,
`lib.rs`, `tests.rs`. 31 new unit tests; `Emulator::step` and
`Emulator::run` now functional.

**Coverage delivered**: BL/MRS/MSR/DSB/DMB/ISB; exception entry for
SVC/BKPT/HardFault/NMI; EXC_RETURN 0xF1/0xF9/0xFD; invalid EXC_RETURN
→ HardFault; unaligned fault on LDR/STR/LDRH/STRH/LDM (base)/PUSH/POP;
T=0 HardFault at BX/BLX/POP{PC}/MOV-to-PC; minimal PPB (vtor, shpr,
icsr, active bitmap).

**Reviewer**: PASS-with-notes. Flagged PRIMASK+SVC (spec-violation),
T=0 non-HardFault vector silent corruption, SYSm=4 misclassified as
APSR, exit_exception cycle drop.

**Devil's advocate**: Found a real **banked SP staleness bug** —
`enter_exception`/`exit_exception` read `self.regs.msp`/`psp` directly,
but `SUB SP,#imm` in a handler updates `r[13]` without syncing to the
banked field. Every Pico SDK handler allocating stack locals would
break. Also confirmed PRIMASK+SVC spec issue and T=0 corruption.

**Findings addressed (apply-feedback)**:
1. **Banked SP sync** — added `sync_sp_to_banked()` at top of enter
   and `sync_sp_from_banked()` after exit, so banked MSP/PSP and r[13]
   stay coherent across handler SP manipulation. New test
   `handler_sp_mutations_sync_to_banked_on_exit` exercises the flow
   end-to-end.
2. **PRIMASK + SVC** — `deliver_fault(Fault::Svc)` now checks PRIMASK
   and escalates to HardFault (exception #3) when set. Test
   `step_primask_does_not_mask_svc` renamed to
   `step_primask_escalates_svc_to_hardfault` with flipped expectation.
3. **T=0 vector escalation** — non-HardFault with T=0 vector now sets
   `pending_fault = HardFault` (with pre-bailout SP sync so the next
   enter sees the committed partial frame); HardFault-in-HardFault
   still locks up. Test
   `nonhardfault_with_t0_vector_escalates_to_hardfault` added.
4. **Alignment pad test** added (`exception_entry_pads_when_sp_is_4_aligned_not_8`).
5. **Emulator::run test** added (`run_advances_pc_over_nops`).
6. **SYSm match tightened** to exact values (0 | 3 | 5 | 8 | 9 | 16 | 20);
   reserved → HardFault. Two new tests for MSR and MRS reserved cases.
7. **`exception_priority`** in `bus/ppb.rs` annotated
   `#[allow(dead_code)]` (will be wired in Phase 5 NVIC work).

**Tech debt surfaced (not fixed in Phase 4)**:
- `mdrp2350` has the same banked-SP staleness in its
  `enter_exception`/`exit_exception`. Not touched per review scope.
  Should add to `tech_debt.md` under "Core Correctness" when CPS bug
  entry gets revisited.

**Post-Phase-4 state**: 992 workspace tests pass (955 → 992, +37).
mdrp2040 unit tests: 147 total. Clean workspace build, zero warnings.
`Emulator::step`/`run` functional on Thumb-16 + Thumb-32 subset + full
exception model. `load_image`/`load_bootrom`/`gpio_*` remain `todo!()`
for Phase 5.

### Phase 5 — RP2040 bus + peripherals

#### Phase 5.A: bus fabric + clocks + SIO + GPIO + integration
Status: completed

**Implementer**: ~2000 lines new. `bus/mod.rs` 127 → 987, plus new
`memory.rs`, `bus/{clocks,resets,sio,io_bank0,pads_bank0}.rs`, and
`tests/firmware.rs`. `lib.rs` completed: `load_image`, `load_bootrom`,
`gpio_read/write/read_all` all functional. Dual-core step:
per-instruction cadence (not quantum like mdrp2350 — noted as tech
debt). Core 1 halted at reset; wakes on FIFO event.

Coverage: RP2040 address decode, SRAM 4-stripe + 2-scratch banking
with +1-cycle contention, CLOCKS/RESETS/XOSC/ROSC/PLL_SYS/PLL_USB
register models, full SIO (GPIO, CPUID, FIFO, 32 spinlocks, divider,
interp storage), IO_BANK0 (30 GPIOs with FUNCSEL), PADS_BANK0, XIP_CTRL
+ SSI stubs.

**Reviewer**: flagged a real bug — CLK_SYS_DIV (0x40) and
CLK_SYS_SELECTED (0x44) were swapped. Also inconsistent unmapped
handling across write widths.

**Devil's advocate**: caught two critical bugs the reviewer missed —
SRAM alias XOR/SET/CLR semantics (wrong per RP2040 §2.1.2: these are
bank-striping aliases, not peripheral aliases) and `bus_fault` sticky
flag set but never observed by CPU (unmapped loads silently returned
0). Test `sram_xor_alias` was actively encoding the SRAM bug. DA also
surfaced landmines around WFE wiring and SDK multicore handshake not
being parsed.

**Findings addressed (apply-feedback)**:
1. **SRAM aliases** — removed XOR/SET/CLR helpers. Aliases now plain
   storage mirror. Old test deleted; 3 new tests (`sram_aliases_mirror_same_storage`,
   `unmapped_writes_fault_at_every_width`, `rom_writes_silently_ignored_at_every_width`).
2. **bus_fault → HardFault** — `CortexM0Plus::step` now observes
   `bus.bus_fault()`, clears it, stages `Fault::HardFault`. New test
   `unmapped_load_escalates_to_hardfault`.
3. **CLK_SYS_DIV/SELECTED swap** — offsets fixed. Test
   `clk_sys_div_offset_0x40_feeds_recompute` guards the correct wiring.
4. **Write-width consistency** — unmapped at any width → `bus_fault`;
   ROM/XIP writes at any width → silent.
5. **MULTICORE_LAUNCH_WAKE_MAGIC dead-code hack** — deleted. TODO
   comment documents the wake-simplification until Phase 6.

**Tech debt logged** (new section "Phase 5.A Simplifications" in
tech_debt.md): WFE/SEV wiring, divider 8-cycle latency, PLL LOCK
always-1, core 1 SDK handshake, per-instruction cadence vs quantum,
divider 2-read dirty-clear heuristic.

**Post-phase state**: 1052 workspace tests pass (1046 + 6 net).
mdrp2040: 212 tests. Clean workspace build, zero warnings. Real RP2040
firmware writing to SIO GPIO runs end-to-end via `Emulator::step`.

#### Phase 5.B: PIO wiring
Status: completed

**Implementer**: +41/-12 lines in `bus/mod.rs`, +54/-14 in `lib.rs`,
plus new `pio_tests.rs` (382 lines, 15 tests). Bus gains
`pio: [PioBlock; 2]` (vs mdrp2350's [3]). PIO0 at 0x5020_0000, PIO1 at
0x5030_0000; XOR/SET/CLR aliases via +0x1000/+0x2000/+0x3000
correctly routed. `update_gpio` OR's PIO `pad_out & pad_oe` (with PIO
pad_oe overriding SIO), masked to 30 pins. PIO steps per `emu.step`
by the core-0 cycle cost (3 for branches, 1 for NOPs) — matches
hardware where PIO clock = sys clock.

All `todo!()` in `mdrp2040/src/` now resolved.

**Review + DA (combined)**: PASS-with-notes. Confirmed merge semantics
mirror mdrp2350, alias stripping correct, reset coverage complete.
Flagged: clippy::identity_op noise in tests; RESETS-not-gating-PIO
carried over from mdrp2350 as tech debt.

**Findings addressed (apply-feedback)**:
1. Added file-level `#![allow(clippy::identity_op)]` to `pio_tests.rs`
   with explanatory comment (12 lints cleared).
2. Logged "PIO not gated on RESETS bit" in tech_debt.md's Phase 5.A
   Simplifications section.
3. Added `emu_step_advances_pio_by_core0_cycle_cost` cadence test +
   module-level doc block explaining the relationship.

**Post-phase state**: 1068 workspace tests pass (1052 + 16 new total:
15 PIO + 1 cadence). mdrp2040: 227 tests. Clean workspace build, zero
warnings, `cargo clippy -p mdrp2040 -W clippy::identity_op` clean.

### Phase 6 — test harness split
Status: completed

**Implementer**: Renamed 6 binaries (`*` → `*_rp2350` for all RP2350
variants; `qemu_diff` → `qemu_diff_m33`). Created
`src/bin/qemu_diff_m0plus.rs` (433 lines — **real implementation**)
and `src/bin/probe_diff_rp2040.rs` (38-line stub that exits 2 with a
rationale pointing at the HLD; no Pico hardware available on the lab
rig). softfloat_diff stayed RP2350-only. Harness version bumped
0.10.0 → 0.11.0, `mdrp2040` added as a path dependency.

**M0+ QEMU oracle**: QEMU 10.2 has no `cortex-m0plus` CPU model, so
the implementer used `-machine microbit -cpu cortex-m0`. M0+ is a
strict ISA superset of M0 for the Thumb-16/-32 subset under test (MUL
cycle counts differ but harness doesn't compare cycles). Separate GDB
port (3334 vs 3333) so the M0+ and M33 runners can coexist. Fuzz
generator filters out M33-only encodings (IT blocks, CBZ/CBNZ,
Thumb-32 DP/LDM/STM/MUL, FP).

**Reviewer + DA**: caught a **commit-blocker** — `mdpicoem_harness::compare`
hardcoded M33 address bases (`QEMU_TEST_SCRATCH`, `EMU_TEST_STACK`,
etc.). 100% of M0+ fuzz tests reported false failures with delta
signatures like `SP=0xFFFC_1000`. The oracle was dead on arrival.
Also flagged: 5 renamed RP2350 bins still printed the old binary
names in their banners/usage strings; missing `tech_debt.md` Phase 6
section.

**Findings addressed (apply-feedback)**:
1. **CompareBases parameterization**: added `CompareBases` struct with
   `M33_RP2350` and `M0PLUS_RP2040` associated consts. `compare()`
   signature grew a `&CompareBases` param. Updated 15 callers (2
   binaries + 13 unit tests). Promoted `EMU_M0PLUS_TEST_*` from
   private to `pub` in lib.rs for symmetry.
2. **Banner/usage strings**: updated in 5 renamed bins
   (`probe_diff_rp2350`, `full_test_rp2350`, `probe_verify_rp2350`,
   `bank_conflict_test_rp2350`, `paced_bench_rp2350`). Preserved
   physical-chip references like "RP2354 hardware".
3. **tech_debt.md**: added "Phase 6 Simplifications" section with
   probe_diff_rp2040 stub entry and cortex-m0-vs-m0plus entry.

**Fuzz smoke results (post-fix)**:
- `qemu_diff_m0plus --fuzz 10 --seed 1`: **127/127 passed** (from 0/127).
  This is the first real validation that mdrp2040's Thumb-16 decoder +
  executor produces state identical to QEMU's M0 CPU. No real M0+
  bugs surfaced at this seed/count.
- `qemu_diff_m33 --fuzz 10 --seed 1`: **397/397 passed** — no
  regression from the compare() refactor.

**Post-phase state**: 1068 workspace tests pass (unchanged).
`cargo build --workspace` clean. `qemu_diff_m0plus` is a real
differential oracle; `probe_diff_rp2040` is an honest stub that fails
loudly if invoked. No real M0+ bugs caught at smoke level (Phase 4's
ISA implementation held up against QEMU on first contact).

### Phase 7 — mdrp2040app
Status: completed

**Implementer**: Created `crates/mdrp2040app/` by copying mdrp2350app
and adapting. 17 files (Cargo.toml, lib/main/sim/snapshot/ui/firmware,
5 panels, 2 devices incl. verbatim 470-line LCD copy, smoke.rs).
Plus `roms/rp2040/gen_blinky.py` + generated `blinky.bin` (28 bytes)
+ `bootrom.bin` (16 KB). roms/.gitignore whitelist updated for the
new bootrom.

Panel adaptation was minimal because mdrp2350app's status panel was
already minimal (no FPU/DCP/RCP/NS register views). Only `isa.rs`
needed content updates — the M33 cycle cheat-sheet was replaced with
M0+-appropriate numbers (MULS=1, LDR=2, LDM 8 regs=9, B=1-3, BL=4,
ADDS=1) sourced from the Phase 4 implementation.

Blinky firmware (hand-assembled M0+ Thumb-16): builds GPIO25 mask
via MOVS+LSLS, loads SIO_BASE=0xD000_0000 from a literal pool,
writes GPIO_OE_SET and GPIO_OUT_SET, then enters a delay loop with
GPIO_OUT_XOR to toggle.

**Review + DA (combined)**: PASS-with-notes. Zero commit-blockers.
- Smoke test `blinky_drives_gpio25_high_within_2s` ran 5/5 green in
  ~0.29s each (7× headroom vs 2s budget). Zero flakes.
- All 7 `isa.rs` cycle numbers cross-checked against Phase 4.A
  oracle tests — all match.
- gen_blinky.py reproduces bit-identical bytes.
- No M33-only leftovers (FPU/coprocessor/NS-banked) in mdrp2040app.
- bootrom.bin correctly whitelisted in roms/.gitignore.
- Non-blocking: LCD device file's docstring still references
  roms/rp2350/gen_lcd_demo.py since no rp2040 LCD demo exists yet.
  Bench panel's REFERENCE_TABLE has cycles=0 placeholders (honest,
  documented; will need real values when a benchmark firmware lands).

No apply-feedback pass needed — findings are minor enough to absorb
into the commit message or leave for follow-up.

**Post-phase state**: 1088 workspace tests pass (1068 + 20 new
mdrp2040app: 17 lib unit + 2 main path-resolution + 1 smoke).
Clean release build. mdrp2040app binary constructs, loads blinky,
toggles GPIO25 within 290ms on this host.

### Final — conformance verification
Status: completed

**Conformance audit (scope: HLD phases 0–7 vs tree state).** Verdict:
**MOSTLY COMPLETE — structurally complete, 2 documentation gaps, 0
architectural gaps.** Every phase's deliverables are present and
exercised; the two gaps were `CLAUDE.md` still describing the
pre-restructure state, and a missing status update on the quantum
convergence tech-debt entry. Neither touches emulator source.

**Devil's advocate audit.** Verdict: **95% done, 5 items flagged** —
all 5 addressed in this final closeout pass. Specifically:
1. `CLAUDE.md` sweep: crate list, QEMU binary names, workspace layout,
   RP2040 architecture block, testing topology, probe-oracle availability
   note — all brought current.
2. `tech_debt.md` MULS hardcode entry (MULS=1 defensible but not
   silicon-calibrated; `isa.rs` consumes it as ground truth) added to
   new "Phase 6/7 Residuals" section.
3. `tech_debt.md` Thumb-32 oracle gap entry (`is_m0plus_safe` filters
   out all wide-form encodings, so Phase 4.B's ~700 lines of
   `execute_wide.rs` ship with unit-test-only coverage) added to same
   section.
4. `tech_debt.md` existing "RP2040 per-instruction dual-core cadence"
   entry updated with a status line noting Phase 7 shipped without
   convergence, `step_quantum` is an unused field, firmware correctness
   is unaffected but perf comparability between chips is.
5. This journal entry filled in.

**Final quality gates (pre-closeout pass):**
- **Workspace tests:** `cargo test --workspace` → 1088 pass, 7
  pre-existing ignores (unchanged since start of Phase 1). Breakdown
  cross-checks against per-phase deltas: 840 (Phase 1 baseline)
  + 5 (Phase 3 smoke) = 845, + 110 (Phase 4.A) = 955, + 37
  (Phase 4.B) = 992, + 60 (Phase 5.A) = 1052, + 16 (Phase 5.B) = 1068,
  + 20 (Phase 7 mdrp2040app) = 1088. Consistent.
- **Build:** `cargo build --workspace --release` clean.
- **paced_bench_rp2350 median: 39.7 MHz** (vs 32.9 MHz baseline,
  +20.7% headroom). **Caveat:** host-variance is first-order here
  (Phase 1 and Phase 2 A/B runs diverged by ~2x on the same tree due
  to thermal/load effects), so this is best read as an "at least as
  fast as baseline" claim. The >=32.24 MHz Phase 2 gate is cleared
  on every run since.
- **`qemu_diff_m33 --fuzz 10 --seed 1`:** 397/397 passed. No M33
  regression from the harness split.
- **`qemu_diff_m0plus --fuzz 10 --seed 1`:** 127/127 passed.
- **`qemu_diff_m0plus --fuzz 100 --seed 42`:** 1280/1280 passed.
- **`qemu_diff_m0plus --fuzz 100 --seed 7`:** 1277/1277 passed.
- **`mdrp2350app`:** starts, loads blinky, runs 5+ seconds without
  crashing.
- **`mdrp2040app`:** starts, loads blinky, runs 5+ seconds without
  crashing; smoke test observes GPIO25 toggle within ~290ms.

**HLD-vs-implementation deviations (all acceptable, all journaled):**
- **Sub-phase commits** — Phase 4 shipped as 4.A+4.B, Phase 5 shipped
  as 5.A+5.B. The HLD specified monolithic phase commits, but each
  sub-phase cleared its own reviewer + devil's advocate pass and was
  committed as an atomic checkpoint. Journaled at each boundary
  (Phase 4.A/4.B entries above, Phase 5.A/5.B entries above).
- **`mdpicoem-common` version: 0.1.0 → 0.2.0 at Phase 3.** Phase 3
  uncovered that `Memory::new()` had hardcoded RP2350 sizes; the fix
  was additive (`Memory::with_sizes(rom, sram)`) rather than breaking
  the existing API. Journaled in the Phase 3 entry above. mdrp2350
  still calls `Memory::new()`; migration to `with_sizes` is a
  cosmetic cleanup, not a blocker.
- **`mdpicoem-harness` version: 0.10.0 → 0.11.0 at Phase 6.** Phase 6
  renamed 6 binaries and added 2 new ones (`qemu_diff_m0plus` real,
  `probe_diff_rp2040` stub). Minor bump per semver. Journaled in the
  Phase 6 entry above.

**Pre-existing issues — NOT restructure regressions:**
- `test_pacer_behind_detection` flake in
  `crates/mdpicoem-common/src/pacer.rs`. Pre-existing; predates the
  restructure. Not touched by any phase. One of the 7 pre-existing
  `cargo test --workspace` ignores.
- Unused-import warning in `crates/mdrp2350/src/tests.rs`.
  Pre-existing; predates the restructure. Not touched by any phase.
  No new warnings introduced anywhere else in the workspace.

**Restructure closeout:** the 10 phase commits (Phase 0 baseline
through Phase 7 mdrp2040app) plus this final documentation pass
satisfy the HLD deliverables. The workspace now supports both
RP2350/RP2354 and RP2040 as independent library + app pairs sharing
`mdpicoem-common`, with per-chip unit + QEMU-differential oracles.
Remaining tech debt is tracked in `tech_debt.md` under the per-phase
Simplifications and Residuals sections, and is deliberately out of
scope for the restructure.

## Running notes and decisions

(none yet)
