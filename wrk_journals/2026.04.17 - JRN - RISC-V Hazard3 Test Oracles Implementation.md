# RISC-V Hazard3 Test Oracles — Implementation Journal

**Started:** 2026-04-17
**HLD:** `wrk_docs/2026.04.17 - HLD - RP2350 RISC-V Hazard3 Test Oracles.md` (V4)
**Core HLD:** `wrk_docs/2026.04.17 - HLD - RP2350 RISC-V Hazard3 Core Support V6.md`

## Scope decision

Phase 1 requires hardware (RP2354 + Pico probe). Phase 3 is gated on Phase 1 outcome. This session implements:

1. **Phase 1 deliverables** — LLD, ~150 LoC throwaway `riscv_probe_spike.rs`, RUNBOOK RV-mode section. Code written; Arthur runs on hardware when available.
2. **Phase 2 LLD** — empirical QEMU checks (host has qemu-system-riscv32.exe), then LLD with pinned invocation + CSR-diff mechanism.
3. **Phase 2 full implementation** — `qemu_diff_riscv32`, `riscv_gen.rs`, `QemuProfile::RISCV32_RP2350`, property test for encoder.

**Phase 3 is deferred** — cannot design paths without Phase 1 hardware results. Surfaced at end.

## Stages

- [x] **Stage 1** — Phase 1 LLD + spike binary + RUNBOOK RV-mode section
- [ ] **Stage 2** — Phase 2 LLD (post empirical QEMU check)
- [ ] **Stage 3** — QEMU infrastructure (`QemuProfile::RISCV32_RP2350`, gdb_client RV extensions)
- [ ] **Stage 4** — `riscv_gen.rs` + property test against hand-rolled reference encoder
- [ ] **Stage 5** — `qemu_diff_riscv32` binary
- [ ] **Stage 6** — End-to-end smoke run against live QEMU
- [ ] **Stage 7** — Final-verification agent: compare against HLD §4 deliverables

## Log

### 2026-04-17 — Session start

Kicked off Stage 1. Delegating to implementer agent with instructions to deliver Phase 1 LLD + throwaway spike binary + RUNBOOK RV-mode section in one pass (tightly coupled).

### 2026-04-17 — Stage 1 complete

**Process:** implementer → code-review + devil's-advocate in parallel → apply-feedback.

**Implementer findings** (pre-review):
- Discovered probe-rs 0.31's `RP235x.yaml` declares both cores `armv8m` — no RV stanza. **Predetermines Plan D** as the likely Phase 1 outcome. Documented loudly in LLD §2.
- Initial binary at 551 LoC (2× target). ARCHSEL row initially SKIP to avoid bricking without datasheet pin.

**Review findings** (code-reviewer + devil's advocate in parallel):
- **Critical:** Row A always-SKIP defeats the LLD goal — needed split into A1 (read-only) + A2 (gated write).
- **Critical:** `auto_attach` likely errors out in RV mode — need fallible wrap + matrix-still-prints path.
- **Major:** HW breakpoint not cleared → Row 5 silently broken; `delta==4` accepted hides probe-rs C-ext decode bugs; E2 missing PC assertion after step (could trap into mtvec); `list_cores()` iteration assumed 0..N; magic literal `RegisterId(0x1002)` for SP.
- Several minor polish items (outcome first in summary, log swallowed errors, LLD pedantry).

**Apply-feedback outcome:** all 17 items addressed, none disputed. Binary live-tested against an attached probe on ARM-booted RP2354 — matrix output produced correctly, Row A1 read `0x4010_0020 = 0x000000B0` (real silicon data point for future datasheet pinning). Final LoC: **820** (up from 551).

**Deliverables:**
- `crates/mdpicoem-harness/src/bin/riscv_probe_spike.rs` (820 LoC)
- `wrk_docs/2026.04.17 - LLD - RISC-V Probe-rs Attach Spike V1.md`
- `RUNBOOK.md` — new "Putting RP2354 into RISC-V mode" section
- `crates/mdpicoem-harness/Cargo.toml` — `[[bin]]` stanza

**Build state:** `cargo build --release` clean, zero warnings on the new code.

**Open items tracked for later:**
- POWMAN CHIP_RESET offset still `ASSUMPTION` in RUNBOOK + `POWMAN_OFFSET_PINNED = false` const in binary. Needs datasheet §5.10 verification before `--attempt-archsel-flip` flag can be used safely.
- LoC grew 4× over HLD estimate (~150 LoC was optimistic); accepted tradeoff for complete error-path coverage.

Committing and moving to Stage 2.
