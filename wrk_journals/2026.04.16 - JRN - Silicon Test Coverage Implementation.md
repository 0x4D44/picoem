# Silicon Test Coverage Expansion — Implementation Journal

**Date:** 2026-04-16
**HLD:** `wrk_docs/2026.04.16 - HLD - Silicon Test Coverage Expansion.md` (V3 Final)

## Phase 1 — Close pure test gaps

**Started:** 2026-04-16
**Goal:** 4 new PeriphScenario entries. No emulator changes.

### Scenarios to implement
1. `sio_divider_unsigned` — unsigned division, observe QUOTIENT/REMAINDER/CSR
2. `sio_divider_signed` — signed division including div-by-zero
3. `sio_mtime_count_and_match` — enable MTIME, spin, disable, observe frozen count
4. `timer1_alarm0_fire_and_clear` — clone of timer0 scenario at 0x400B_8000

### Implementation notes
- SIO divider: non-custom-sled (immediate compute on DIVISOR write)
- MTIME: custom sled required (enable → spin → disable → BKPT)
- TIMER1: custom sled (clone timer0_alarm0 sled with address adjustment)
- All addresses verified against sio/mod.rs and timer.rs in review rounds

### Status: COMPLETE
Already implemented in a prior session. Verified all 4 scenarios present with
correct addresses, MTIME sled disables before BKPT, all tests pass.

---

## Phase 2 — Banked SP fix + ISR oracle verification

**Started:** 2026-04-16
**Goal:** Fix banked SP staleness bug. Update stale ISR docs.

### Changes made
1. **exceptions.rs** — added `sync_sp_to_banked()` in `enter_exception` (after
   lockup check, before reading msp/psp) and `exit_exception` (before reading
   msp/psp for unstack). Added explanatory comments matching mdrp2040 pattern.
2. **tests.rs** — added `test_pendsv_stacks_at_post_sub_sp_not_stale_banked_msp`:
   SUB SP then trigger PendSV, verify stacked frame at correct (post-SUB) address.
3. **isr_scenarios.rs** — fixed stale module header: ICSR dispatch IS wired,
   W1S/W1C IS implemented. Corrected tech_debt.md reference from :295 to :314.
   Removed premature "has been resolved" claim.
4. **silicon_isr_diff_rp2350.rs** — updated banner to note dispatch is wired.

### Devil's advocate review findings
- Sync placements: CLEAN (both match mdrp2040 structural position)
- Unit test: CLEAN (correctly reproduces the stale-SP condition)
- Doc: CRITICAL — fixed stale tech_debt.md line ref (295→314) and removed
  stale "ICSR W1S/W1C not implemented" paragraph (ppb.rs:454-465 implements it)
- Parity with mdrp2040: CLEAN

### Verification
- 878 mdrp2350 tests pass (+ 2 ignored)
- Harness builds clean
- Phase 2.0 (run ISR oracle before fix) and 2.2 (re-run after) deferred to
  hardware session — requires RP2354 probe attached

### Status: COMPLETE (code changes; hardware validation deferred)

---

## Phase 3 — Peripheral fidelity upgrades

**Started:** 2026-04-16
**Goal:** Make storage-only registers functional + 3 silicon scenarios.

### Changes made (3 parallel agents)
1. **ADC** (`adc.rs`) — FIFO format fixed to ERR:AINSEL:RESULT hardware format.
   RROBIN AINSEL advancement on conversion complete. `adc_round_robin_2ch` scenario.
2. **PWM** (`pwm.rs`) — 8.4 fractional accumulator with O(1) batch computation.
   `frac_accum` field on PwmSlice. DIV_RESET doc corrected to "8.4".
   `pwm_fractional_div` scenario with TOP=0xFFFF, DIV=0x0020.
3. **DMA** (`dma.rs`, `dreq.rs`) — timer pacing for TREQ 59-62 in Dma::tick().
   timer_accum[4] + timer_dreq_asserted[4] fields. X=0/Y=0 guards.
   DREQ_TIMER0..3 constants. `dma_timer_paced` scenario.

### Status: COMPLETE

---

## Phase 4 — Deeper peripheral paths

**Started:** 2026-04-16
**Goal:** PIO INTn routing, UART RX loopback, PIO RESETS gating.

### Changes made (3 parallel agents)
1. **PIO INTn** (`mdpicoem-common/pio/mod.rs`, `lib.rs`) — INT0_INTE/INT1_INTE
   fields + read/write dispatch. raw_intr(), int0_ints(), int1_ints() methods.
   route_pio_irqs() in lib.rs. `pio0_int_routing_split` scenario.
2. **UART LBE** (`uart.rs`) — TX drain path checks UARTCR.LBE; when set, pushes
   drained byte to RX FIFO. `uart0_rx_loopback` scenario.
3. **PIO RESETS** (`lib.rs`, `pio_tests.rs`) — tick_pio gated on RESETS.DONE per
   PIO block. Registers remain accessible while in reset. Unit test confirms
   SM doesn't advance while held in reset.

### Status: COMPLETE

---

## Final verification

**Compliance checker:** 29/29 code items PRESENT. 3 stale doc items fixed
(silicon_isr_diff_rp2350 runtime banner, CLAUDE.md ISR description, tech_debt.md
SP bug entry).

**Devil's advocate:** All 7 concerns CLEAN. Minor UART gaps (RX interrupt, OE on
overflow) are documented as deferred.

**Test results:** 880 mdrp2350 tests pass, 394 harness tests pass.

### Commits
1. `00d006b` — Phase 2: banked SP fix + ISR doc updates
2. `c08bafe` — Phase 3: ADC RROBIN, PWM frac DIV, DMA timer pacing
3. `8bb7614` — Phase 4: PIO INTn, UART LBE, PIO RESETS
4. `615da6f` — Final doc fixes (stale ISR/SP references)

### Status: ALL PHASES COMPLETE
