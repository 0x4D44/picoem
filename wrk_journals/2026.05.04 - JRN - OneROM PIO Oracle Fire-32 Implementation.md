# OneROM PIO Oracle Fire-32 Implementation Journal

Date: 2026-05-04

## Supervisor kickoff

Arthur asked me to supervise an agent team through the Stage 4 implementation,
with implementation, review, feedback application, final plan-compliance audit,
and commits after significant stages. I read the `code-reviewer` skill and the
available feedback-application skill (`code-review-apply-feedback`; the exact
name `code-review-apply-changes` is not installed). I also re-read the Stage 4
HLD and checked the worktree.

Current worktree has pre-existing untracked Stage 1-3/HLD artifacts:

- `crates/picoem-harness/fixtures/onerom-fire-32-a-rp2350-seabios.bin`
- `crates/picoem-harness/fixtures/sources/seabios-32-27c020.json`
- `wrk_docs/2026.05.04 - HLD - OneROM Serving Oracle Fixture Generalization.md`
- `wrk_docs/2026.05.04 - HLD - OneROM PIO Oracle Fire-32 Generalization.md`
- `wrk_journals/2026.05.04 - JRN - Fire-32-a SeaBIOS Firmware Build.md`
- `wrk_journals/2026.05.04 - JRN - OneROM Fixture Generalization Implementation.md`
- `wrk_journals/2026.05.04 - JRN - OneROM PIO Oracle Fire-32 HLD.md`

Supervision plan:

1. Stage 4A: worker implements PIO `GPIOBASE`, high-bank serial GPIO, and
   threaded mirror. A devil's-advocate agent reviews the shape while the worker
   works.
2. Review Stage 4A with a separate code-reviewer agent.
3. Apply review feedback with a feedback-application worker.
4. Validate and commit Stage 4A.
5. Repeat for Stage 4B oracle matcher and Stage 4C cleanup/acceptance.
6. Final auditor agent compares implementation against the HLD and acceptance
   criteria; iterate if anything is missing.

Emotional temperature: alert and slightly wary. The biggest risk is letting
the main thread get pulled into debugging. I will keep the main thread focused
on coordination, reviewing agent outputs, journal updates, and commits.

## Design stage committed

Committed the Stage 4 HLD and kickoff journals as `07a4d98` (`docs: add
fire-32 pio oracle design`). I intentionally left older untracked fixture/docs
artifacts alone. Next delegation is Stage 4A only: GPIOBASE register behavior,
physical-to-local PIO pin sampling, low/high pad mapping, and serial/threaded
high-bank GPIO input plumbing.

## Stage 4A delegated

Spawned `Confucius` as the Stage 4A implementation worker with a narrow write
scope across picoem-common PIO and rp2350-emu serial/threaded GPIO paths.
Spawned `Huygens` as the devil's-advocate reviewer with read-only scope to
challenge GPIOBASE semantics, high-bank sampling, pad mapping, threaded parity,
and overengineering risk.

Emotional temperature: steadier. The initial spawn hiccup was a tooling shape
issue, not a design issue. I am keeping the implementation and critique tracks
separate so the main thread can coordinate rather than get pulled into the code.

## Stage 4A wait checkpoint

First wait window expired with both agents still running. I did not inspect or
debug the code path myself; the supervision boundary is intentional. Continuing
to wait for either implementation output or devil's-advocate critique.

## Stage 4A devil's-advocate feedback

`Huygens` returned a useful read-only critique. The strongest warnings were:
threaded `pad_out/pad_oe` cannot remain a single local u32 pair once GPIOBASE
can move the PIO window, and high-bank input must become a merged sampled state
rather than only a `GPIO_HI_IN` register-read overlay. Other nudges: handle the
exact GPIOBASE register before the existing unmodelled range, keep GPIOBASE per
`PioBlock`, keep `StateMachine` internals local/u32, and avoid force-exec tests
for high-pin sampling because they currently pass `gpio_in = 0`.

Forwarded this feedback to `Confucius` while Stage 4A implementation is still in
flight. Emotional temperature: good. This is exactly why I wanted a separate
challenger early; both blocker findings are cheap to account for before review
and expensive to discover after.

## Stage 4A second wait checkpoint

`Confucius` is still running after the longer wait. That is acceptable for this
slice because Stage 4A spans shared PIO behavior plus both serial and threaded
RP2350 paths. I am deliberately not opening the code to debug in the main
thread; waiting for the worker's implementation handoff.

## Stage 4A implementation handoff

`Confucius` completed Stage 4A without committing. Reported changes: per-block
PIO `GPIOBASE`, physical-to-local PIO sample projection, low/high physical pad
mapping, serial `Bus::gpio_in_hi`, threaded `AtomicGpio::in_hi`, and physical
PIO pad snapshots. The worker reported focused tests, clippy, and workspace
`cargo test -q` passing. Stage 4B harness changes remain untouched.

Next step is a separate code-reviewer pass before any commit. Emotional
temperature: cautiously positive. The worker claims the two early
devil's-advocate blockers were handled, but this touches enough concurrency
plumbing that I want review before treating the stage as complete.

## Stage 4A code review

`Carson` reviewed Stage 4A with the code-reviewer stance. Findings were focused
and actionable:

1. Threaded `WorkerBus` readback does not expose PIO `GPIOBASE`, so threaded
   MMIO read behavior differs from serial after a firmware write.
2. Threaded low/high GPIO publication is not a coherent physical snapshot for
   the new cross-bank GPIO16..47 window; separate atomics can expose a mixed
   adjacent-quantum state.

The reviewer also noted an out-of-scope picoem-harness diff in the worktree. I
will keep that out of Stage 4A unless a later Stage 4B worker explicitly owns
it. Next step is a feedback-application worker using the installed
`code-review-apply-feedback` skill.

## Stage 4A feedback application delegated

Spawned `Sartre` as the Stage 4A feedback-application worker with the
`code-review-apply-feedback` process. The worker owns only the threaded
readback/coherent-snapshot review findings and the related test gap. I asked
for explicit agree/disagree reasoning, because the coherent-snapshot issue needs
engineering judgment rather than blind acceptance.

## Stage 4A feedback applied

`Sartre` applied all Stage 4A review feedback without committing. Reported
fixes: threaded `GPIOBASE` readback via published per-block base, coherent
packed 64-bit GPIO input snapshot, seqlock-backed physical PIO pad snapshot, and
threaded MMIO write/drain/readback coverage for `PIO1 + 0x168`. The worker
reported scoped fmt, focused tests, clippy, `cargo test -q`, and `git diff
--check` passing. They also reported full workspace `cargo fmt --check` remains
blocked by out-of-scope picoem-harness formatting diffs, which they correctly
left untouched.

Next step is a post-feedback review gate before Stage 4A commit. Emotional
temperature: constructive. The coherent snapshot fix sounds like it adds a small
concurrency primitive, so the re-review needs to check whether that complexity
is justified and contained.

## Stage 4A committed

`Zeno` completed post-feedback review and found no blocking issues. Prior
threaded `GPIOBASE` readback and low/high snapshot findings were confirmed
resolved. Committed Stage 4A as `a57e1d6` (`feat: add pio gpiobase high-bank
sampling`) with the picoem-common/rp2350-emu files and this journal only.

After the commit, `git status` still shows older picoem-harness paths as
modified, but `git diff --name-only` returns no textual diff. I am treating
those as pre-existing/out-of-stage status noise until the Stage 4B worker owns
the harness changes explicitly.

Emotional temperature: satisfied but not relaxed. Stage 4A was the risky
emulator plumbing slice; Stage 4B is narrower, but it is where the actual
fire-32 oracle acceptance either succeeds or fails.

## Stage 4B delegated

Spawned `Wegener` as the Stage 4B implementation worker for fixture-aware
ServingOracle address matching. Scope is intentionally narrow:
`onerom_serving_oracle.rs`, adjacent tests, and only other harness files if a
small API adjustment truly requires it. The worker was told not to touch Stage
4C docs/cleanup yet and not to commit.

The first devil's-advocate spawn hit the agent thread limit, so I closed the
completed Stage 4A agents and then spawned `Banach` as the Stage 4B challenger.
The challenger is focused on hidden `u16` truncation, remaining `hi16 == 0x2000`
assumptions, literal/raw `Case` regressions, fragile shape derivation, and tests
that fail to exercise address bits 16..18.

## Stage 4B wait checkpoint

First Stage 4B wait window expired with both `Wegener` and `Banach` still
running. No intervention yet. This stage is small in code shape but subtle in
semantics, so I would rather wait for a complete first pass than push partial
instructions.

## Stage 4B devil's-advocate feedback

`Banach` returned before the implementation worker. Key challenges forwarded to
`Wegener`: fire-32 matcher tests must actually cover addresses above `0xFFFF`
and GPIO32/33/34 mapping to resolved bits 16/17/18; shape derivation must not
hide partial `addr_pins` parsing bugs; fire-32 proof should use `Case::from_addr`
unless raw masking is intentionally widened; fire-24 overlay behavior needs
non-literal/raw regression coverage; direct `ResolvedAddrOutOfRange` production
should be tested; and diagnostics should not truncate `pin_pattern` to `u32`.

Emotional temperature: attentive. This critique caught the exact kind of
"tests pass but the new fixture is not truly exercised" risk that would make
the implementation look complete while leaving HLD §6.2 unresolved.

## Stage 4A worker implementation started

Arthur asked this worker to implement Stage 4A only: PIO `GPIOBASE`, physical
GPIO high-bank sampling, serial GPIO high-bank merge, and the threaded mirror.
I read `CLAUDE.md`, the Stage 4 HLD, and the local AGENTS instructions from
the prompt. The repo has concurrent untracked fixture/docs artifacts and this
journal is already modified, so I will append only and avoid commits.

Initial read: `PioBlock` currently treats offsets `0x128..=0x168` as
unmodelled, serial `tick_peripherals` samples only `Bus::gpio_in`, serial
`update_gpio` composes only the low 32 pins, and threaded execution has high
external stimulus for `GPIO_HI_IN` reads but no internal `in_hi` latch for PIO
sampling or PIO output merge. The implementation should stay small: no u64
state-machine internals, just projection at the block and bus boundaries.

Emotional temperature: focused but mildly tense. The threading side has more
shared-state seams than the serial path, and I want to keep the metadata
plumbing narrow enough that Stage 4B does not inherit avoidable complexity.

## Stage 4A implementation completed

Implemented the Stage 4A slice in `picoem-common` and `rp2350-emu` only:
per-block PIO `GPIOBASE` storage at offset `0x168`, physical-to-local GPIO
sampling helpers, low/high physical pad projection, serial `gpio_in_hi`
merge, and threaded high-bank input/pad snapshots. The devil's-advocate
blockers were incorporated before final validation: threaded PIO workers now
publish already-mapped physical pads for both `pad_out` and `pad_oe`, and the
threaded GPIO state has a merged `in_hi` path that feeds PIO sampling.

Validation completed with focused tests, package clippy, package tests, and a
broad workspace `cargo test -q`. The broad `cargo fmt` touched unrelated
harness files because other Stage 4 work is present in the same checkout; I
manually removed the textual rustfmt churn from those out-of-scope files and
did not touch picoem-harness behavior. Git still reports some out-of-scope
paths as modified even when their content hash matches HEAD, which looks like
line-ending/stat bookkeeping rather than a textual diff.

Emotional temperature: relieved but still cautious. The implementation feels
appropriately small for Stage 4A, but the handoff to Stage 4B should be
careful because the oracle still needs to consume the new 48-pin surface
without reintroducing local-vs-physical pin confusion.

Final command pass after cleanup: scoped `cargo fmt --check` for
`picoem-common` + `rp2350-emu`, focused serial/threaded `gpiobase` and
`gpio_in_hi` tests, scoped clippy with `threading`, and broad workspace
`cargo test -q`. No commits made.

## Stage 4A review feedback application

Arthur asked for the Stage 4A feedback worker, explicitly leaving
`picoem-harness` to Stage 4B and forbidding commits. I used the
`code-review-apply-feedback` skill, reread `CLAUDE.md`, the Stage 4 HLD, and
the active Stage 4A diff before editing.

Feedback evaluation:

1. Agree: threaded PIO readback published CTRL/IRQ but not `GPIOBASE`, so a
   worker-owned `PioBlock` could apply `WriteReg(0x168)` while CPU-side MMIO
   readback still returned zero. I added a published per-block `gpio_base`
   atomic to `ThreadedPio`, seeded it during handoff, updated it after generic
   PIO writes, and routed `WorkerBus` reads of `PIOx + 0x168` to it.
2. Agree: the Stage 4A threaded GPIO high/low model used separate atomics for
   low/high input and low/high PIO pad halves. That is enough for old low-bank
   paths but not for a `GPIOBASE=16` PIO-local window spanning physical
   GPIO16..47 while coordinator and PIO workers overlap. I changed the merged
   input to one packed 64-bit snapshot and changed physical PIO pad publication
   to a small seqlock over packed out/oe low/high banks.

Emotional temperature: calmer after reading the actual paths. The feedback is
well targeted; the only tension is keeping the coherent snapshot fix small
without pretending a 128-bit hardware snapshot exists in Rust atomics. The
seqlock is a little more machinery than I like, but it preserves the existing
out/oe atomicity contract and fixes the cross-bank tear directly.

Validation after feedback application:

- `cargo fmt -p picoem-common -p rp2350-emu` passed.
- Focused `gpiobase`, `gpio_in_hi`, `physical_pads`, `pio_published`, and
  `in64` tests passed with `--features threading` where applicable.
- `cargo clippy -p picoem-common -p rp2350-emu --features threading
  --all-targets -- -D warnings` passed.
- `cargo test -q -p picoem-common -p rp2350-emu --features threading` passed.
- Broad `cargo test -q` passed.
- Broad `cargo clippy --all-targets -- -D warnings` passed.
- Broad `cargo fmt --check` still fails on out-of-scope `picoem-harness`
  formatting diffs; I did not apply those because Arthur explicitly scoped
  this worker away from Stage 4B/harness files. A scoped `cargo fmt --check -p
  picoem-common -p rp2350-emu` passed.

No commits made.

## Stage 4B implementation worker notes

Arthur asked this worker to implement Stage 4B only: fixture-aware
ServingOracle address-shape matching. I reread `CLAUDE.md`, the Stage 4 HLD,
the current git status/diff, and the PIO serving oracle before editing. The
worktree already had status noise in several harness files and this journal,
but `onerom_serving_oracle.rs` had no textual diff at handoff, so I treated it
as owned for this slice.

Implemented a small local `PioAddressShape` in the oracle. It derives
`gpio_base=0,count=16` for fire-24-a and `gpio_base=16,count=19` for fire-32-a
from `FixtureSpec.addr_pins`, then computes the full expected
`SHADOW_BASE | pio_window_bits(stim)` address. `run_case`,
`try_evaluate_conclusive`, and `evaluate_case_trace` now match pushes by that
full resolved address instead of `hi16 == 0x2000` plus a truncated low-16
stimulus.

Added focused unit tests for the fire-24 shape, fire-32 shape, low-16
truncation guard, fire-24 mismatch rejection, and a synthetic fire-32 trace
with PIO-local address bits 16/17/18 set. Literal/raw `Case` behavior remains
covered by the existing compose-level and literal-mask tests.

Emotional temperature: focused and a bit impatient with the old halfword
predicate. The good news is this stage stayed as small as the HLD intended:
no fixture framework, no bytecode sniffing, and no Stage 4C cleanup.

## Stage 4B devil's-advocate feedback applied

Arthur forwarded Stage 4B challenge findings before finalization. I agreed with
the coverage concerns. The first implementation had a fire-32 high-bit
evaluator test, but it used `Case::from_raw`, which proved the evaluator more
than the production case path. I switched that coverage to `Case::from_addr`
against the real fire-32 fixture and added explicit assertions that parsed
GPIO32/33/34 map into PIO-local resolved bits 16/17/18.

Also tightened the shape derivation so 32-pin fixtures must actually include
the high PIO window, added a fire-24 overlay-vs-raw expected-address regression,
covered `ResolvedAddrOutOfRange` for a matching push beyond a deliberately
small shadow, and fixed the report pattern formatter to keep u64 GPIO patterns
visible.

Emotional temperature: better. The critique was right that the raw-case test
could have let a production `from_addr` regression slip through. The revised
tests now pin the real fixture parser, case composer, shape helper, and
evaluator together without broadening Stage 4B.

## Stage 4B implementation handoff

`Wegener` completed Stage 4B and reported a narrow diff in
`onerom_serving_oracle.rs` plus this journal. The implementation adds a local
fixture-derived `PioAddressShape`, replaces the `hi16 == 0x2000` plus low-16
stimulus predicate with full expected resolved-address matching, and adds tests
for fire-24/fire-32 shapes, GPIO32/33/34 mapping, low-16 truncation, raw versus
non-literal overlay behavior, out-of-range matching pushes, and u64 pattern
diagnostics.

Validation reported by the worker: focused serving-oracle tests, harness
clippy, harness tests, and broad `cargo test -q` pass. The fire-32 smoke binary
now reports the important oracle counters clean (`PASS=4096`, `wrong=0`,
`no_resolve=0`, `no_stable=0`, `addr_oor=0`) but exits nonzero due to an
internal smoke accounting mismatch: `pass count 4096 != servable_seen 4097`.
I am treating that as a Stage 4C acceptance item unless code review identifies
it as a Stage 4B regression.

Emotional temperature: encouraged. The important failure mode from the previous
session, `NoResolve` on high fire-32 address bits, appears closed in the smoke
counters. The remaining issue is no longer semantic resolution; it is whether
the acceptance binary's accounting matches the new filter behavior.

## Stage 4B review

`Linnaeus` reviewed Stage 4B and found no blockers. They confirmed the old
`hi16 == 0x2000` / low-16 production matcher is gone, `Case::from_addr` versus
`Case::from_raw` overlay behavior remains correct, and the tests exercise
GPIO32/33/34 plus offsets above `0xFFFF`. The only non-blocking note is that
`PioAddressShape::from_fixture` is deliberately strict and may assert on
malformed shapes; that is acceptable for the current in-tree fixtures.

The reviewer classified the smoke failure as an existing binary off-by-one:
`servable_seen` increments before the smoke-cap break, so a cap of 4096 reports
4097 processed after running 4096 cases. That moves to Stage 4C acceptance
cleanup.

## Stage 4B committed

Committed Stage 4B as `60778bb` (`fix: generalize onerom pio address
matching`) with only `onerom_serving_oracle.rs` and this journal. After commit,
`git diff --name-only` is clean; `git status` still shows older harness files as
modified with no textual diff, plus pre-existing untracked fixture/docs
artifacts. I will keep avoiding those unless Stage 4C explicitly needs them.

## Stage 4C delegated

Spawned `Gibbs` as Stage 4C cleanup/acceptance worker and `Hegel` as the
Stage 4C devil's-advocate reviewer. Scope is smoke accounting, acceptance
status text, top tech-debt cleanup, and validation; no broad oracle/emulator
refactor.

`Hegel` returned first with useful guardrails. The fire-32 fixture binary is
currently untracked but required by the default binary and tests, so Stage 4C
must explicitly decide whether to include it. The smoke fix should use separate
"processable/processed" counters instead of weakening success. Documentation
should not claim completion until full fire-32 and fire-24 gates pass. I
forwarded those findings to `Gibbs`.

## Stage 4C cleanup and acceptance

Arthur asked for the Stage 4C cleanup worker and then forwarded
devil's-advocate findings before finalization. I agreed with the important
counter critique: the smoke fix should make the processed-case counter explicit
instead of merely moving the old `servable_seen` check. I changed the binary to
use `servable_processed`, incremented only after the smoke cap admits a case,
and added a full stride-1 accounting guard that expects 262144 processed
servable cases and 262144 unservable skips.

Validation after the counter fix:

- `seabios32_fixture_byte_correct -- --smoke`: PASS=4096, wrong=0,
  no_resolve=0, no_stable=0, addr_oor=0, exit 0.
- `seabios32_fixture_byte_correct`: PASS=262144, wrong=0, no_resolve=0,
  no_stable=0, addr_oor=0, unservable_skipped=262144, exit 0.
- `seabios_fixture_byte_correct`: grand total pass=131072, wrong=0,
  no_stable=0, not_driven=0, latency_oor=0, exit 0.

Documentation cleanup is now justified: I replaced the stale partial-status
block in the fire-32 binary and rewrote the top `tech_debt.md` entry as
resolved with the actual validation counters. The fire-32 fixture binary is
still untracked but required by both defaults and tests; the JSON config is
provenance for regenerating it, not a runtime dependency.

Emotional temperature: relieved. The semantic matcher was already clean from
Stage 4B; the remaining work was mostly making the accounting honest and then
earning the right to delete the old partial warning with a full sweep.

## Stage 4C review

`Newton` reviewed the Stage 4C diff and found no blocking code issues. The
review confirmed that the accounting now separates `servable_processed` from
unservable skips and still fails on any wrong/no-resolve/no-stable/out-of-range
counter. Full stride-1 mode explicitly expects `262144` processed servable
cases and `262144` unservable skips. The review also confirmed the binary
fixture is required for fresh checkouts/tests, and the JSON provenance file is
safe to include.

Commit content for Stage 4C should therefore be explicit: the seabios32 binary,
`tech_debt.md`, the required fire-32 fixture `.bin`, its source JSON provenance,
and this journal. I will not include the other no-text-diff harness status noise
or older untracked docs/journals.

## Final audit round

Spawned `Singer` as the final plan-compliance auditor and `Planck` as the final
devil's-advocate auditor. Both found that the core behavior is complete: fire-32
smoke and full sweep pass with all failure counters zero, fire-24 regression
passes, clippy/tests/threading tests pass, and Stages 4A/4B/4C functionally
match the HLD.

They did not allow a 100% verdict yet. Remaining cleanup items:

- `cargo fmt --check` fails, which is an explicit HLD completion gate.
- A stale lower `tech_debt.md` section still describes
  `ResolvedAddrOutOfRange` as unreachable because of the old
  `hi16 == 0x2000` predicate.
- The untracked fixture-generalization HLD is referenced by committed files and
  should be release-contained.
- The fire-32 fixture provenance should live in tracked docs, not only in an
  untracked journal.
- `PioAddressShape::from_fixture` is stricter than the HLD sketch; evaluate
  whether a small addr-pin-derived implementation is better or whether the
  strictness is intentional enough to leave.

Spawned `Galileo` as a final audit-feedback cleanup worker using the
`code-review-apply-feedback` process. Emotional temperature: a little annoyed
but aligned. The feature works; the remaining issues are release hygiene and
documentation truthfulness, and those are worth closing rather than hand-waving.

## Final audit feedback applied

`Galileo` applied the final audit feedback without committing. Reported fixes:
ran `cargo fmt` so the workspace formatting gate passes, kept the referenced
fixture-generalization HLD as a required artifact, documented fire-32 fixture
provenance in the tracked fixtures README, removed stale tech-debt reasoning
about `hi16 == 0x2000`, and changed `PioAddressShape` to derive `GPIOBASE` from
`FixtureSpec.addr_pins` while preserving the one-window assertion.

Validation reported: `cargo fmt --check`, targeted `pio_address_shape` tests,
`cargo clippy`, `cargo test -q`, `cargo test -q --features threading`, and
`git diff --check` all pass. Next step is a review of this final cleanup before
I commit it.

## Final cleanup review

`Ampere` reviewed the final cleanup and found no code blockers. They confirmed
the `PioAddressShape` derivation now follows `FixtureSpec.addr_pins`, the
full-width evaluator behavior is intact, rustfmt-only changes are mechanical,
fixture hashes in the README match the files, and the full validation gates
still pass.

One release-packaging issue remained: the fixtures README and fixture
generalization HLD reference `wrk_journals/2026.05.04 - JRN - Fire-32-a SeaBIOS
Firmware Build.md`, so that journal must be included or the provenance chain is
not release-contained. I also reviewed the historical fixture-generalization
implementation journal; it is part of the same OneROM fixture-generalization
chain and references the same HLD/predecessor. I will include the HLD and the
related OneROM journals explicitly in the final cleanup commit rather than
leaving dangling untracked artifacts.
