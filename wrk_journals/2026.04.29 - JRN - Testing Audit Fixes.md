# JRN — Testing Audit Fixes

**Date:** 2026-04-29 (evening)
**Predecessor:** `wrk_docs/2026.04.29 - Testing Audit and Risk Review.md`
**Supervisor:** Claude Opus 4.7 (1M ctx)
**Branch:** main

## Goal

Act on the testing audit. Fix the concrete issues surfaced by the audit + V2 mutation triage. Per Arthur:
- Each workstream gets an HLD up-front; supervisor signs off before code starts.
- Iterate on each design until supervisor is happy.
- Agents use `/code-reviewer` + `/code-review-apply-feedback`.
- Journal updated regularly.

## Candidate workstreams

From the audit's revised priority list:

| # | Workstream | Size | Status |
|---|---|---|---|
| 1 | BFC width=32 UB shift @ `execute_thumb32.rs:349` | small (surgical) | candidate |
| 2 | Coresight PPB read16 dispatch @ `bus/mod.rs:2123` | small-medium | candidate |
| 3 | Per-function oracle routing in V2 mutation runner | medium | candidate |
| 4 | RP2040 NVIC plumbing (`silicon_isr_diff_rp2040` decoration) | large | recon-first |
| — | M0+ T32 randomised generator | small | **in flight by another agent — skip** |
| — | V2.1 deep-pass at fuzz=2000 | run-only | **in flight — skip** |
| — | M0+ T32 filter relaxation | small | **already landed 2026-04-29 — skip** |

## Plan

1. **Recon** — inspect the actual code state for #1 / #2 / #4 to confirm the bugs are still present and to scope #4 honestly.
2. **HLD round** — dispatch HLD-writing agents in parallel (one per scoped workstream).
3. **Review** — supervisor reviews each HLD, iterates until happy.
4. **Implementation round** — dispatch implementation agents in parallel; each uses `/code-reviewer` and `/code-review-apply-feedback`.
5. **Validation** — supervisor verifies tests pass, commits gated on Arthur's approval.

## Log

### Recon (complete)

- BFC bug at `crates/mdrp2350/src/core/execute_thumb32.rs:349`: confirmed; same UB at SBFX line 340 and (per HLD agent) UBFX line 389.
- Coresight `debug_assert!` at `bus/mod.rs:2123` (read16) and 2320 (write16): confirmed; HLD agent found same shape at `read32:2770` and `write32:2909`. Fix is 4-site, not 2-site.
- RP2040 NVIC plumbing (ISER/ICER/ISPR/ICPR, `tick_peripherals`, `try_take_any_pending_exception`): **already landed**. Coverage agent's claim that it wasn't was stale. Skipping that workstream.
- M0+ T32 randomised generator: **in flight by another agent** (`2026.04.29 - JRN - M0+ T32 Randomised Fuzz Generator.md`). Skipping.
- V2.1 deep-pass: **in flight, run-only**. Skipping.

Three workstreams launched.

### HLD round (complete)

Three HLD-writing agents dispatched in parallel. All returned with HLDs and explicit open questions. HLD paths:
- `wrk_docs/2026.04.29 - HLD - BFC SBFX UBFX Width 32 Fix V1.md`
- `wrk_docs/2026.04.29 - HLD - Coresight Halfword Assert Fix V1.md`
- `wrk_docs/2026.04.29 - HLD - V2 Per-Function Oracle Routing V1.md`

### Supervisor sign-off (decisions on open questions)

**HLD A — BFC/SBFX/UBFX width=32 fix.** SIGNED OFF.
- Q1 (u64 vs branch): u64 widening (Option 2). Rationale: branchless, defensive, matches existing `i64`/`u64` widening pattern in adjacent USAT code at `:367`.
- Q2 (corpus extension): defer. Note the gap; don't fix in this work-package.
- Q3 (helper): no helper — three sites is below the abstraction threshold.

**HLD B — Coresight halfword assert.** SIGNED OFF.
- Recommendation accepted: Option 1 (extend assert exemption to `is_coresight_trace`), 4-site symmetric edit (read16/write16/read32/write32).
- Q1 (dead-arm cleanup option (c) — make wrappers route coresight to bus): **defer to a separate tech-debt entry**. This HLD's job is to unblock V2; behaviour-changing refactor of coresight access is a separate work-package. Add a tech-debt note in Stage E pointing at this concern.
- Q2 (read32/write32 sibling tests): write16 sibling only. Smallest change. read32/write32 follow-up if needed.
- Q3 (assert message wording): reword to "PPB or coresight aperture address" so the message stays accurate. Apply in Stage A, not Stage C.

**HLD C — V2 per-function oracle routing.** SIGNED OFF with one explicit deferral.
- Q1 (sidecar location): `scripts/v2_oracle_routing.json` — approved.
- Q2 (mixed unavailable+survived aggregation): `oracle_survived` (we did measure once). Approved — keeps consumer schema simple.
- Q3 (function field 99.6 % reliability): module-scope fallback to file default is fine forever. No source-walk-back.
- Q4 (smoke probe budget): bump from 5 s → **10 s** for headroom on slow hosts; the broken-QEMU path EAGAINs in <3 s so fail-fast is preserved.
- Q5 (operator overrides): keep `--allow-fpu` / `--no-fpu`; default per smoke-probe; no per-route timeout cap (loud failure on `--allow-fpu` against broken env is correct).
- Q6 (cross-port race when FPU class is healthy): **defer.** On the current host the FPU class smoke probe fails → `oracle_unavailable` → no race materialises. When the FPU env is fixed, a separate work-package adds launcher cross-port coordination (`--owned-oracle` flag or re-sharding). For V1, document this in HLD §10.6 as a follow-up and put a `# TODO V2.x: cross-port coordination when fpu_class healthy` comment in the runner near the dispatch site.

### Implementation round — partial completion + supervisor finish

Three implementation agents launched in parallel. **All three hit Anthropic's per-account rate limit at ~21:00 BST** before completing the full stage plan. Partial state:

- **A (BFC):** Stage A complete — three-site u64-widening edits at `execute_thumb32.rs:340/349/389`, four tests added/re-enabled in `tests.rs::stage2_thumb32_residue`, `Cargo.toml` 0.1.0 → 0.1.1.
- **B (Coresight):** Stage A complete — four-site assert exemption at `bus/mod.rs:2123/2320/2770/2909` plus reworded "PPB/coresight-aperture-bypass" message, `coresight_trace_halfword_write_dispatches_through_word_path` sibling test added.
- **C (V2 routing):** Runner code + test file written, **JSON sidecar missing**, README not updated.

After 21:00 the supervisor completed C's remaining stages directly (sidecar `scripts/v2_oracle_routing.json` with 7 default + 44 per-function entries, README updated for the new flags / smoke probe / aggregation rules / cross-port caveat).

### Validation

| Check | Result |
|---|---|
| `cargo test -p mdrp2350 stage2_thumb32_residue` | 48 pass, 0 fail (BFC/BFI/SBFX/UBFX width=32 all green) |
| `cargo test -p mdrp2350 coresight_trace` | 4 pass, 0 fail (read16 + write16 sibling now pass) |
| `cargo test -p mdrp2350 --lib` | **3123 pass, 0 fail, 30 ignored** |
| `cargo build --release -p mdrp2350` | Clean (2.25 s) |
| `python3 -m unittest scripts.test_v2_routing` | 23 pass |
| `python3 -c "json.load(...routing.json)"` | OK, `version == 1` |

### Code review (workstream-spanning)

Spawned a code-reviewer sub-agent over all three workstreams together. **Verdict: APPROVE_WITH_CHANGES.** No required changes. Suggestions applied:

1. Added `oracle_unavailable` to the classification print-list in `scripts/v2_mutation_summary.py:50-51` (otherwise the new outcome would be silently dropped from the "By classification" section).
2. Extracted `_full_candidates(mutant, routing)` helper in `scripts/v2_mutation_runner.py` so `_make_unavailable_routes` and the dispatch site share one source of truth (also fixed an `or`-fallback edge case where `[]` would have triggered the wrong branch — now explicit `is None`).

Suggestions deferred (logged here for future reference):
- BFI underflow when `lsb > msb` (architecturally UNPREDICTABLE; out of scope per HLD A §6).
- `process_mutant` size (~110 lines, on the upper edge but readable).
- Python typing-imports mix (`Optional` from typing vs `set[str]` builtin generics — acceptable, no project-wide style decision).
- `__post_init__` for legacy back-compat fields on `Result` (single construction site, not worth it).

Re-ran V2 tests after the refactor — 23/23 still green.

### State at end of session

Working tree: A, B, C edits + supervisor's C completion + summary-script + runner refactor — all uncommitted, awaiting Arthur's commit approval.

Files touched:
- `crates/mdrp2350/src/core/execute_thumb32.rs` — 3 line-edits (A)
- `crates/mdrp2350/src/bus/mod.rs` — 4 site-edits (B)
- `crates/mdrp2350/src/tests.rs` — 5 new tests (A: 4, B: 1) + 1 re-enable (A)
- `crates/mdrp2350/Cargo.toml` — version bump 0.1.0 → 0.1.1
- `scripts/v2_mutation_runner.py` — extensive (HLD §3.2 / §3.3 / §3.6 / §3.7 / §5)
- `scripts/v2_oracle_routing.json` — NEW (51 entries)
- `scripts/test_v2_routing.py` — NEW (23 tests)
- `scripts/v2_mutation_summary.py` — 1 line edit (oracle_unavailable in print list)
- `scripts/MUTATION_V2_README.md` — operator notes for the routing JSON, smoke probe, new CLI flags

Tech-debt entries (Stage E) **not** modified — that's gated on Arthur's commit approval and falls in scope for the next session.


