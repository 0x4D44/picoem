# JRN — M0+ T32 Randomised Fuzz Generator

**Date:** 2026-04-29
**Predecessor:** `wrk_docs/2026.04.28 - HLD - QEMU M0+ Thumb-32 Oracle Coverage V1.md` §7 follow-up.
**Starting commit:** `e2f382a`
**Branch:** main
**Supervisor:** Claude Opus 4.7 (1M context)

## Context

The V1 work-package (commits `e02b070`..`0d31d5a`) closed the QEMU oracle filter gap for the M0+ Thumb-32 subset, but the actual fuzz path stayed Thumb-16 only. Differential signal for `execute_wide.rs` came from 10 hand-curated cases — too thin for catching encoding-edge bugs (e.g. BL imm21 sign-extension off-by-one near ±2^20). This iteration adds a randomised generator that produces M0+-subset T32 cases the relaxed filter admits, so `--fuzz N` becomes a real differential against QEMU `cortex-m0`.

No formal HLD: the work is small (one function ~80 LOC, one call-site, ~6 unit tests) and design decisions are constrained. This journal entry is the contract.

## Design (greenlit)

**New function** `generate_fuzz_m0plus_t32(count, rng) -> Vec<TestCase>` in `crates/mdpicoem-harness/src/thumb32_gen.rs`. Round-robin/random pick among four shapes:

| Shape | Random parameters | Encoder |
|---|---|---|
| BL T1 | imm21 ∈ ±2^20 (full 21-bit signed range, valid encodings only) | `enc_t32_bl` |
| MSR T1 | Rn ∈ 0..=12, sysm ∈ {0, 8, 9, 16, 20} | `enc_t32_msr` |
| MRS T1 | Rd ∈ 0..=12, sysm ∈ {0, 8, 9, 16, 20} | `enc_t32_mrs` |
| Barriers | DSB / DMB / ISB, option ∈ 0..=15 | inline (hw0=0xF3BF, hw1=0x8F0X) |

**SYSm set** — {0 (APSR), 8 (MSP), 9 (PSP), 16 (PRIMASK), 20 (CONTROL)}. The filter admits a wider range but these are the SYSm fields M0+ firmware actually reaches. Closes the V1 §7 gap on MSR CONTROL + MSP/PSP coverage.

**Filter discipline.** Every generated `TestCase` must satisfy `is_m0plus_safe(&case)`. Asserted in unit tests. No wasted fuzz cycles.

**Integration.** Add the call to `generate_fuzz_classes` in `crates/mdpicoem-harness/src/lib.rs:~4970` alongside the existing `generate_fuzz_t32_alu` / `_t32_mem` / `_fpu` calls. Per-class N expansion: `--fuzz 10000` adds ~10k cases on top of the existing ~127k → ~137k. Acceptable.

## Out of scope

- Mask variation in MSR (`enc_t32_msr` mask=0b10 hardcode stays — separate item).
- Per-case `[T32 PASS]` print in `run_targeted` (separate item).
- Curated cases in `gen_t32_branch` / `gen_t32_misc_control` — untouched.
- M33 / mdrp2350 generators.

## Plan

| Stage | Agents | Goal | Commit |
|---|---|---|---|
| 0 | Supervisor | Journal commit | `wrk_journals: M0+ T32 randomised fuzz generator plan` |
| A | Implementation (general-purpose, TDD) | Generator + integration + unit tests | `harness: randomised M0+ T32 fuzz generator` |
| B | code-reviewer + devils-advocate (parallel) | Independent review | (none) |
| C | code-review-apply-feedback | Address review findings | `harness: address Stage A review feedback` (if any) |
| D | Validation | cargo test, smoke, `--fuzz 10000`, `--fuzz 100000` | (none — runs only) |
| E | Completeness + devils-advocate (parallel) | Confirm 100% per this journal contract | (none unless gap found) |

## Log

### Stage 0 — journal commit (`2956a2b`)
Done. Starting state captured at `e2f382a`.

### Stage A — TDD implementation (agent `a707107f`)

Two files changed (+370 LOC):
- `thumb32_gen.rs`: `M0PLUS_SYSM_ADMIT` const, four private helpers (`fuzz_m0plus_bl`, `_msr`, `_mrs`, `_barrier`), `rand_bl_offset_21bit_even`, public `generate_fuzz_m0plus_t32`. Round-robin order BL→MSR→MRS→barrier (so 4× count cases) for stable seeded reproduction.
- `lib.rs:4975–4983`: one `base_alu.extend(thumb32_gen::generate_fuzz_m0plus_t32(...))` call inside `generate_fuzz_classes`.

**TDD red:** 5 unresolved-function compile errors before implementation.
**TDD green:** 6/6 new tests pass (`bl_offsets_admit_filter`, `msr_sysm_in_admit_set`, `mrs_sysm_in_admit_set`, `barriers_admit_filter`, `mixed_distribution`, `deterministic_with_seed`).

**Cross-checks:**
- `cargo test --bin qemu_diff_m0plus`: 9/9 (existing filter tests unbroken).
- `cargo build --release`: clean.
- Clippy: new code clean. Pre-existing `type_complexity` in `generate_fuzz_fpu` ignored per instructions.

**Agent decisions / observations worth noting:**
- BL offset kept at 21-bit signed (per journal contract). Encoder actually accepts 25-bit, but 21-bit is unambiguously inside the safe envelope.
- Distribution chose deterministic round-robin over random shape pick — strengthens `deterministic_with_seed` and guarantees no-shape-starvation in `mixed_distribution` test without probabilistic flake.
- **Per-fuzz scaling.** New generator adds 4 × count_per_class cases per class. `--fuzz 10000` will add ~40k T32 cases on top of the existing ~127k; `--fuzz 100000` will mean ~1.67M total (vs. ~127k before). Wall time for Stage D may need adjustment.

Pending Stage B review.

### Stage B — review (parallel agents `a15170c9` code-reviewer, `a86534518a7c59366` devils-advocate)

**Code-reviewer:** clean overall. No required changes. Suggestions: the BL 21-bit choice abandons the 22..24-bit XOR-boundary surface; barrier op narrowing below the filter needs an explanatory comment; tests duplicate the sysm admit literal instead of reusing `M0PLUS_SYSM_ADMIT`; one tautological assertion.

**Devils-advocate:** confirmed the bones are right but called the contract overstated. Six substantive concerns:
- **C3** — uniform random sampling at 21-bit hits exact boundary values ~2^-20 per draw; the journal's stated motivation ("BL imm21 sign-extension off-by-one near ±2^20") is structurally untargeted without curated boundary cases. **Blocker.**
- **C4** — encoder accepts 25-bit signed; 21-bit silently drops the s/I1/I2 XOR boundary surface near ±2^24.
- **C6** — MSR sysm=0 (APSR) cases use `MASK_NO_FLAGS`, masking out exactly the bits that APSR write changes. APSR-write differential is structurally blind. **Blocker.**
- **C1/C2** — "round-robin" is misnamed; implementation is block-sequential. `mixed_distribution` test is tautological as a result.
- **C5** — unit tests re-derive filter predicates inline; drift between them and `is_m0plus_safe` is undetected.
- **Wall-time scaling** — generator emits 4×count cases, so `--fuzz 100000` would mean ~2h11min vs the 30 min the journal promised.

Supervisor decision: take all six as Stage C. Code-reviewer nits also bundled.

### Stage C — apply Stage B feedback (agent `acb0fb03`)

Single file changed: `crates/mdpicoem-harness/src/thumb32_gen.rs` (+161 / −38).

- **C3** — added `boundary_bl_cases()` returning 15 curated TestCases at offsets {0, ±2, ±2^20, ±(2^20−2), ±2^21, ±(2^21−2), ±2^24, ±(2^24−2)}. Prepended to the BL stream unconditionally.
- **C4** — `rand_bl_offset_21bit_even` → `rand_bl_offset_24bit_even`, range now ±2^24 even-only. Half-the-encoder-max as a safe envelope; the boundary cases pin the explicit edges.
- **C6** — `fuzz_m0plus_msr` now sets `MASK_ALL_FLAGS` for sysm=0 (APSR), `MASK_NO_FLAGS` for others. New test `msr_apsr_uses_flag_mask` asserts both branches.
- **C7** — generator now emits `count` total (`count/4` per shape) + 15 boundary BL prefix. `--fuzz 100000` returns to the original ~30 min budget.
- **C1/C2** — doc-comments rephrased "block-sequential" (concrete) instead of "round-robin" (wrong).
- **C5** — module doc-comment on `mod m0plus_t32_fuzz_tests` flags the drift-risk explicitly.
- **Code-reviewer nits** — barrier-op narrowing comment added; tests use `M0PLUS_SYSM_ADMIT`; tautology dropped.

**Test outcomes:**
- `cargo build --release`: clean.
- `cargo test --lib m0plus_t32_fuzz`: 7/7 (old 6 + new `msr_apsr_uses_flag_mask`).
- `cargo test --lib thumb32_gen`: 16/16.
- `cargo test --bin qemu_diff_m0plus`: 9/9.

Pending Stage D validation against QEMU.

### Stage D — validation (agent `a2028622`) — bugs found

Validation working as designed. Generator surfaces 2,277 real bugs in `crates/mdrp2040/src/core/execute_wide.rs` MSR/MRS sysm handling.

**Smoke (no `--fuzz`):** 392/392 passed. Curated path unbroken.

**`--fuzz 10000 --seed 1`:** 137,561 cases (V1: 127,513, +10,048 = matches contract). 135,284 passed, **2,277 FAILed**. Wall ~12 min. Failure surface is finite and bounded by `(Rn × sysm)` cardinality — bug count plateaus around case ~60k.

**FAIL classification (all class-(a) — real `execute_wide.rs` bugs):**

| Shape | FAILs | Bug |
|---|---|---|
| MSR sysm=0 (APSR write) | 261 | Emulator drops Q flag (bit 27). xPSR diff mask `0xf8000000` shows only 4 bits written, not 5 (NZCVQ). |
| MSR sysm=8 (MSP write) | 458 | Emulator writes MSP unconditionally; QEMU rejects in this privilege context. SP-bank divergence. |
| MSR sysm=9 (PSP write) | 236 | Same shape as MSP. |
| MSR sysm=20 (CONTROL write) | 347 | CONTROL write side-effect: emulator does SP-bank switch unconditionally, QEMU does not. |
| MRS sysm=8 (MSP read) | 457 | Returns 0 instead of actual MSP value (e.g. `0x20001000`). |
| MRS sysm=9 (PSP read) | 518 | Same shape as MSP. |
| MSR sysm=16 (PRIMASK) | 0 | Pre-existing curated coverage. Works. |
| MRS sysm=0/16/20 | 0 | Pre-existing curated coverage. Works. |
| BL (curated 15 + ~2,500 random) | 0 | The journal-motivated imm21 sign-extension bug class is **not present**. |
| Barriers (DSB/DMB/ISB) | 0 | Clean. |
| Thumb-16 | 0 | No regression. |

**`--fuzz 100000`:** not run, gated on previous step's clean signal per protocol.

**Stage D acceptance:** the generator does what we wanted (non-zero T32 differential signal, found real bugs); the journal's clean-fuzz acceptance criterion is unmet because the bugs exist. **This is success at finding bugs and failure at establishing zero-diff acceptance.** Supervisor escalation point: fix the executor in this iteration, defer the fixes, or close the iteration with bugs documented and open a separate fix work-package.

Validation outputs:
- `/tmp/qemu_m0plus_smoke_v2.log`
- `/tmp/qemu_m0plus_fuzz10k_v2.log` (317 KB, full FAIL detail)
- `/tmp/qemu_diff_m0plus_v2_fuzzer` (binary preserved for `--seed 1` repros)

**Supervisor decision: option 1 — fix the bugs in this iteration.** The whole point of differential fuzzing is to find AND fix bugs; closing the iteration with bugs documented is half the value.

### Stage E plan

| Sub-stage | Goal | Commit |
|---|---|---|
| E.1 | TDD triage + fix in `execute_wide.rs` (per-bug-class repro tests in `mdrp2040/tests.rs`, then fixes) | `mdrp2040: fix M0+ MSR/MRS sysm handling` |
| E.2 | Re-run smoke + `--fuzz 10000`. Expect 0 FAILs | (none — runs only) |
| E.3 | Code-review + devils-advocate (parallel) | (none) |
| E.4 | Apply review feedback if any | conditional |
| E.5 | `--fuzz 100000` final acceptance | (none) |
| E.6 | HLD-completeness + devils-advocate cross-check | (none) |

### Stage E.1 — TDD triage + fix (agent `a647bc58`)

**Verdict reversal.** Of the six FAIL classes, only **two are genuine mdrp2040 bugs** (B5, B6 — MRS reads of MSP/PSP). The other four (B1 Q-flag, B2/B3/B4 MSR writes for MSP/PSP/CONTROL) are **QEMU `cortex-m0` non-spec-compliance**:

- **B1 (sysm=0 / APSR write).** ARMv6-M B1.4.2 defines APSR as NZCV only (bits 31:28). The Q flag (bit 27) is an ARMv7-M Mainline addition. QEMU's `cortex-m0` is a stripped cortex-m3 and incorrectly exposes Q. mdrp2040's APSR write at `execute_wide.rs:151` uses mask `0xF000_0000` — spec-correct.
- **B2/B3/B4 (MSR writes for sysm=8,9,20).** Per ARMv6-M B5.2.3, MSR MSP/PSP writes the named bank with `Rn[31:2]:'00'`; if it's the active bank, R13 retargets. The fuzz log shows QEMU is **inconsistent both ways** — for sysm=8 (MSP) with MSP active QEMU usually leaves R13 alone (against spec); for sysm=9 (PSP) with MSP active QEMU updates R13 (also against spec). mdrp2040's existing logic at `execute_wide.rs:155–186` is spec-correct (existing tests at `tests.rs:3964–4031` already pin this).
- **B5 / B6 (MRS reads of MSP/PSP).** Genuine bugs. The architectural MSP/PSP value lives in `r[13]` when that bank is active and in `regs.msp` / `regs.psp` when banked. The MRS executor was reading `regs.msp` / `regs.psp` unconditionally, returning 0 when the requested bank was the active one. **Fixed** at `execute_wide.rs:211–228` to mirror the existing write-side asymmetry: return `r[13]` when the requested bank is active, else the banked field.

**TDD outcome:**
- 8 new tests in `crates/mdrp2040/src/tests.rs` mod `m0plus_msr_mrs_fixes`.
- TDD red: 2/8 fail pre-fix (B5, B6 reproducers). The other 6 pass pre-fix because they pin spec-correct behaviour mdrp2040 already had.
- TDD green after fix: 8/8.
- Full mdrp2040 lib test suite: 1034/1034 pass — no regressions.
- `cargo test --bin qemu_diff_m0plus`: 9/9.
- `cargo build --release`: clean.

**Out of scope:** `registers.rs` was not touched (banked storage was already correctly designed; only the MRS read-side asymmetry needed fixing).

**Foreign files in working tree:** `crates/mdrp2040/src/bus/mod.rs` and `crates/mdrp2040/src/threaded/bus.rs` modified by concurrent agents. Supervisor leaves them alone, stages only the authored files.

### Stage E.1.5 — harness-side mitigation (planned)

Stage E.1's verdict means we still need to make the fuzz generator clean. Harness-side fix:
- Drop MSR sysm ∈ {8, 9, 20} from `fuzz_m0plus_msr` (QEMU diverges; curated unit tests in `mdrp2040/tests.rs:3964–4031` already cover the spec-correct behaviour).
- For MSR sysm=0, use a new `MASK_NZCV_ONLY = 0xF000_0000` for `xpsr_mask` instead of `MASK_ALL_FLAGS` (Q is not in M0+'s APSR).
- MRS generator unchanged — B5/B6 fix means MRS sysm=8/9 will now agree with QEMU.

### Stage E.1.5 — harness mitigation (agent `a99eeec6`)

Two files changed: `lib.rs` (+3 lines: `MASK_NZCV_ONLY = 0xF000_0000` constant) and `thumb32_gen.rs` (narrow `fuzz_m0plus_msr` SYSm to `[0, 16]`, swap APSR comparison mask, retarget unit-test assertions).

- New private constant `M0PLUS_MSR_FUZZ_SYSM = [0, 16]` in `thumb32_gen.rs`. MSR generator samples from this; MRS keeps using the wider `M0PLUS_SYSM_ADMIT = [0, 8, 9, 16, 20]`.
- Doc-comment cites Stage E.1 verdict so future readers understand why MSR's set is narrower than MRS's.
- `MASK_NZCV_ONLY` used for sysm=0 (APSR) cases. Q flag (bit 27) excluded — not architectural on M0/M0+.
- Three unit-test bodies updated; `msr_apsr_uses_flag_mask` renamed `msr_apsr_uses_nzcv_mask`.

**Tests:** 7/7 m0plus_t32_fuzz, 16/16 thumb32_gen, 9/9 qemu_diff_m0plus. Build clean.

**Minor follow-up noted:** no symmetric constants test for `MASK_NZCV_ONLY` next to the existing `mask_all_flags_covers_nzcvq` / `mask_nz_only_covers_nz` blocks in `lib.rs`. Trivial to add if desired; not blocking.

### Stage E.2 — fuzz validation rerun (agents `ae8a01cb` + recovery `ac23bca6`)

Smoke 392/392 pass. **`--fuzz 10000`: 136,262 / 136,262 pass, 0 FAIL** (V2 had 2,277). 100k partial (killed at 12% / 165k cases) showed 0 FAIL up to that point. Stage E.1 + E.1.5 closed all V2 differentials.

### Stage E.3 — review (parallel, `a06dec2f` code-reviewer + `ac7532bf` devils-advocate)

**Code-reviewer:** clean. Spec verdict (B1–B4 are QEMU bugs, B5/B6 are real mdrp2040 bugs) confirmed independently. Three nice-to-have suggestions: handler-mode MRS PSP test, `MASK_NZCV_ONLY` constants test, debug_assert subset check.

**Devils-advocate found a blocker:** `MASK_NZCV_ONLY = 0xF000_0000` is rejected by `is_m0plus_safe` (admit list is `{0, MASK_ALL_FLAGS, MASK_NZ_ONLY}`). Every MSR sysm=0 fuzz case was silently filtered out. The v3 "clean" 10k run executed **zero** APSR-write fuzz cases. B1's Q-flag QEMU-divergence verdict had only the curated unit test as evidence. Coverage decrease vs V2: 1,299 cases dropped, all from MSR sysm 0/8/9/20.

Supervisor decision: kill the in-progress 100k (running on the broken-in-this-respect binary), bundle blocker + the three code-reviewer nits as Stage E.4.

### Stage E.4 — apply Stage E.3 feedback (agent `abd5339d`)

Five files changed:
- `qemu_diff_m0plus.rs:339` — admit list extended with `MASK_NZCV_ONLY`. `filter_admits_mask_nzcv_only` test added.
- `probe_diff_rp2040.rs:207` — same fix to keep the silicon-side filter in sync (V1 HLD §3.1 duplication discipline). `filter_admits_mask_nzcv_only` test added.
- `lib.rs:6051–6059` — `mask_nzcv_only_covers_nzcv` constants test.
- `thumb32_gen.rs:7941–7951` — `debug_assert!` that `M0PLUS_MSR_FUZZ_SYSM ⊆ M0PLUS_SYSM_ADMIT`. Tightens drift catch.
- `mdrp2040/tests.rs:4147–4170` — `mrs_psp_in_handler_returns_banked_psp` pinning the handler-mode bank-read corner.

**Tests:** qemu_diff_m0plus 10/10, probe_diff_rp2040 29/29, harness lib mask 11/11, thumb32_gen 16/16, mdrp2040 m0plus_msr_mrs 9/9. Workspace build clean.

100k fuzz needs to re-run on the corrected binary — the previous launch was killed.
