# RP2350 Peripheral Coverage — Implementation Journal

Supervising the implementation of `wrk_docs/2026.04.15 - HLD - RP2350 Peripheral Coverage V5.md`.

## Orchestration model

- Supervisor (me): high-level guidance only. No direct coding/testing/debugging.
- Per phase:
  1. **Implementer** agent writes the code with TDD discipline (unit tests first, run `cargo test -p mdrp2350` + touched harness crates). Produces a summary of what landed.
  2. **code-reviewer** agent using the `code-reviewer` skill reviews the delta.
  3. **code-review-apply-feedback** agent using the `code-review-apply-feedback` skill acts on Required / Suggested items.
  4. **Devil's advocate** agent challenges remaining assumptions and surfaces missed edges.
  5. **Final**: commit the phase as an atomic commit, update this journal, move to the next phase.
- **Hardware-gated items** (probe-rs / real RP2354 silicon) cannot be run by agents. These are surfaced explicitly — Arthur runs them on the lab rig.

## Plan (from V5)

| Phase | Scope | Impl days | Author hrs |
|---|---|---|---|
| 0a | NVIC banks per-core, BASEPRI fold, external-IRQ poll, build_image mode-set, 3 ISR scenarios | ~8 | ~4 |
| 0b | mmio_trace_rp2350, retry wrapper, red-path scenarios, CLOCKS audit, bootrom NS trace, paced_bench baseline | ~4 | ~4 |
| 1 | irq_pending[u64;2], tick_peripherals, IRQ drain, RESETS guard, TICKS, TIMER0/1 | ~5 | ~8 |
| 2 | UART0, SPI0, I2C0, ADC, PWM, IO_BANK0/PADS_BANK0 plain-storage | ~13 | ~20 |
| 3 | DMA 16ch, ring, chain-trigger, DMA_IRQ_0/1 | ~8 | ~12 |

## Running log

### 2026-04-15 — Kickoff

- Task list created (#1..#6) with dependency chain 1→2→3→4→5→6.
- Journal initialized.
- Dispatching Phase 0a implementer next.

### 2026-04-15 — Phase 0a complete

**Implementer** landed: `irq.rs` (new, +264 — IRQ constants, CORE_LOCAL_IRQS); `bus/ppb.rs` (+488 — per-core NVIC banks ISER/ICER/ISPR/ICPR/IABR = 2 u32, IPR = 13 u32); `core/exceptions.rs` (+66 — BASEPRI fold, try_take_external_irq); `core/mod.rs` (+15 — third async block, short-circuit); `bus/mod.rs` (+46 — irq_pending: [u64;2], assert_irq_core); `tests.rs` (+248 — 13 new unit tests); `isr_scenarios.rs` (+998/-102 — mode-set + 3 external-IRQ scenarios). Bumped `mdrp2350 0.5.4 → 0.6.0`.

**Code reviewer (code-reviewer skill) + devil's advocate** found four blockers and three required tests:
- B1: MMIO writes to NVIC_ISPR/ICPR didn't mirror into `bus.irq_pending` — all three new scenarios pend via MMIO, so they'd fail on EMU.
- B2: Async-vs-external arbitration was split — ARMv8-M requires unified priority ordering. Unified into `try_take_any_pending_exception`.
- B3: `CORE_LOCAL_IRQS` defined but never consulted. Added `assert_irq_shared`, `clear_irq_core`, `clear_irq_shared`, `is_core_local_irq`.
- B4: Vector default-fill only went slot 16..=47 — extended to 64, image layout restructured (handler offsets 0x100/0x104/0x140/0x180).
- R1–R3: Missing tests for MMIO-ISPR-dispatch, BASEPRI-no-op, end-to-end preempt. Added.
- Four suggestions fixed (stale dead_code, impl merge, test rename, clippy).

**Apply-feedback agent** landed all fixes. Final state:
- `cargo test -p mdrp2350 --lib` — **636 passed** (was 628 at impl handoff, +8 net after adding tests/deleting one made obsolete by B2).
- `cargo test -p mdpicoem-harness --lib` — **292 passed**.
- `cargo test -p mdrp2040 --lib` — **501 passed** (untouched).
- `cargo build --release` on all crates except `epio-sys` (pre-existing clang gap) — zero new warnings.
- `cargo clippy -p mdrp2350 --lib` — **34 → 31** (all 3 fixed were Phase 0a-introduced).
- `qemu_diff_m33 --fuzz 1000` — 39640/39640 pass.

**Hardware-gated, flagged for Arthur's rig:**
- Pre-flight `silicon_isr_diff_rp2350` filter `isr_pendsv_cold|isr_tail_chain|isr_lazy_fp_save|isr_eager_fp_save` — confirm commit `2ed1c7c`'s ICSR poll left them green.
- New external-IRQ scenarios `isr_ext_irq_timer0_cold`, `isr_ext_irq_masked_pending`, `isr_ext_irq_priority_preempt` green on real RP2354.

**Committed** as atomic commit (see git log).

### 2026-04-15 — Phase 0b complete

**Implementer** landed: `mmio_trace_rp2350` binary (runtime `Bus::trace_enabled` flag, `#[cold] #[inline(never)] emit_trace`, all six access methods hooked, per-core `active_pc` + sentinel PCs `0xFFFF_FFFE`/`0xFFFF_FFFD` on exception entry/exit); `run_scenario_with_retry` + `is_transient_probe_error` ported from RP2040 (`probe_rs::Error::Probe | Timeout` matcher); `--red-path` CLI flag + `RED_PATH_SCENARIOS` catalogue (3 scenarios); CLOCKS `_SELECTED` handshake audit (clk_gpout0..3/peri/hstx/usb/adc now synthesize, matching RP2040 `b1a40e4`). Bumped `mdrp2350 0.6.0 → 0.6.1` (later → 0.7.0 per review) and `mdpicoem-harness 0.24.0 → 0.25.0`.

**Reviewers found:**
- V5 §4.2.8's specific red-path register choices (SIO_GPIO_OUT bit, XOSC_CTRL write) don't actually diverge — those registers are correctly modelled. Only 1 of 3 scenarios (TIMER0 TIMERAWL) was a genuine red-path.
- Sentinel PC paths (`0xFFFF_FFFE`/`0xFFFF_FFFD`) had no test coverage — regression risk.

**Apply-feedback** replaced the 2 weak scenarios with genuine red-paths:
- `red_uart0_fr_at_reset_unmodelled` — UART0_UARTFR (0x4007_0018) expected TXFE|RXFE=0x90 post-reset; EMU HashMap returns 0, silicon PL011 reports both FIFOs empty. Diverges on bits 4 & 7.
- `red_adc_cs_ready_unmodelled` — ADC_CS (0x400A_0000) READY bit (0x100) post-enable; EMU stores 0x1 verbatim (READY never set), silicon asserts READY after EN. Diverges on bit 8.

Plus 2 sentinel-PC tests (`trace_exception_entry_publishes_sentinel_fe`, `trace_exception_exit_publishes_sentinel_fd`). Version bumped to `mdrp2350 0.7.0` (public `trace_enabled` field is a backward-compatible addition).

Final state:
- `cargo test -p mdrp2350 --lib` — **644 passed** (was 636; +8 for Phase 0b tests).
- `cargo test -p mdpicoem-harness --lib` — **303 passed** (was 292; +11 trace + retry + red-path tests).
- Full workspace: **1703 passed, 0 failed**.
- `cargo clippy -p mdrp2350 --lib` — 33 (pre-existing baseline; no Phase 0b warnings).
- `qemu_diff_m33 --fuzz 1000` — green.

**Hardware-gated, flagged for Arthur's rig:**
- `mmio_trace_rp2350 --cycles 1_000_000 hello_timer.elf` (V5 §4.2.7).
- `mmio_trace_rp2350` over `roms/rp2350/arm-bootrom.elf` (V5 §4.2.11 — requires `.bin` rebuild since ELF loader not implemented; consistent with RP2040 precedent).
- `paced_bench_rp2350` baseline pin in journal (V5 §4.2.12 / §9.8).
- `silicon_periph_diff_rp2350 --red-path` on RP2354 — expect 3 FAILs with correct `first_divergence`.

**Committed** as atomic commit (see git log).

### 2026-04-15 — Phase 1 complete

**Implementer** landed: `peripherals/{mod,ticks,timer,phase1_tests}.rs` — TICKS (6 domains, fixed-point accumulator per domain, CYCLES=0 halt, consumer API via `take_timerN_edges`); TIMER0 @ `0x400B_0000` + TIMER1 @ `0x400B_8000` (64-bit µs counter, 4 alarms each, INTR/INTE/INTF/INTS W1C, lazy scheduling); RESETS Bus-level guard inline in all six access methods; per-cycle `tick_peripherals(sys_clks)` called from `Emulator::step`; post-bootrom reset-state seeding; `mdpicoem-common/src/clocks.rs` new constants `RP2350_SYS_CLK_HZ=150_000_000`, `RP2350_ADC_CLK_HZ=48_000_000`. Versions: `mdrp2350 0.7.0 → 0.8.0`, `mdpicoem-common 0.9.0 → 0.10.0`.

**Reviewers found 5 blockers + 4 required items:**
- B1/B2: V5 §6 Phase 1 exit criteria required two silicon scenarios (TIMER alarm-fire + TICKS retarget via custom_sled) — not authored.
- B3: `hello_timer.bin` (V5 §3 Phase 1 corpus) not generated.
- B4: Phase 0b's `red_timer0_timerawl_unmodelled` scenario became a lying witness once TIMER0 was modelled (PASSes both sides now).
- B5: `Bus::new` / `Emulator::new` / `Emulator::reset` had three different clock states — V5 §5.7 mandates a single post-bootrom table.
- R1: `invalidate_lazy` silently disarmed active alarms on TICKS rate change — not datasheet behaviour.
- R3: Comment arithmetic bug in `emulator_step_calls_tick_peripherals_each_iteration`.
- R4: Missing reset-state tests for PLL_*.CS.LOCK, `clk_*_selected` mirror.

**Apply-feedback landed all 13 items:**
- Authored `timer0_alarm0_fire_and_clear` + `ticks_timer0_retarget_halves_rate` silicon scenarios (custom_sled Thumb bytes; silicon-validated later by Arthur).
- Replaced lying red-path with `red_spi0_sspsr_tfe_unmodelled` (SPI0 still in HashMap fallthrough).
- Authored `roms/rp2350/gen_hello_timer.py` + 256-byte `hello_timer.bin`; 2 integration tests in `crates/mdrp2350/tests/hello_timer.rs` verify the emulator executes it correctly.
- B5 resolved via option (c): `Bus::new()` now seeds post-bootrom clock table directly; `Emulator::new`/`::reset` land on identical state (asserted by `bus_new_equals_emulator_reset_clock_state`).
- `invalidate_lazy` made a true no-op (TIMER state is µs-based, rate-invariant); silicon doesn't disarm on TICKS rate change.
- 14 reset-state tests covering clk_sys, clk_ref, XOSC.STABLE, PLL_*.LOCK, clk_*_selected, TICKS.CYCLES, RESETS.
- Nit cleanups: rename `ticks_enable_toggle` → `ticks_ctrl_enable_set`, remove dead import, docstring fixes.

**Final state:**
- `cargo test --workspace --exclude epio-sys` — **1793 passed, 0 failed** (was 1703; +90 net).
- `mdrp2350` lib: 732 tests + 2 hello_timer integration tests.
- `mdpicoem-harness`: 303 tests (2 new scenarios).
- `cargo clippy -p mdrp2350 --lib` — 42 warnings (baseline 42; zero new).
- `qemu_diff_m33 --fuzz 1000` green.
- `cargo build --release --workspace --exclude epio-sys` zero warnings.

**Hardware-gated, flagged for Arthur's rig:**
- Run `hello_timer.bin` on RP2354 and diff stop-state vs emulator (V5 §6 Phase 1 exit).
- Validate `timer0_alarm0_fire_and_clear` + `ticks_timer0_retarget_halves_rate` on silicon.
- Confirm `red_spi0_sspsr_tfe_unmodelled` FAILs on the rig.
- `paced_bench_rp2350` baseline still pending per Phase 0b.

**Committed** as atomic commit (see git log).

### 2026-04-15 — Phase 2 complete

**Implementer** landed: `peripherals/{uart,spi,i2c,adc,pwm,io_bank0,pads_bank0,phase2_tests}.rs` — full PL011 UART0 @ `0x4007_0000`, PL022 SPI0 @ `0x4008_0000` with loopback, DesignWare I2C0 @ `0x4009_0000` with NACK-default scan, ADC @ `0x400A_0000` with fixed-point `clk_adc` scaling (48 MHz nominal), PWM @ `0x400A_8000` (12 slices with `PWM_IRQ_WRAP_0` for 0..7 / `PWM_IRQ_WRAP_1` for 8..11), plain-storage IO_BANK0 @ `0x4002_8000` (48 GPIO CTRL/STATUS pairs + INT blocks) and PADS_BANK0 @ `0x4003_8000` (VSEL + 48 pads); `bus/mod.rs` hooks — new peripheral fields, narrow-access dispatch for UARTDR / SSPDR / IC_DATA_CMD / ADC FIFO on read8/write8/read16/write16, subword-alias-preserving word-RMW for the rest, `raise_irqs_u64` helper folding peripheral `&mut u64` masks into `assert_irq_shared`, tick_peripherals wiring, post-bootrom RESETS release for UART0/SPI0/I2C0/ADC/PWM; 5 new silicon scenarios + 3 replaced red-paths; `roms/rp2350/gen_hello_uart.py` + `gen_hello_pwm.py` authored with 4 integration tests. Bumped `mdrp2350 0.8.0 → 0.9.0`.

**V5 deviation (flagged)**: V5 §6 Phase 2 row quotes `0x4005_0000` as the PWM base. That's the RP2040 address; on RP2350 `0x4005_0000` is PLL_SYS. Per datasheet §2.3 Table 6 and pico-sdk-pico2 `addressmap.h`, the correct RP2350 PWM base is `0x400A_8000`, which is what this implementation uses. Documented in `crates/mdrp2350/src/peripherals/pwm.rs` header and `silicon_scenarios.rs` `PWM_BASE_RP2350`. Arthur: consider updating V5 to match.

**Firmware authored (option a)**: `hello_uart.bin` + `hello_pwm.bin` (gen scripts; 256 bytes each; self-contained SRAM firmware with vector table + reset handler). Integration tests `crates/mdrp2350/tests/hello_uart.rs` + `hello_pwm.rs` exercise them on the emulator. **Deferred to Arthur (option b)** for SDK-source firmware: `hello_adc.bin`, `spi_master.bin`, `bus_scan.bin` — these touch more of the pico-sdk ecosystem (printf, GPIO bring-up, RX stimulus for SPI) than a hand-coded minimal firmware can validate, and Arthur can pull them from pico-examples-pico2.

**Silicon scenarios authored (hardware-gated for Arthur)**:
- `uart0_tx_single_byte` — enable UART0, write one byte, observe UARTFR.TXFE=1.
- `spi0_loopback_single_byte` — enable+LBM, write 0xA5, observe SSPDR readback.
- `i2c0_bus_scan_reserved_nack` — target 0x7F (I2C-reserved), expect TX_ABRT_SOURCE.ABRT_7B_ADDR_NOACK. (Renamed from `i2c0_bus_scan_nack` per code-review feedback R2 — the prior 0x3C target collided with common SSD1306 OLED displays that would ACK on rigs with a display attached.)
- `adc_one_shot` — EN+START_ONCE, observe CS.READY.
- `pwm_wrap_irq` — slice0 TOP=100, observe INTR bit 0 set after wrap.

**Red-path replacements** (Phase 0b witnesses retired once peripherals got modelled):
- `red_uart1_fr_at_reset_unmodelled` — UART1 @ 0x4007_4000 still in HashMap.
- `red_trng_imr_unmodelled` — TRNG @ 0x400F_0100 unmodelled.
- `red_sha256_csr_wfifo_ready_unmodelled` — SHA256 @ 0x400F_8000 unmodelled.

**Final state:**
- `cargo test --workspace --exclude epio-sys` — **1930 passed, 0 failed** (was 1793; +137 net; 112 mdrp2350 unit + 28 phase2_tests + 4 hello_* firmware + 5 silicon_scenarios + net churn).
- `mdrp2350` lib: 844 tests + 4 hello_* integration + 12 lazy_fp.
- `mdpicoem-harness`: 303 tests (5 new scenarios, 3 red-paths rotated).
- `cargo clippy -p mdrp2350 --lib` — 42 warnings (same as Phase 1 baseline; zero new Phase 2 warnings).
- `cargo build --release -p mdrp2350 -p mdrp2350app` — zero warnings.
- `qemu_diff_m33 --fuzz 1000` — 39677/39677 pass.

**Hardware-gated, flagged for Arthur's rig:**
- 5 new silicon scenarios (uart0/spi0/i2c0/adc/pwm) on live RP2354.
- 3 replaced red-paths FAIL on rig (`--red-path` filter).
- `hello_uart.bin` / `hello_pwm.bin` running to completion on RP2354 — test_silicon invocation or manual.
- `hello_adc.bin` / `spi_master.bin` / `bus_scan.bin` firmware pull from pico-examples-pico2 (deferred here).

**Committed as atomic commit (see git log).**

### Hardware-gated validations (tracked for Arthur's rig)

- Pre-flight `silicon_isr_diff_rp2350` at HEAD (validates commit `2ed1c7c`'s ICSR poll covers PendSV/SysTick).
- Phase 0a's 3 external-IRQ scenarios green on silicon.
- Phase 0b's `mmio_trace_rp2350` over `hello_timer` + `arm-bootrom.elf`.
- Phase 0b `paced_bench_rp2350` baseline measurement.
- Phases 1/2/3 `silicon_periph_diff_rp2350` scenarios green against live silicon.

Agent-runnable validations: `cargo test --workspace`, `cargo build --release`, `qemu_diff_m33` / `qemu_diff_m0plus` short smoke runs.

### 2026-04-16 — Phase 3 complete

**Implementer** landed: `dma.rs` (1190 lines, 24 tests) — 16-channel DMA at `0x5000_0000` with CTRL_TRIG, RING_SIZE/RING_SEL, CHAIN_TO, CH_ABORT, INTE0/INTS0 + INTE1/INTS1 W1C, flat arbitration (lowest-channel-wins); `dreq.rs` (187 lines) — DREQ matrix; `collect_dreqs()` on Bus; `tick_dma()` swap pattern; DMA dispatch; DREQ accessors on uart/spi/i2c/adc/pio. 2 silicon scenarios: `dma_mem_to_mem_32bit`, `dma_chain_trigger`. Firmware: `gen_hello_dma.py` + `hello_dma.bin` (190 B) + 1 integration test. `mdrp2350 0.9.0 → 1.0.0`.

**1966 workspace tests pass** (was 1935; +31). Clippy zero new. Committed as atomic commit.
