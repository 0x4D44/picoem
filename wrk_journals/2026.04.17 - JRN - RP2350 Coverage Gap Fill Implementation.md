# 2026.04.17 — JRN — RP2350 Coverage Gap Fill Implementation

## Goal

Implement `wrk_docs/2026.04.17 - HLD - RP2350 Coverage Gap Fill V5.md` via an
agent team. Supervisor (me) does not touch code directly — dispatch, track,
redirect, tie-break.

## Team roles per step

- **Implementer agent** — writes code against the HLD's spec for the current
  step. Uses TDD where appropriate (write tests first, then implementation).
- **Code-reviewer agent** — uses the `code-reviewer` skill against the
  implementer's diff.
- **Apply-feedback agent** — uses the `code-review-apply-feedback` skill to
  action the review comments.
- **Devil's-advocate agent** — challenges the result of each stage
  independently. Raises implicit assumptions, missed corner cases.

## Landing plan (from V5 §10)

1. **Step 1** — Part A.1 warn-once + A.3 TICKS alias fix + Bus field for
   watchdog reset flag.
2. **Step 2** — UART1/SPI1/I2C1 array reshape.
3. **Step 3a** — SYSCFG / TBMAN / GLITCH_DETECTOR inert cluster + PSM +
   WATCHDOG (with reset plumbing).
4. **Step 3b** — OTP (storage) + TRNG (counter) + SHA-256 (`sha2` crate).
5. **Step 4** — §4.A2 inert-register convention (ten warn sites) + POWMAN +
   CORESIGHT_TRACE.
6. **Step 5** — Interpolator CTRL semantics (largest diff).
7. **Final** — plan-vs-implementation audit.

Each step: implement → review → apply → devil's advocate → commit.

## Progress log

### 23:40 — Kickoff

HLD V5 saved. Implementation journal seeded. Task list populated (7 impl
steps + final audit). Starting Step 1.

### 00:20 — Step 1 implementer returned

Files: `Cargo.toml` (+3, v1.0→1.1), `bus/mod.rs` (+62: warned_addrs field,
watchdog_reset flag + API, warn instrumentation at 7 fallthrough sites),
`peripherals/ticks.rs` (+14, regression test), `tests.rs` (+126, 5 tests in
`bus_observability` submodule). 901 tests pass.

**HLD discrepancies found:**
- §4.A1 / §9.1 list six fallthrough sites; actual count is **seven**
  (32-bit write arm missed). Implementer instrumented all seven — correct
  by HLD intent ("At every fallthrough site"). Need HLD line-list amendment.
- §4.A3 TICKS `0x81C` fix **was already landed** at `ticks.rs:207` (commit
  `997f6ba`). The silicon-scenario residual is a different issue
  (CYCLES-readback register model, see `tech_debt.md:998`) — out of scope.
  Implementer added a pure-unit regression test instead.

**Concurrent-agent collision.** Another agent is mid-flight on RISC-V
Hazard3 (changing `Emulator.cores` from `[CortexM33; 2]` to `Cores` enum,
WIP uncommitted, causing ~125 pre-existing test-compile errors). Per
CLAUDE.md multi-agent rule, implementer did not fix the other agent's
bugs; they isolated via a backup-and-restore dance to verify step 1
correctness. Flag: step 2 (UART/SPI/I2C reshape) also touches Bus
construction — need to be careful about ordering if the other agent's
work lands first.

Next: code-reviewer on step 1 diff, then apply-feedback, then
devil's-advocate.
